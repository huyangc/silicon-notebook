# silicon-notebook 算法逻辑与功能清单

更新日期：2026-05-29 ｜ 以代码为准（`backend/app/**`、`frontend/app/page.tsx`）

> 本文件把**当前重要的算法逻辑**与**所有涉及到的功能/函数/接口/数据表**集中到一处。
> 算法常量、阈值、函数签名均逐行核对过源码。配套总览见 [architecture.md](architecture.md)。

---

## 第一部分：核心算法逻辑

主链路（任意上传内容生效的闭环）：

```
创建 notebook → 上传 source（PDF/MD/DOCX/PPTX）
 → [异步 BackgroundTask] parse 解析 → embed 嵌入 → extract 抽取
 → 候选进 Review Queue，curator approve/reject/edit → knowledge_objects
 → 知识治理（状态机 / 去重合并 / 冲突）
 → 混合检索（关键词 + 向量 + 场景 boost）
 → 结构化问答 ask / scenario / case / checklist（citation 校验）
 → Article Studio 研究简报（claims + 关系 + 派生规则候选）
 → 用户反馈 👍/👎
```

> LLM / embedding / MinerU 任一未配置 → 走 **deterministic 回退**，整条链路离线可跑。

---

### 1. CJK 感知分词 `_tokens`（`retrieval.py`）——所有打分的基石

被**检索打分、抽取证据绑定、场景 boost、去重/冲突**共用。

- 非字母数字字符统一替换为空格、转小写后按 chunk 切分。
- 每个 chunk 走 `_segment_tokens`：
  - **CJK 连续串**（`_is_cjk`：CJK 统一/扩展 A/兼容/假名区段）：长度 1 → uni-gram；长度 ≥2 → 滑动 **bi-gram**（`run[k:k+2]`）。
  - **拉丁/数字串**：长度 >1 → 整串作为一个 token。
- 设计目的：中文为主的语料能真正被关键词召回。

派生函数：
- `keyword_score(query, text)` = query token 集合在 text 中命中占比（0..1）。
- `token_overlap(span, text)` = span token 在 text 中命中占比（用于证据绑定）。
- `cosine(a, b)` = 纯 Python 余弦（长度不等/零向量→0）。

---

### 2. 混合检索打分 `score_knowledge` / `score_elements`（`retrieval.py`）——最关键算法

**融合公式 `_fuse`（按激活信号重归一化）：**
```
denom = W_KEYWORD + (W_SEMANTIC if has_vector else 0)
fused = (W_KEYWORD*keyword + (W_SEMANTIC*semantic if has_vector else 0)) / denom
```
- 权重：`W_KEYWORD = 0.4`、`W_SEMANTIC = 0.6`。
- **重归一化关键点**：纯关键词对象的 denom 只含 0.4，分数仍落在 0..1，不被 `W_KEYWORD` 上限压制。
- `query_vector` 为 None（未配置 embedding）→ 自动退化为纯关键词，行为可复现。

**语义分来源**：query 向量与「对象 payload 向量 `knowledge_vectors`」或「任一证据 element 向量 `element_vectors`」的**最佳 cosine**。

**结构化场景 boost `structured_boost`（软加权，不硬过滤）：**
```
boost = 命中场景字段数 / 提供的场景字段数          # 0..1
final = relevance * (1 + SCENARIO_BOOST_ALPHA * boost)
```
- `SCENARIO_BOOST_ALPHA = 0.5`。
- 场景 9 字段 `SCENARIO_FIELDS`：`domain / block_type / package_type / design_stage / signal_type / concern / constraint / process_or_node / application`。
- boost 目标字段：规则 payload 的 `applies_to / condition / title / use_when`。

**噪声地板**：`RELEVANCE_FLOOR = 0.12`，低于即丢弃（knowledge 与 element 都用）。

**类型权重 `_TYPE_WEIGHT`**（仅用于跨类型排序/分组，**不**乘进同类相关度）：
`rule 1.0 > case 0.9 > checklist 0.8 > method 0.7 > risk 0.6 > glossary 0.4`。

---

### 3. 文档解析（`parsers.py` + `mineru_client.py`）

- 按扩展名分发：Markdown（heading/paragraph/list_item）、DOCX（paragraph/table_row）、PPTX（每 shape `slide_text` + `speaker_notes`）、PDF、纯文本回退。
- **MinerU 适配（PDF 与 GPU 解耦，`MINERU_MODE`）**：
  - `http` → POST 远端 `mineru-api` `/file_parse`；`cli` → subprocess 本机 `mineru`（Apple Silicon 自动 MLX）；`off`（默认）→ pypdf 纯文本。
  - `mineru_content_list_to_elements` 映射 `content_list`：公式→`formula`（保留 LaTeX、去 `$$`）、表格→`table`（HTML 存 `metadata.table_html`，正文展平）、标题保留 `text_level`、`page_idx` 转 1-based。
  - MinerU 不可达/报错/产出空 → **静默回退 pypdf**，上传永不阻塞；PDF 解析 0 元素时提示"疑似扫描件"。
- 每个 `SourceElement` 带 `location_label`，作为 evidence/citation 锚点。

---

### 4. 嵌入（`sqlite_repository._embed_source / _embed_query / _embed_knowledge`）

- `embedding_configured` 时，解析后对每个 element 调 `llm_client.embed(text[:2000])`，向量 JSON 存 `element_embeddings`；payload 级向量存 `knowledge_embeddings`（编辑 payload 时 `_embed_knowledge` 重嵌入）。
- 未配置则跳过，检索退化为纯关键词。

---

### 5. 知识抽取 `run_extraction`（`extraction.py`）

两条路径，统一去重 + 按 confidence 排序：

**LLM 路径（配置后）**
- **分窗 `_element_windows`**：`WINDOW_MAX_CHARS=9000` + `WINDOW_OVERLAP_CHARS=500` 覆盖**整篇**；`MAX_WINDOWS=0` 即不限窗（可设上限控成本），保证前向进展不卡死。
- 逐窗 `chat_json` 抽取 6 类（`_LLM_SECTIONS`：rule/method/risk/case/checklist/glossary），跨窗 `_dedupe_records`。
- **证据绑定 `bind_evidence`**：① 归一化后精确子串包含 → ② CJK `token_overlap ≥ BIND_OVERLAP_THRESHOLD(0.6)` 模糊回退（容忍改写/标点差异，置信度 -0.1）。绑定成功 `status=candidate`，否则 `needs_review`。
- 证据始终对**全量 element**绑定（不限本窗），最大化召回。

**启发式回退（离线）`_extract_with_heuristics`**
- **句子级**切分 `_sentences`（一段可多候选，句长≥12）。
- `_classify_sentence` 优先级：`rule > case > checklist(或 list_item) > risk > method`，正则 `_RULE/_RISK/_CASE/_METHOD/_CHECK_PATTERNS`（中英双语）。
- rule 用 `_split_recommendation_risk` 拆 recommendation 与 `risk_if_ignored`（否则/以免/or risk/to avoid/otherwise），`_detect_scope` 抽轻量 `applies_to`（`_SCOPE_TERMS` 双语领域词）。
- **glossary 仅从定义句式** `_glossary_from_text`（`术语：定义` / `is defined as|refers to|means|是指|定义为`）。
- **`_NOISE_TYPES`**（heading/table/formula/image_caption/speaker_notes）**不**走指令/案例启发式（消除旧版"每标题→空 glossary"噪声）。

**去重 `_dedupe_records`**：按 (type, 归一化主文本) 去重；主文本取 title/name/term/question/symptom/statement。候选带 `confidence` 与 `extraction_mode`(llm/heuristic)。

**JSON 稳健（`llm.py chat_json`）**：`response_format=json_object`（不支持则回退）+ `strip_json_fences` 去围栏。

---

### 6. 知识治理（`sqlite_repository.py`）

- **状态机** `KNOWLEDGE_STATUSES = approved/reviewed/deprecated/conflict/project_specific`；
  仅 `USABLE_STATUSES`(approved/reviewed/project_specific/conflict) 进检索/回答，`deprecated` 排除。
- `update_knowledge`：改 status/owner/payload；改 status 时盖 `last_reviewed`；改 payload 时重嵌入向量。
- **去重检测 `find_duplicates`**：同类型两两 `_knowledge_similarity` = `max(双向 keyword_score, 证据向量最佳 cosine × 0.95)`，**阈值 ≥0.6 成组**（排除 deprecated）。
- **合并 `merge_knowledge`**：同类型才可合；并合 evidence（按 element_id+quoted_span 去重），源对象置 `deprecated`。
- **冲突检测 `find_conflicts`**：规则对 `scope_sim ≥ 0.5` 且 `rec_sim < 0.2`（scope=title+applies_to，rec=recommendation 或 statement，均用双向 keyword_score）→ 判为「范围重合但取向相反」。

---

### 7. 问答 `ask` + `scenario_query` / `case_search` / `checklist`

1. query = 问题 + scenario 各非空字段拼接。
2. 取各类型 `USABLE` 知识 + 全量 elements，`_embed_query` 出 query 向量。
3. `score_knowledge` 截断：**rules 5 / cases 4 / checklist 6 / methods 4 / risks 4**；`score_elements` 默认 limit 8。
4. **LLM 模式**：`_answer_with_llm` 生成 conclusion/applicable_scenario/methods/risks/checklist/missing_information，`llm_mode=configured`；失败或无 LLM → **模板组装**检索结果，`llm_mode=deterministic`。
5. **Citation 校验**：引用 element_id 必须能回查到有效元素（`valid_element_ids`），否则丢弃。
6. **§12 冲突提示**：命中 `status=conflict` 规则时，在 `missing_information` 追加 owner-review 提示。
7. 保存 `answers` 返回 `answer_id`（供反馈关联）。

- `scenario_query`：9 字段拼场景后走 ask。
- `case_search` / `checklist`：走对应类型检索；checklist 无命中时从 rules 兜底生成。
- `explain_rule`（§6.10）：规则回溯 origin 证据 + 关联 cases(3)/risks(3)/checklist(5)，返回 `RuleExplanation`。

---

### 8. Article Studio `research_article`（`sqlite_repository.py`）

- 文章作为 source（标题+摘要）；LLM 或句子级回退抽 claims（带 evidence）。
- `_attach_claim_relationships`：每条 claim 用 `keyword_score` 找最佳规则，启发式判 `relation_type`(supports/extends/refines/challenges…) + `related_rule_id` + `implication`。
- 产出 `core_contribution / claims / limitations / notebook_relationships / derived_rule_candidates(rationale+evidence) / validation_plan`，持久化 `article_claims` / `derived_rule_candidates`。

---

### 9. LLM 客户端与日志（`llm.py`）

- OpenAI 兼容；`chat_json`（json_object + fence 清理）、`embed`、`configured` 属性。
- 每次调用写 `.local/logs/llm.jsonl`（状态/延迟/token/错误）。

---

## 第二部分：完整功能 / 函数清单

### A. HTTP API（`backend/app/api/routes.py`，39 条，`/api` 前缀）

| 分组 | 方法 路径 | 响应模型 |
|---|---|---|
| 系统 | `GET /health` | — |
| 系统 | `GET /me` | UserProfile |
| Notebook | `GET /notebooks` | List[NotebookSummary] |
| Notebook | `POST /notebooks` | NotebookSummary |
| Notebook | `GET /notebooks/{id}` | NotebookSummary |
| Notebook | `PATCH /notebooks/{id}` | NotebookSummary |
| Notebook | `DELETE /notebooks/{id}` | 204 |
| Source | `GET /notebooks/{id}/sources` | List[SourceSummary] |
| Source | `POST /notebooks/{id}/sources/import` | List[SourceSummary] |
| Source | `POST /notebooks/{id}/sources`（上传 multipart） | List[SourceSummary] |
| Source | `GET /sources/{id}` | SourceDetail |
| Source | `POST /sources/{id}/parse` | SourceSummary |
| Source | `GET /sources/{id}/elements` | List[SourceElement] |
| Source | `DELETE /sources/{id}` | 204 |
| Source | `POST /sources/{id}/extract` | List[Candidate] |
| 候选审核 | `GET /notebooks/{id}/candidates` | List[Candidate] |
| 候选审核 | `GET /notebooks/{id}/candidates/{type}` | List[Candidate] |
| 候选审核 | `PATCH /candidates/{id}` | Candidate |
| 候选审核 | `POST /candidates/{id}/approve` | Candidate |
| 候选审核 | `POST /candidates/{id}/reject` | Candidate |
| 知识浏览 | `GET /notebooks/{id}/rules` | List[RuleCard] |
| 知识浏览 | `GET /notebooks/{id}/rules/{rule_id}/explain` | RuleExplanation |
| 知识浏览 | `GET /notebooks/{id}/methods` | List[MethodCard] |
| 知识浏览 | `GET /notebooks/{id}/risks` | List[RiskItemCard] |
| 知识浏览 | `GET /notebooks/{id}/glossary` | List[GlossaryTermCard] |
| 知识治理 | `PATCH /knowledge/{id}` | — |
| 知识治理 | `GET /notebooks/{id}/duplicates` | List[DuplicateGroup] |
| 知识治理 | `GET /notebooks/{id}/conflicts` | List[ConflictPair] |
| 知识治理 | `POST /knowledge/{id}/merge` | — |
| 检索/问答 | `GET /notebooks/{id}/search` | NotebookSearchResponse |
| 检索/问答 | `POST /notebooks/{id}/ask` | AskResponse |
| 检索/问答 | `POST /notebooks/{id}/scenario-query` | AskResponse |
| 检索/问答 | `POST /notebooks/{id}/case-search` | List[CaseCard] |
| 检索/问答 | `POST /notebooks/{id}/checklist` | List[ChecklistItem] |
| Article | `GET /notebooks/{id}/articles` | List[ArticleSummary] |
| Article | `POST /notebooks/{id}/articles` | ArticleSummary |
| Article | `DELETE /articles/{id}` | 204 |
| Article | `POST /articles/{id}/research` | ArticleResearchBrief |
| 反馈 | `POST /answers/{answer_id}/feedback` | FeedbackResponse |

### B. 仓库业务方法（`sqlite_repository.py`，业务核心）

- 用户/Notebook：`current_user` · `list_notebooks` · `create_notebook` · `get_notebook` · `update_notebook` · `delete_notebook`
- Source：`list_sources` · `get_source` · `import_sources` · `upload_sources` · `process_source`（异步 parse→embed→extract，含 `stage()` 状态推进）· `parse_source` · `source_elements` · `delete_source`
- 抽取/候选：`extract_source` · `list_candidates` · `update_candidate` · `approve_candidate` · `reject_candidate`
- 知识浏览：`list_rules` · `list_methods` · `list_risks` · `list_glossary` · `explain_rule`
- 知识治理：`update_knowledge` · `find_duplicates` · `merge_knowledge` · `find_conflicts`（辅助 `_knowledge_similarity` · `_knowledge_ref` · `_knowledge_headline` · `_payload_join`）
- 检索/问答：`search_notebook` · `ask` · `_answer_with_llm` · `scenario_query` · `case_search` · `checklist` · `submit_feedback`
- Article：`list_articles` · `create_article` · `delete_article` · `research_article`
- 向量辅助：`_embed_source` · `_embed_query` · `_embed_knowledge` · `_element_vectors` · `_knowledge_vectors`

### C. 检索打分函数（`retrieval.py`）

`_is_cjk` · `_segment_tokens` · `_tokens` · `_normalize` · `cosine` · `keyword_score` · `token_overlap` · `_fuse` · `structured_boost` · `score_knowledge` · `score_elements` · `_payload_text`
数据类：`RetrievedKnowledge` · `RetrievedElement`

### D. 抽取函数（`extraction.py`）

`run_extraction`（入口）· `_extract_with_llm` · `_extract_with_heuristics` · `_element_windows` · `_parse_llm_records` · `_records_from_llm_section` · `_dedupe_records` · `bind_evidence` · `_make_evidence` · `_sentences` · `_first_sentence` · `_classify_sentence` · `_heuristic_payload` · `_split_recommendation_risk` · `_glossary_from_text` · `_detect_scope` · `_normalize`
数据类：`CandidateRecord`

### E. SQLite 数据表（15）

`users` · `user_profiles` · `notebooks` · `sources` · `source_elements` · `articles` · `extraction_runs` · `extraction_candidates` · `element_embeddings` · `knowledge_embeddings` · `knowledge_objects` · `answers` · `feedback` · `article_claims` · `derived_rule_candidates`

### F. Pydantic Schema（`models/schemas.py`）

- 用户/证据/元素：`UserProfile` · `Evidence` · `SourceElement`
- Source：`SourceSummary` · `SourceImportFile` · `SourceImportRequest` · `SourceDetail`
- Notebook：`NotebookCreate` · `NotebookUpdate` · `NotebookSummary`
- 知识卡片：`RuleCard` · `CaseCard` · `MethodCard` · `RiskItemCard` · `GlossaryTermCard` · `ChecklistItem` · `ArticleClaimCard`
- 问答：`AskRequest` · `AskResponse` · `Citation` · `ScenarioQueryRequest` · `ChecklistRequest` · `CaseSearchRequest` · `RuleExplanation`
- 检索：`SearchHit` · `NotebookSearchResponse`
- 候选/治理：`ExtractionCandidate` · `Candidate` · `CandidateUpdate` · `KnowledgeUpdate` · `KnowledgeRef` · `DuplicateGroup` · `ConflictPair` · `MergeRequest`
- Article：`ArticleCreate` · `ArticleSummary` · `ArticleResearchBrief`
- 反馈：`FeedbackRequest` · `FeedbackResponse`

### G. 知识对象类型与状态

- 类型（统一存 `knowledge_objects`，payload 内联）：`rule / method / risk / case / checklist / glossary`
- 知识状态：`reviewed / approved / deprecated / conflict / project_specific`
- 候选状态：`candidate / needs_review / approved / rejected`
- `source.parse_status` 状态机：`queued → parsing → parsed → extracting → extracted`（失败 `failed`）；前端对非终态每 ~1.5s 轮询 `GET /sources/{id}`。

### H. 前端功能（`frontend/app/page.tsx`，单文件）

- 集合页：tab 过滤 / grid·compact·list 视图 / 排序 / debounce 搜索 / 新建·编辑·删除。
- 工作区三栏：左 Source Stack（上传 + 实时状态 + detail/删除 + Review Queue）、中 Knowhow 工具 tab（问答 / 场景查询 / 案例检索 / Checklist / 知识库）、右 Studio（思维导图 / 新建文章 / 信息图）。
- 知识库：规则/方法/风险/术语子切换、状态徽标、状态下拉 + owner 编辑（PATCH）、查重/冲突面板 + 合并。
- source detail：KaTeX 渲染公式、HTML 渲染表格、element_type 徽标。
- 回答区：结构化卡片 + citation + 👍/👎 反馈。

### I. 配置开关（`.env` / `core/config.py`）

- LLM：`OPENAI_COMPAT_BASE_URL / API_KEY / MODEL / EMBEDDING_MODEL / TIMEOUT_SECONDS`（→ `llm_configured` / `embedding_configured`）。
- MinerU：`MINERU_MODE(off|http|cli)` · `MINERU_API_URL` · `MINERU_BACKEND` · `MINERU_MODEL_SOURCE` · `MINERU_TIMEOUT_SECONDS` · `MINERU_FORMULA_ENABLE` · `MINERU_TABLE_ENABLE`。
- 存储/CORS：`DATABASE_URL` · `SILICON_NOTEBOOK_STORAGE_DIR` · `SILICON_NOTEBOOK_CORS_ORIGINS`。

### J. 验证（`scripts/`）

- `check.sh`：`py_compile` + 离线 hermetic `smoke_backend.py`（钉死 `mineru_mode=off`、清空 LLM/embedding）+ 前端 `tsc --noEmit`。
- smoke 覆盖：上传/解析、MinerU 映射、检索打分（关键词/向量/None 三态 + 场景 boost）、抽取（启发式多候选/无 glossary 噪声/去重/绑定）、状态机（deprecated 不召回）、合并/冲突、article、反馈、JSON fence 清理、异步 scheduler、重启持久化。

---

## 关键阈值速查表

| 常量 | 值 | 位置 | 作用 |
|---|---|---|---|
| `W_KEYWORD` / `W_SEMANTIC` | 0.4 / 0.6 | retrieval | 融合权重 |
| `RELEVANCE_FLOOR` | 0.12 | retrieval | 噪声地板 |
| `SCENARIO_BOOST_ALPHA` | 0.5 | retrieval | 场景软加权强度 |
| 类型权重 | rule1.0→glossary0.4 | retrieval | 跨类型排序 |
| `WINDOW_MAX_CHARS` / `OVERLAP` | 9000 / 500 | extraction | 分窗覆盖 |
| `MAX_WINDOWS` | 0（不限） | extraction | 成本上限 |
| `BIND_OVERLAP_THRESHOLD` | 0.6 | extraction | 模糊证据绑定 |
| 去重相似度阈值 | ≥0.6 | repository | find_duplicates |
| 冲突 scope / rec | ≥0.5 / <0.2 | repository | find_conflicts |
| ask 截断 | rule5/case4/checklist6/method4/risk4/element8 | repository | 上下文裁剪 |

---

## 关键设计取舍

- **GPU 解耦**：后端绝不 import torch/MinerU；重活在子进程/远端，本机/CI 始终轻量离线。
- **离线可跑**：每个 LLM/embedding/MinerU 环节都有 deterministic 回退；测试不依赖外部服务与密钥。
- **Evidence-first**：抽取与回答都绑定 element 级证据，citation 回查校验，杜绝"无出处结论"。
- **SQLite + Python 余弦**：本机 beta 不引 pgvector；预留 PostgreSQL/pgvector 放量方向。
