# silicon-notebook 算法逻辑与功能清单

更新日期：2026-06-05 ｜ 以代码为准（`backend/app/**`、`frontend/app/page.tsx`）

> 本文件把**当前重要的算法逻辑**与**所有涉及到的功能/函数/接口/数据表**集中到一处。
> 算法常量、阈值、函数签名均逐行核对过源码。配套总览见 [architecture.md](architecture.md)。

---

## 第一部分：核心算法逻辑

主链路（任意上传内容生效的闭环）：

```
创建 notebook（直接进入，无弹窗）
 → 上传 source（PDF/MD/DOCX/PPTX/CSV/XLSX）
 → [process_source] 解析 parse → 元素向量化(daemon 线程) ‖ KG 抽取(前台)
 → extracted(绿) — 仅看 KG 抽取完成
 → 知识对象 → 混合检索 → KG-native ask（逐句 [k_i] 引用 + 多轮会话）
 → 统一 KG / 概念聚类
 → 用户反馈 👍/👎
```

> LLM / embedding / MinerU 任一未配置 → 走 **deterministic 回退**，整条链路离线可跑。

---

### 1. 文档解析（`structural_markdown.py` + `parsers.py` + `mineru_client.py`）

**唯一结构化 Markdown 解析器**：`markdown-it-py`（commonmark 预设 + 启用 table，不启用 linkify）→ `Block` 列表。

```python
@dataclass
class Block:
    type: str          # heading / paragraph / list_item / code_block / table / image
    text: str
    raw: str
    level: int = 0     # 标题层级
    lang: str = ""     # code_block 语言
    char_start: int = 0
    char_end: int = 0
    line_start: int = 1
    line_end: int = 0
    section_path: str = ""   # 面包屑，如 "引言 > 背景"
    anchor_id: Optional[str] = None
```

被两个适配器复用：
- `parsers.parse_markdown` → `SourceElement`，供存储与嵌入。
- `kg/parsing.parse_elements` → `SourceElementQ`，供 KG 窗口化。

**代码块整块保留，但不进 KG 抽取**（不在 `_PROSE_TYPES` 中）。`<a id>` 锚点产出时丢弃。

PDF 走 MinerU（`MINERU_MODE` off/http/cli）→ PyMuPDF4LLM 本地降级（pypdf 最终兜底）；MinerU 不可达时完成解析并显示质量警告，上传/URL 摄取不被阻塞。

---

### 2. KG 窗口化 `make_windows`（`kg/windowing.py`）

```python
_PROSE_TYPES = ("paragraph", "list_item", "formula", "table", "figure_caption")
# 不含 heading 和 code_block
```

按文档顺序**贪心打包** prose 块到 `kg_window_target_chars`（默认 9000）字符：
- 相邻窗口 overlap `kg_window_overlap_chars`（450 字符）。
- 吸收碎小节，避免碎片窗口。
- 超长单元素按 step = target − overlap 内切，不丢弃。
- 窗口数超 `kg_window_warn_threshold`（1200）记 WARNING，**不截断**。

---

### 3. KG 抽取 `extract_graph`（`kg_ingest.py` + `kg/extract.py`）

**并发**：`ThreadPoolExecutor(max_workers=kg_extract_workers=16)` 并发逐窗 LLM 抽取。

```python
NODE_TYPES = {"Concept", "Claim", "Formula", "Procedure"}
EDGE_TYPES = {"defines", "part_of", "composed_of", "contrasts_with", "kind_of", …}
```

抽取 profile（`kg/extraction_profiles.py`）：`academic_paper / textbook`，每个 profile 对应不同的 prompt 指令与侧重。

**失败隔离**：单窗异常记入 `failed_windows` 计数，其他窗口继续。跨窗 `canonicalize` 合并同名节点。

**Evidence 绑定**（`kg_ingest.bind_evidence`）：
1. 精确子串包含 → `status=candidate`。
2. CJK token 重叠 ≥ 0.6 → 模糊回退（容忍改写/标点差异）。
3. 绑不上 → 节点整体丢弃。

**`build_records`**：节点 → 对象（payload `{name, section_path}`），evidence 按元素绑定。

---

### 4. 嵌入（dashscope，`embedding_dashscope.py`）

两类向量，**均用 `ThreadPoolExecutor` 并发、每批独立连接落库**：

| 向量类型 | 函数 | 存储表 | 文本来源 |
|---|---|---|---|
| 元素向量 | `_embed_source` | `element_embeddings` | 元素文本前 2000 字符 |
| 知识对象向量 | `_embed_objects_batch` | `knowledge_embeddings` | payload 文本 |

- 线程前缀：`emb-el`（元素）/ `emb-kg`（知识对象）。
- 并发度 `embed_concurrency=50`，batch 大小 `embed_batch_size=10`。
- 每批独立连接（WAL + busy_timeout），批失败隔离，不中断整体。
- 落库粒度 `embed_persist_chunk=200`。

---

### 5. 摄取管线 `process_source`（`sqlite_repository.py`）

```
queued → parsing → parsed → extracting → extracted（失败 failed）
```

- 解析后：**元素向量化在后台 daemon 线程**与前台 KG 抽取并发。
- `extracted`（前端绿）**仅看 KG 抽取完成**，不等向量化。
- 末尾 `embed_thread.join()` 收尾。
- **导入后不自动覆盖笔记本名字或描述**。

---

### 6. CJK 感知分词 `_tokens`（`retrieval.py`）——所有打分基石

被**检索打分、evidence 绑定、去重/冲突**共用。

- 非字母数字字符 → 空格，转小写后切 chunk。
- 每个 chunk 走 `_segment_tokens`：
  - **CJK 连续串**（`_is_cjk`：CJK 统一/扩展 A/兼容/假名区段）：长度 1 → uni-gram；长度 ≥ 2 → 滑动 **bi-gram**（`run[k:k+2]`）。
  - **拉丁/数字串**：长度 > 1 → 整串作 token。

派生函数：
- `keyword_score(query, text)` = query token 命中占比（0..1）。
- `token_overlap(span, text)` = span token 在 text 中命中占比（evidence 绑定）。
- `cosine(a, b)` = 纯 Python 余弦（长度不等/零向量→0）。

---

### 7. 混合检索打分（`retrieval.py` + `vector_index.py` + `vector_cache.py`）

**融合公式 `_fuse`（按激活信号重归一化）：**

```
denom = W_KEYWORD + (W_SEMANTIC if has_vector else 0)
fused = (W_KEYWORD * keyword + (W_SEMANTIC * semantic if has_vector else 0)) / denom
```

- 权重：`W_KEYWORD = 0.4`、`W_SEMANTIC = 0.6`。
- **重归一化**：纯关键词对象 denom 只含 0.4，分数仍落 0..1，不被上限压制。
- `query_vector` 为 None（未配 embedding）→ 自动退化纯关键词，行为可复现。

**向量矩阵（`vector_index.py`）**：
- `build_matrix`：流式读 `element_embeddings` / `knowledge_embeddings`，每条 JSON 向量 → float32 → L2 归一化，拼成 2D ndarray。
- 内存有界（约百 MB；旧版 Python list 达 1.3 G）。
- `query_sims`：单次 matmul 出所有相似度。
- `vector_cache`：版本键 = (表名, 行数, max created_at)，命中即复用矩阵。

**类型权重 `_TYPE_WEIGHT`**（仅用于跨类型排序，**不**乘进同类相关度）：

```python
_TYPE_WEIGHT = {"claim": 1.0, "formula": 1.0, "procedure": 0.7, "concept": 0.5}
```

**噪声地板**：`RELEVANCE_FLOOR = 0.12`，低于即丢弃（knowledge 与 element 均适用）。

**Top-N + 1-hop 扩展**：`retrieval_top_n=12`；沿 `knowledge_relations` 做 1-hop 邻居扩展，填充 `related_knowledge`。

---

### 8. 问答 `ask()`（`sqlite_repository.py`）

1. 解析 question + 多轮 `conversation_id` → 拉取历史 `history`。
2. query 向量化 → `vector_cache` 取矩阵 → `query_sims` matmul。
3. `score_knowledge` 混合打分，top `retrieval_top_n`（12）hits。
4. 1-hop relation 扩展邻居 → 组装 `related_knowledge`（上限 12）。
5. **LLM 合成**（`_answer_kg`）：
   - 每命中对象分配 `k{i}` 标记，附 definition / 首现 snippet / procedure steps。
   - concept 按 unified 簇去重（避免同义词堆叠）。
   - 答案含 `[k_i]` 引用标记，`grounded=True`，`llm_mode="grounded"`。
6. **确定性兜底**（无 LLM 或 LLM 失败）：模板组装检索结果，`llm_mode="deterministic"`。
7. `AskResponse{answer, grounded, anchors, related_knowledge, citations, llm_mode, conversation_id}`。
8. 持久化 `answers`，供反馈关联。

多轮会话：`conversations` 表 + `answers.conversation_id`，通过 `PATCH/DELETE /conversations/{id}` 管理。

---

### 9. 统一 KG 与概念聚类（`kg_merge.py`）

跨文档概念聚类/合并存 `concept_clusters`，暴露接口：

- `POST .../unified-kg/rebuild` 触发重聚类。
- `GET .../unified-kg` 查看当前聚类。
- `GET .../unified-kg/pending-merges` + `confirm/reject` 审核。
- `GET .../concepts/{canonical_id}/detail` 查看规范概念详情。
- `GET .../objects/{object_id}/context` 查看对象上下文。

---

### 10. LLM 客户端（`llm.py`）

OpenAI 兼容；核心方法：
- `chat_json`：`response_format=json_object`（服务不支持则回退）+ `strip_json_fences` 去 ` ``` ` 围栏。
- `embed`：调用嵌入端点。
- 每次调用写 `.local/logs/llm.jsonl`（状态/延迟/token/错误）。

---

## 第二部分：完整功能 / 函数清单

### A. HTTP API（`backend/app/api/routes.py`，`/api` 前缀）

| 分组 | 方法 路径 | 说明 |
|---|---|---|
| 系统 | `GET /health` | 健康检查 |
| 系统 | `GET /me` | 当前用户 profile |
| 系统 | `GET /doc-types` | 支持的文档类型 |
| Notebook | `GET /notebooks` | 列表 |
| Notebook | `POST /notebooks` | 新建（直接创建，不需预填） |
| Notebook | `GET /notebooks/{id}` | 详情 |
| Notebook | `PATCH /notebooks/{id}` | 更名/更描述 |
| Notebook | `DELETE /notebooks/{id}` | 删除 |
| Notebook | `GET /notebooks/{id}/analytics` | 统计数据 |
| Source | `GET /notebooks/{id}/sources` | 列表 |
| Source | `POST /notebooks/{id}/sources` | 上传（multipart） |
| Source | `POST /notebooks/{id}/sources/import` | 从 URL/路径导入 |
| Source | `GET /sources/{id}` | 详情 |
| Source | `POST /sources/{id}/parse` | 手动触发解析 |
| Source | `GET /sources/{id}/elements` | 元素列表 |
| Source | `DELETE /sources/{id}` | 删除 |
| 知识类型 | `GET /notebooks/{id}/knowledge-types` | 已有的对象类型列表 |
| 知识对象 | `GET /notebooks/{id}/knowledge` | 对象列表（`?type=` 过滤） |
| 知识对象 | `PATCH /knowledge/{id}` | 改 status/owner/payload |
| Object Schema | `GET /object-schemas` | schema 列表 |
| Object Schema | `POST /object-schemas` | 新建 schema |
| Object Schema | `PATCH /object-schemas/{type}` | 更新 schema |
| Object Schema | `DELETE /object-schemas/{type}` | 删除 schema |
| Object Schema | `POST /notebooks/{id}/schema-proposals` | 从数据提议 schema |
| 知识图谱 | `GET /notebooks/{id}/graph` | KG 节点/边 |
| 检索 | `GET /notebooks/{id}/search` | 混合检索 |
| 问答 | `POST /notebooks/{id}/ask` | KG-native 问答 |
| 会话 | `GET /notebooks/{id}/conversations` | 会话列表 |
| 会话 | `GET /conversations/{id}` | 会话详情（含历史） |
| 会话 | `PATCH /conversations/{id}` | 重命名 |
| 会话 | `DELETE /conversations/{id}` | 删除（级联删 answers） |
| 统一 KG | `POST .../unified-kg/rebuild` | 重聚类 |
| 统一 KG | `GET .../unified-kg` | 查看聚类 |
| 统一 KG | `GET .../unified-kg/pending-merges` | 待审合并 |
| 统一 KG | `GET .../concepts/{canonical_id}/detail` | 规范概念详情 |
| 统一 KG | `GET .../objects/{object_id}/context` | 对象上下文 |
| 统一 KG | `POST .../unified-kg/merges/{id}/confirm` | 确认合并 |
| 统一 KG | `POST .../unified-kg/merges/{id}/reject` | 拒绝合并 |
| 知识治理 | `GET /notebooks/{id}/duplicates` | 重复组 |
| 知识治理 | `POST /knowledge/{id}/merge` | 合并 |
| Article | `GET /notebooks/{id}/articles` | 文章列表 |
| Article | `POST /notebooks/{id}/articles` | 新建文章 |
| Article | `DELETE /articles/{id}` | 删除文章 |
| Article | `POST /articles/{id}/research` | 生成研究简报 |
| 派生规则 | `GET /notebooks/{id}/derived-rules` | 派生规则候选列表 |
| 派生规则 | `POST /derived-rules/{id}/approve` | 批准 → RuleCard |
| 派生规则 | `POST /derived-rules/{id}/reject` | 拒绝 |
| 反馈 | `POST /answers/{answer_id}/feedback` | 👍/👎 |

### B. 仓库业务方法（`sqlite_repository.py`）

- **Notebook**：`list_notebooks` · `create_notebook` · `get_notebook` · `update_notebook` · `delete_notebook`
- **Source**：`list_sources` · `get_source` · `import_sources` · `upload_sources` · `process_source`（状态机）· `parse_source` · `source_elements` · `delete_source`
- **KG 摄取**：`extract_graph`（通过 `kg_ingest`）· `_embed_source` · `_embed_objects_batch`
- **知识对象**：`list_knowledge` · `update_knowledge` · `find_duplicates` · `merge_knowledge`
- **检索/问答**：`search_notebook` · `ask` · `_answer_kg` · `submit_feedback`
- **会话**：`list_conversations` · `get_conversation` · `rename_conversation` · `delete_conversation` · `_ensure_conversation` · `_conversation_history`
- **统一 KG**：`rebuild_unified_kg` · `get_unified_kg` · `pending_merges` · `concept_detail` · `object_context`
- **Article**：`list_articles` · `create_article` · `delete_article` · `research_article`
- **向量辅助**：`_embed_source` · `_embed_objects_batch` · `_element_vectors` · `_knowledge_vectors`

### C. 检索打分函数（`retrieval.py`）

`_is_cjk` · `_segment_tokens` · `_tokens` · `_normalize` · `cosine` · `keyword_score` · `token_overlap` · `_fuse` · `score_knowledge` · `score_elements` · `_payload_text`

数据类：`RetrievedKnowledge` · `RetrievedElement`

### D. 向量矩阵与缓存（`vector_index.py` + `vector_cache.py`）

`build_matrix`（流式读、float32、L2 归一化）· `query_sims`（单次 matmul）· `VectorCache`（版本键缓存）

### E. SQLite 数据表（19）

| 表名 | 用途 |
|---|---|
| `users` | 用户 |
| `user_profiles` | 用户 profile |
| `notebooks` | 笔记本 |
| `sources` | 文档来源 |
| `source_elements` | 解析元素 |
| `articles` | 研究文章 |
| `extraction_runs` | 抽取运行记录 |
| `element_embeddings` | 元素向量 |
| `knowledge_embeddings` | 知识对象向量 |
| `knowledge_objects` | 正式知识对象（concept/claim/formula/procedure） |
| `knowledge_relations` | 对象间关系（KG 边） |
| `answers` | 问答记录 |
| `conversations` | 会话 |
| `feedback` | 👍/👎 反馈 |
| `article_claims` | 文章 claims |
| `derived_rule_candidates` | 派生规则候选 |
| `object_schemas` | 用户自定义对象 schema |
| `concept_clusters` | 跨文档概念聚类 |
| `concept_merge_candidates` | 概念合并候选（pending-merges） |

### F. Pydantic Schema（`models/schemas.py`，关键）

- **问答**：`AskRequest` · `AskResponse` · `AnswerAnchor` · `Citation` · `ConversationSummary` · `ConversationDetail`
- **知识对象**：`KnowledgeRecord` · `KnowledgeUpdate` · `KnowledgeGraph` · `ObjectSchemaModel`
- **Source**：`SourceSummary` · `SourceDetail` · `SourceElement` · `SourceImportRequest`
- **Notebook**：`NotebookCreate` · `NotebookUpdate` · `NotebookSummary` · `NotebookAnalytics`
- **检索**：`SearchHit` · `NotebookSearchResponse`
- **候选/治理**：`ExtractionCandidate` · `Candidate` · `CandidateUpdate` · `DuplicateGroup` · `ConflictPair` · `MergeRequest`
- **统一 KG**：通过 dict 返回，尚无独立 Pydantic 模型
- **Article**：`ArticleCreate` · `ArticleSummary` · `ArticleResearchBrief`
- **派生规则**：`DerivedRuleCandidate` · `RuleCard`
- **反馈**：`FeedbackRequest` · `FeedbackResponse`
- **其他**：`Evidence` · `UserProfile`

### G. 知识对象类型与状态

- **对象类型**（4 种）：`concept / claim / formula / procedure`（统一存 `knowledge_objects`，payload 内联）
- **知识状态**：`approved / reviewed / deprecated / conflict / project_specific`
- **候选状态**：`candidate / needs_review / approved / rejected`
- **`source.parse_status` 状态机**：`queued → parsing → parsed → extracting → extracted`（失败 `failed`）；前端对非终态每 ~1.5 s 轮询 `GET /sources/{id}`。

### H. 前端功能（`frontend/app/page.tsx`，单文件）

- **集合页**：tab 过滤 / grid·compact·list 视图 / 排序 / debounce 搜索 / **「＋新建」直接创建未命名笔记本并进入（无弹窗）** · 编辑 · 删除。
- **工作区三栏**：
  - 左：Source Stack（上传 + 实时状态 + detail/删除）。
  - 中：tab — **问答 ask**（逐句 [k_i] 引用，多轮会话列表）｜ **知识库 rules**（通用类型无关）。
  - 右：Studio（文章 · 派生规则 · 知识图谱）。
- **状态点**：绿色仅给 `extracted`，其余处理中为橙色。
- **知识图谱视图**：Concept / Claim / Formula / Procedure 同屏展示，节点/边/类型形状直接画在主视图。
- **回答区**：逐句 `[k_i]` 引用 + anchors 展开（definition / snippet）+ 👍/👎 反馈。
- **会话管理**：新建 / 重命名 / 删除（级联移除历史问答）。

### I. 配置旋钮速查表（`core/config.py`）

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `openai_compat_base_url` | — | LLM 端点 URL |
| `openai_compat_api_key` | — | LLM API Key |
| `openai_compat_model` | — | 模型名 |
| `openai_compat_timeout` | 60 | 请求超时（秒） |
| `openai_compat_max_retries` | 2 | 最大重试次数 |
| `embed_provider` | `""` | 嵌入提供商（`""`=off / `"dashscope"`） |
| `embed_model` | `""` | 嵌入模型名 |
| `embed_dim` | 1024 | 向量维度 |
| `embed_truncate_chars` | 2000 | 嵌入文本截断长度 |
| `embed_batch_size` | 10 | 单批嵌入元素数 |
| `embed_persist_chunk` | 200 | 每次落库批次大小 |
| `embed_concurrency` | 50 | 并发嵌入线程数 |
| `kg_window_target_chars` | 9000 | KG 窗口目标字符数 |
| `kg_window_overlap_chars` | 450 | 相邻窗口重叠字符数 |
| `kg_extract_workers` | 16 | KG 抽取线程池大小 |
| `kg_window_warn_threshold` | 1200 | 窗口数 WARNING 阈值 |
| `db_busy_timeout_ms` | 30000 | SQLite busy_timeout（毫秒） |
| `retrieval_top_n` | 12 | 检索 top-N |
| `mineru_mode` | `off` | MinerU 模式（off / http / cli） |
| `mineru_api_url` | — | MinerU HTTP 端点 |
| `mineru_backend` | — | MinerU 后端类型 |
| `mineru_vlm_server_url` | — | VLM 推理服务器 URL |
| `mineru_timeout_seconds` | — | MinerU 超时 |
| `mineru_formula_enable` | — | 公式解析开关 |
| `mineru_table_enable` | — | 表格解析开关 |
| `database_url` | — | SQLite 路径 |
| `storage_dir` | — | 文件存储目录 |
| `cors_origins` | — | CORS 白名单 |

### J. 验证（`scripts/`）

- `check.sh`：`py_compile` + 离线 hermetic `smoke_backend.py`（钉死 `mineru_mode=off`、清空 LLM/embedding，不读真实密钥）+ 前端 `tsc --noEmit`。
- smoke 覆盖：上传/解析、结构化解析适配器、KG 窗口化（prose filter / overlap / 超长内切）、嵌入并发落库、向量矩阵 float32 构建与缓存、混合检索（关键词/向量/None 三态）、ask 多轮会话、状态机（extracted 绿标）、article、反馈、JSON fence 清理、重启持久化。

---

## 关键设计取舍

- **解析单一结构化实现**：`structural_markdown.py` 是唯一 Markdown 解析器；代码块/表格保真输出，代码块不入 KG 实体。
- **KG 抽取并发 + 高效窗口化**：成本随文档线性（非爆炸）；单窗失败隔离不中断整体；窗口数超阈值告警不截断。
- **检索 float32 矩阵 + 缓存**：低内存（旧版 Python list 1.3 G → 现在 ~百 MB）；SQLite + numpy 不引向量库（超大规模再上 sqlite-vec）。
- **抽取优先、向量化后台并发**：绿色 = KG 就绪，用户不用等向量化完成。
- **Evidence-first 逐句引用**：抽取与回答都绑定 element 级证据，[k_i] 可追溯，杜绝"无出处结论"。
- **离线可跑**：无 LLM/embedder 时降级为关键词 + 确定性答案；测试不依赖外部服务与密钥。
- **GPU 解耦**：后端绝不 import torch/MinerU；重活在子进程/远端，本机/CI 始终轻量离线。
