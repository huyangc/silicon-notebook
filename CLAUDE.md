# Claude Code 在本仓库的操作规范

本文件对 **Claude Code** 生效，每个会话自动加载。

`AGENTS.md` 是本仓库完整的开发契约，但 **Claude Code 不会自动加载它**（只加载 `CLAUDE.md` 与 `.claude/rules/`），所以本文件承担两件事：把必须随时在线的红线内联在这里，再给出 `AGENTS.md` 的章节索引供按需查阅。**两者冲突时以 `AGENTS.md` 为准**——它是真源，本文件是摘要（穷举的例外见第一节末尾）。改动开发约束时，本文件与 `AGENTS.md`、两份 README 一起改。

## Extension UI Kit 与部署问答引擎

`frontend/features/extension-sdk` 导出的 `ExtensionFormRow`、`ExtensionTextInput`、`ExtensionResultList` / `ExtensionResultItem`、`ExtensionActions`、`ExtensionAlert`、`ExtensionEmptyState` 是插件共享 UI 基座；`.extension-*` 样式只落核心 `globals.css`、只用 `:root` token、保留原生 input props，私有包仍不带 CSS。

部署 `ask.engine` provider 以自身 plugin id 命名空间提供完整的非流式 Ask mode。核心只经 `/ask-modes` 投影实时可用描述符供浏览器消费；自动模式隐藏运行时 `extension` 分组并强制内建默认，高级模式把它作为第三分组；MCP `ask_notebook` 对已注册且实时可用的插件 mode id 同样放行（未注册与已注册但暂不可用两类拒绝各给可区分的可操作文案）。provider 只拿当前问题和带范围检索、模型、轨迹、取消端口；真收窄的 run 把范围谓词下推进 SQLite/PostgreSQL 的候选 `LIMIT` 之前，未收窄的 run 保留接缝的 notebook ANN 臂、把冻结天花板留到证据 hydrate 应用（内建未收窄语义——无 scope 的调用面获得与浏览器冻结快照精确同形状的合成全选天花板），结果只给本次 run 的不透明证据句柄。检索面除元素 search/fetch 外新增一套有界 KG 读端口（search_kg/kg_neighbors/kg_overview），与元素检索独立共享一份 KG 调用预算；KG 命中只有存在存活证据绑定才可引用，引用打开的是该对象首条存活证据元素，邻居查询不带边类型标签与截断信号。核心对句柄与引用标记 fail-closed、自己创建 durable citations、提交前复核 run 身份。V1 仍不给历史、意图预检、PPR/社区/图漫游、Report、repository、全局 settings、连接或 raw model client。投影失败不影响内建模式，只在下一次成功提交 workspace 后重试。精确数值护栏只写产品/API 中英文对；模型 workload 是 `plugin_engine`，输出 cap 继承绑定客户端的普通回答上限。部署插件是受信同进程代码，Protocol 是架构能力面而不是对抗恶意 Python 的沙箱。

部署 `indexing.pipeline` contribution 是按笔记本选择的分块/索引策略，不是 parser 选择器。笔记本设置现在分成两档：owner 与组内容管理员都可编辑笔记本信息并切换索引管线，但只有 owner 可读取/修改参考库挂载（`notebook:configure` 仍恒 owner）；纯只读成员也能从“设置”看到当前索引管线与 pending/missing/unavailable 状态，但没有写入口。切换索引管线必须明确确认“将重建全库索引”；返回只暴露 sanitized 状态，不泄露插件内部 reason，旧索引继续可读，而新的索引写入在重建发布前暂时阻塞。每个可见来源的有界 chunk/KG/extraction payload 先持久化到不可见 stage，模型与 embedding I/O 均在发布事务外；SQLite/PostgreSQL 只有在一次事务里通过完整 payload 与精确 job/generation/source-snapshot CAS 后，才能替换 live 产物、失效来源派生 KG、发布 identity/成功终态并清 job authority。失败、取消、恢复或迟到代次只能删自己的 stage，不能改 live chunks、KG、extraction/parser 状态或 identity；隐藏 Memory/Knowhow 永不进入这次替换。

---

## 一、红线

`mcp_server.create_memory_mcp` 仍创建唯一 stateful FastMCP/SSE transport；唯一 API-owned tool host 从七个固定 registrar 捕获精确 22-tool core 目录，`PUBLIC_TOOLS` 就是这份活目录本身（`CORE_TOOLS` 是同一份清单的结构化别名），不存在第二份手抄。scope 只能取既有前后端真源，core policy table 强制写 scope 走 owner-only，既有两个 scope-only core 写例外不外借。注册/listing 零 repository/model I/O，每次调用仍 live 复核并恰好一次 progress wrapper。原先「按冻结 contribution 顺序追加显式信任的进程内 `agent.tool_provider` descriptor」那一半零消费者，已整体移除。

深度报告的已认证后端批量导出只有一个启动冻结的 `report.exporter` Provider。Repository SQL 先按 notebook、creator、done 与非空正文收窄并释放连接，provider 只取得不可变 opaque export view；默认 topology 只有内建 Markdown provider，不存在 host fallback 或第二套 backend formatter。Provider 输出按完整有序批次接纳，文件名净化、重复后缀、MIME/格式 policy 与 ZIP 外壳仍归 core。浏览器单篇 Markdown Blob 是已授权详情的本地呈现，不属于该 backend seat；新增用户可见格式必须同 PR 做 frontend/API parity。

不读 `AGENTS.md` 也必须遵守的部分。

### 工作区

- 凡任务会写仓库代码、测试、文档或配置，第一次写入前必须新开 **git worktree** 和分支；该任务期间主 checkout 只读，小修也不例外。当前目录若已是隔离 worktree，就在原地继续。纯调研、设计、状态汇报和只读审查除外。
- **在 worktree 里跑 `npm install` 之前先 `ls -l frontend/node_modules`**：若它是**软链**（指向主 checkout 的共享安装树），装依赖会写穿真树，绝不能跑——改依赖去主 checkout，或用 `cp -Rc` 拿隔离副本。若它是真目录或不存在，照常装即可。软链由开发者本机的 SessionStart hook 建立，不是仓库产物，所以这条**必须先看一眼再决定**，不能当成无条件红线。
- 不回滚用户的改动；不删生成物或用户提供的文件，除非用户明确要求。
- 改文件用 Edit/Write，不要用 shell 重定向整体覆写。

### 交付完整性

- **图谱质量分析视图交互**：界面必须先给出报告可信度、合并信号、主题结构与来源复核候选，直接解释红／黄／灰／当前状态，并明确收敛率和关联度是诊断信号而非越高越好的质量总分；五份数据逐项说明用途，最大概念合并组与关联形成方式不得只留在账本回执里。可编辑成员的「生成分析／更新分析」复用带确认、按笔记本单飞及 `job_id` 配对轮询的 `POST /unified-kg/rebuild`，不另造任务链、不重新分析来源，完成后自动重取报告；只读成员只看报告。
- **概念合并审阅不回流**：pending 以 canonical component 对为用户语义，每个展示对只留一条确定性的最高分代表；稳定 seed 决策经 confirmed union 传播为 component 级 cannot-link，不能让同簇另一个 seed 把已拒绝／deferred 的展示对重新生成。一次点击需按确定顺序锁定并原子收束存量同 canonical 对的全部重复行，以最后决定覆盖它们；任一兄弟行曾 confirmed 都按 confirmed 翻转处理。重建发布 pending 前须在同一刷新事务里删旧代、重读并应用实时决定。全为待审状态的展示对点「分开」不改当前簇与检索产物，不得 invalidate、置 dirty 或启动全库重建；含已 confirmed 行的展示对翻成 rejected 是例外，必须 invalidate + dirty，因为旧 union 可能已物化。只有确认待审合并才立即触发重新聚类。改聚类／候选语义必须提升 `CLUSTER_ALGO_VERSION`。
- **引用标记兼容**：模型证据标记的权威键仍是 `kN`；半角 `[k1]` 与本地化 `【k1】`（含半角/中文逗号复合组）必须在 Ask 锚点解析、分节清洗、深度报告重编号/高风险审计和前端 Markdown 链接化中采用同一语法口径，最终统一显示为可点击编号。普通复合组遇到未知键仍整体失败关闭；按节 Ask 继续沿用既有隔离规则，在绑定前删除不属于本节服务端号段的键，同时保留本节合法键。
- **Ask 引用图片正文内联**：持久化图片必须放在绑定引用所在的最小完整 Markdown 块之后（列表/引用块内部、表格整块之后），复合引用合并，同一资产只在第一次引用处显示；Ask 正文与公开问答页只显示图片和「引用 [n]／模型未直接读取图片」标签，引用浮层保留紧凑附图区，caption/description 仅用于检索和 `alt`，不得重复上屏。引用本身就是附图时隐藏其解析器生成的 snippet/quoted-span，文字证据只是附近带图时保留真实摘录。点击走 root-modal 的无卡片页内图片舞台：页面只经遮罩压暗，图片本身不放在面板背景中；用 `react-zoom-pan-pinch` 支持滚轮/按钮/双击缩放、拖动与触控手势，并保留关闭按钮、点遮罩、Escape 与焦点返回，不跳来源；预览期间引用浮层必须保持挂载。**图片居中**靠缩放内容盒以 `!important` 铺满舞台——库把自己那份单类 CSS module（`display:flex` + `width/height: fit-content`）在运行时注入 `<head>` 末尾，同特指度下它赢，配上库固定的 `transform-origin: 0% 0%` 就把每张图钉死在左上角（jsdom 量不到，守卫 `frontend/tests/guards/image-preview-stage-guard.test.mjs` 钉 CSS 文本）。打开时**冻结一份画册**：本次渲染真正落进正文的那些图片，顺序由 `rehypeCitationImages` 落位时**自己记账**（`CitationImageOrder`），绝不按 reference/payload 数组另行推导——回退 `citations` 路径下数组顺序与正文顺序可以相反，还会把从未在正文出现过的引用的图片算进来（codex #599 R1 P2；`answer-citation-images.component.test.tsx` 按 DOM 顺序对账）；正文区块与引用浮层附图共用，`←`/`→` 与两侧按钮因此走遍整条回答而不是点进去的那一组；下标夹取不循环、端点按钮禁用并变灰、显示 `n / N`、每次切换在 layout 相位（新图绘制前）复位缩放与位移，单张图不出任何切换控件。方向键挂 `window`（舞台没有焦点陷阱，焦点可能在 `body`），被更高层弹窗盖住或键落在可编辑元素上时不接。公开会话分享页复用同一套舞台、画册按轮组织。公开投影只可增加既有公开 citation key 与无内部 id 的展示布尔值，严禁暴露 asset/element id；旧载荷缺位置键时明确降级成未定位的纯图片区，不丢图、不猜绑定。单独 Markdown 引用相对/本地图片时，上传弹窗必须提示改传 ZIP/完整文件夹；远程图片提示不会代下，二者都不阻断文字上传；批内串行扫描，迟到提示只在相同 name+size 文件仍暂存时接纳。Deep Report 正文内联属于第二期。
- **全栈对等**：面向用户的后端能力必须在同一次改动里带上前端 UI。不接受只做一侧。
- **群组 owner、邀请链接与独立页面**：群组创建者是唯一初始 owner；只有 owner（系统管理员只作恢复旁路）可转让或删除群组，转让目标必须是现有成员且会升为管理员，原 owner 保留管理员身份。owner 不得被降级、移出或直接退出，必须先转让。群组管理使用 `frontend/app/groups-page.tsx` 独立页面，按笔记本、成员、申请、设置分区；成员只能查看其当前组角色实际授予读权的 notebook，只有 `group_admins` 边的库不得向普通成员披露，管理员在此统一增删可见范围，并可生成、重新打开、换新或撤销一条可重复使用的 bearer 邀请链接。登录用户打开后原子加入为普通成员，重复访问不改变既有角色；无效、已撤销、已换新或群组已删除统一 404。浏览器只让 token 跨过登录门，兑换前先从 URL 历史移除；链接不自动过期，不得向用户暗示会自行失效。视觉必须复用系统现有色板、边框、圆角、排版与响应式节奏。
- **解析能力注册表与生成问题索引**：上传校验、内置 parser 分派、云端格式准入、登录后系统配置与导入界面的支持格式提示必须从同一后端解析注册表派生；前端只收顺序、能力、执行边界、可用状态与固定原因枚举，绝不收 endpoint/凭证/路径/原始异常，且**刻意不向用户展示解析路由流程**（引擎顺序/可用状态面板已移除，用户只需知道支持哪些格式）。路由自动选择，已配置自托管 MinerU 绝不能静默切公共云。生产 ingestion 只走启动冻结的 Parser ProviderChain；所有 link 的 core route 在 provider I/O 前一次冻结，旧 dispatcher、facade patch seam 与可选双路均已退役。Probe 最多做一次远端解析和纯映射且拿不到持久化端口；workbook 对账与 core admission 通过后才能 materialize/替换图片资产，URL 的 self-hosted/builtin fallback 共用一次下载，同一来源的资产替换到 element/chunk marker 发布由同一把锁串行。生成问题召回是部署级可选补充，不是用户 scope 或 evidence：默认 `off` 零成本，`shadow` 只做有界评估与无正文计数且逐字返回 baseline，只有显式 `on` 才能在 baseline 后追加原 chunk，不能驱逐/重排；离线 `question-index` 只用 `chunk_question_generation` + `chunk_embedding`，在线先下推冻结来源上限、超扫描 cap 直接跳过，最终只 hydrate/引用原 chunk。空结果持久记完成、失败可重试，重解析/删除/复制/迁移保留原 chunk 所有权；精确数值只登记在 `docs/product-and-api*.md`。
- **数值上限与截断**：生产路径不得新增会改变结果的数字字面量切片/上限（如 `hits[:20]`）。不可调的协议边界复用具名常量，部署需在质量/成本间调整的预算放入带校验的 `Settings`。用户编辑的数据不得静默截断：前端显示同一护栏，API 超限明确拒绝。embedding/模型输入截断必须共用既有配置真源，避免在线、批处理与回填路径分叉。测试 fixture 的显式数字不在本规则范围内。
- **生产/测试目录边界**：前端生产代码只放 `frontend/app` 与 `frontend/features`；测试入口只放 `frontend/tests/{unit,component,guards}`，共享 setup/adapter 放 `frontend/test-support`。不得把测试搬回生产目录。用户文案守卫的信任边界同时覆盖 `app` 与 `features`。
- **检索运行时边界**：Ask 每次回答、深度报告规划/生成分别开独立 retrieval run，只在该 run 内共享成功 query embedding 的 single-flight；报告所有节 worker 共用一个叶子 I/O 扇出闸。只有真正的 KG/chunk/element/PPR 调用占槽，编排器、持有 DB 连接的代码不得先占槽再等子任务。等待槽位时必须感知取消，拿到槽位后、发起 I/O 前再检查一次；已进入底层数据库/后端调用的 leaf 安全收束，不遗留后台 I/O。观测 fail-open 且只记阶段/索引/计数/耗时/不透明 run/report id，严禁 notebook/source id、问题、标题、查询、证据与异常文本。
- **来源证据多样性**：新导入/重解析只在元素、chunk、embedding 与 KG 发布前清理高置信度的重复文本页边界，并保留一个代表；解析器明确标记的 header/footer 即使被其它版式块夹在页内、不是首末文本元素，也按页边界处理。不得全局去重正文，也不得误删表格、公式、图片、代码或同页重复。历史数据与外部 parser 另由检索侧独立兜底：同一来源的规范化同文副本必须在 chunk/元素候选与结果上限之前、reasoning 累积、反思摘要和 Ask/报告最终组装时折叠；不同来源即使同文也保留独立出处。反思与最终合成必须看到同一份有界多样元素视图，大纲合法绑定只优先占闭合上限、不能绕过上限。规范化与择优规则必须共享，精确结构阈值只写入成对的产品/API 参考。
- **深度报告时间**：已完成报告的列表与详情以 `updated_at` 同时显示浏览器本地时区的精确生成时间和相对时间，并显示从原子 `outline_ready → generating` 认领时持久化的 `generation_started_at` 到该终态写入的总耗时；意图/大纲确认等待不计入，旧报告缺开始戳时不编造耗时。未完成报告只显示创建时间，不冒充已有最终耗时。不能退回只显示“几小时前”。
- **界面模式**：`ui_mode`（`auto` 默认 / `advanced`）存 `user_profiles`、经 `GET /me` 下发、`PATCH /me/ui-mode` 自助修改（非法取值 422，旧后端缺字段前端按 `auto` 处理）；自动模式隐藏的每个控件都必须在请求侧强制其默认值（引擎子切换固定逐步推理、检索档位固定「标准」、报告研究深度固定「标准」，尤其检索范围强制全选，不得沿用高级模式残留的收窄勾选）；报告 `auto_generate` 只在问题清晰（无阻断性歧义）时复用人工确认端点的同一份确定性冻结自动确认意图并直接推进，歧义必停，任一步自动推进失败/竞态一律 fail-open 回人工确认门；两模式的隐藏/降级都不得改变高级模式的既有行为。
- **深度报告可信度与全篇综合**：报告复用共享意图并持久化 `result_scope` / `completeness_required`；完整枚举未接入时不得把相关性检索伪装成“全部”。假设不是证据也不进入检索串。「资料基础」必须由 SQL 聚合和有界代表页生成，完整 source-family 映射不得进意图、大纲、轮询或 prompt；只按实际触达来源有界解析身份。身份未知来源在参考文献中逐份可见且不增加独立来源数，Top-1 集中度按全部未知支持归入最大已确认族的保守上界计算。**画像缺席必须说清是哪一种**：收窄来源范围是有意跳过（`scope_restricted`），聚合出错才是失败（`failed`）——两者都持久化 reason 而不是空画像、都 fail-open，只有后者发 `report_corpus_profile_failed`；不可用标记是**非空** dict，绝不能靠真值判断漏进任何会把它当统计格式化的消费方（那会把每个计数渲染成 0 并把伪造语料摘要送进规划）。历史报告存的是空画像、事后无法归类，保持原失败文案。**画像只统计当前笔记本**，而检索跨已挂载参考库，所以披露必须另行点明正文实际引用了多少份参考库资料——由已装配引用推导（零新增查询）、按来源去重不按锚点计数；缺这一句，「基于 N 份资料生成」会被读成证据全部（真机实测：画像 4 份，而 42 个锚点里 26 个来自挂载库）。**报告生成的分段耗时**（检索/综合/撰写）经 `report_stage_timing` 事件记账，只带 index 与毫秒、不带章节标题或正文；模型延迟已可从 LLM 日志回推，检索不能，缺它就无法解释墙钟与模型时间之间的差额。`_generation_started_at` 是 store 的私有键，终态写 understanding 必须保留它而不是整体覆盖（覆盖会让已完成报告永远显示不出耗时）。**所有深度**都必须先完成全部章节检索，再至多调用一次全篇综合蓝图，最后并行分节写作；唯一的闸是章节数（跨章节一致性对一节不成立），只有单节报告保留逐节检索→撰写流水线，深度只决定检索预算。低档因此失去逐节流式——这是已登记接受的取舍。分节撰写、全篇蓝图与最终终审必须分别显式使用部署可配置的输出预算，不能静默继承全局 chat cap；总上下文兼容性由 provider/model 绑定负责。整篇准入与单篇节级扇出是独立于物理模型容量的数据库保护闸，排队时不得持有数据库连接。综合失败/跳过与账本可用数须在有信息量时上屏：只静默单节报告、以及综合还闸在 depth ≥ 8 时生成的历史报告的 `not_requested` + `0/N` 纯否定回执，多节 no-op、任何跳过/失败与可用账本均不得隐藏；模型/校验失败 fail-open 且可观测。综合蓝图的 facet 标签同样是组织性标注：先确定性修复（复合前缀收窄 + facet id/名称/取值的大小写不敏感唯一反查，声明 id 优先、歧义拼写绝不猜测归属），修不了只清空该条主张的标签并以仅含计数与不透明 id 的 `report_synthesis_facet_tags` 事件计数，绝不因 facet 标签作废整份蓝图；证据绑定/章节归属/结构仍原子校验。零节产出有效正文时必须落 `failed`，不得假 `done`；失败行保留已确认大纲时，重试只重跑生成并原子清空旧产物，不重新解释意图或规划。高风险引证扫描始终披露、只代表锚点覆盖；自动降级由独立默认关闭开关控制。趋势置信度按身份可确认的可区分引用来源封顶，来源身份缺失时只能标为研究性；终审只接收语法合法且总量有界的跨节上下文，只审计、不改写正文。数值上限只登记在 `docs/product-and-api*.md`。
- **报告公开分享**：单份**已完成**报告可发布成免登录只读页。发放幂等（重发同一 token，已给出去的链接不会失效），撤销后与从未存在的 token 同为 404，未完成报告拒绝分享。匿名端点必须挂**独立 router**——主 API router 带 router 级 `Depends(get_current_user)`（"零逐路由遗漏"），挂上去会 401 拦掉它要服务的访客；它也因此不绑定请求用户，只能调不依赖 current-user 的仓储方法（ContextVar 未设时 `current_user` 回退 seeded admin，等于以管理员身份跑）。投影是**白名单**不是脱敏：正文/问题/时间 + 每条引用的标题/原始文件名/位置/摘录；`source_id`/`element_id`/`object_id`/`notebook_id` 与整个 `understanding`（意图、冻结的来源范围）一律不跨出去——公开页本就打不开原文，给 id 只是让人拿去探测已认证接口。`auth_optional` 的测试夹具**看不见**这条 401（缺 token 会静默回退 seeded admin），所以接线本身要有守卫。隔离的是**产品层**不是**渲染管线**：公开页必须复用 `remarkCitations`（`[k]`/【k】按同一语法口径链接化成可点编号，编号取自 key 里的序号——公开投影会丢掉既无标题又无摘录的条目，按位置数就会与正文对不上）、`.answer-table-wrap` / `.answer-code`（宽内容在自己的内容块内横向滚动，否则整页被顶宽），并**自己** import `katex/dist/katex.min.css`——它是唯一不经 `app/page.tsx` 的界面，漏了不会报错，只会让 rehype-katex 的 MathML 不被裁掉、每条公式连着逐字符文本渲染两遍。守卫是 `frontend/tests/guards/katex-stylesheet-guard.test.mjs`（钉「接 rehype-katex 就得带样式表」）与 `frontend/tests/component/public-report-page.component.test.tsx`。
- **问答会话公开分享**：一条问答**会话**（多轮 Q&A）可发布成免登录只读页 `/c/{token}`，是报告公开分享的平移，报告那条的坑逐条继承（独立匿名 router、只收 token 的仓储方法、白名单投影、创建者读权实时复核、公开页自带 `remarkCitations`/`remarkGfmPlugin`/`katex.min.css`/`.answer-table-wrap`），只登记会话多出来的部分。**快照冻结 + 显式更新**（用户裁决）：`POST /notebooks/{nb}/conversations/{cid}/share` 发放 token（幂等复用）**并把水位 `shared_through_at`/`shared_through_id` 推到客户端披露到的那条答案**——请求体带 `expected_through_id`（弹窗据以算披露的那批 turns 的最新答案 id），服务端把水位钉死在**正好那条**（之后又落的更新答案不纳入，关闭「披露到 X、发布到更新的 Y」的 consent TOCTOU）；披露的边界答案已删则 409 让用户刷新重看，省略/空则回退当前最新（旧行为）。「分享」与「更新到最新」是同一个调用；公开页只显示水位之前已写入的答案（生成中的轮次有 `ask_jobs` 无 `answers` 行、构造性排除），边界是对水位答案 `(created_at, rowid/ordinal)` 的 **keyset**（精确含 tie-break，同刻并列时排在水位之后那条不多纳入；`shared_through_id` 因此是承重的 keyset 锚而非仅展示），仅当 `shared_through_id` 解析不到（答案被删）时才回退纯 `julianday(created_at) <= julianday(shared_through_at)` 时刻谓词。发放**行级 `created_by` 门**（`_own_conversation_or_404`，镜像报告 `_own_report_or_404`；「存在但不是你的」与「不存在」同 404）；**空 `created_by` 会话拒绝分享**（409 fail closed，`conversations.created_by` 是 `DEFAULT ''` 历史行可空，无创建者则实时复核无从做起——空检查**必须排在相等判定之前**，否则 `''` 永不等真 id、fail-closed 分支成死代码）；**零答案会话拒绝分享**并回滚刚发的 token（用 `share_conversation` 返回的水位当信号，避开单独 has-answers 预检与 share 之间的并发窗口；回滚走**有条件** `discard_unwatermarked_share`——`AND shared_through_at IS NULL`，并发 share 一旦推进水位就 no-op，不误撤已交付的有效链接；DELETE 端点的撤销仍无条件）。投影是**白名单**（`conversation_public_view.py`，每轮 question/answer_md/timing/evidence_level + 每条引用 title/file_name/location/snippet）：`reasoning_trace`/`intent`/`retrieval_scope`/`retrieval_query`/`mode`/`llm_mode`/`retrieval_effort` 与一切 addressable id（`source_id`/`element_id`/`object_id`/`notebook_id`/`memory_id`/`provenance`/`knowhow`）一律不跨出去（构造性丢弃，不是读了再删）。**Memory 引用一并公开但剥 memory_id**（用户裁决）：这是自我发布——Memory 按创建者私有，一个人的答案只可能引到自己的；分享弹窗**必须明说**包含几条个人记忆摘录（consent 红线，`summarizeShareDisclosure` 承载，`memoryCount>0` 必显、加载失败退化成不带数字的告警而非不显示，纯逻辑抽在 `conversation-share-disclosure.ts` 由单测钉住）。匿名路由还**独立** fail-closed 空创建者（复核前 `if not creator: 404`，不把这道拒绝完全托付给 T2 发放门——配 everyone 授权边时 `user_can_read_notebook(nb,"")` 恒真）。引用选择是前端 `buildAnswerReferences` 的跨语言端口（`_select_references`：anchors 权威 else citations，重复 key 末次优先对齐前端），畸形 payload 逐条降级（`_as_list` 守 truthy 标量，per-turn 兜底不让一条坏 turn 掀翻整页）。**附图**（用户裁决 v1 带图）：投影给**按 token 派生的不透明别名**（`conversation_asset_alias` = HMAC-SHA256(key=token, msg=asset_id) 前 32 hex，投影与端点唯一共用实现），不给 asset_id——三个好处对应三条红线（无 id 跨界、撤销即全灭、同图跨链接不可关联）；匿名图片端点 `GET /public/conversations/{token}/assets/{alias}` 只服务**快照内被引用到**的资产（端点枚举 asset_id 集合必须 ≥ 投影能发的别名上限，由守卫钉 `MAX_REFERENCED_ASSETS >= MAX_TURNS × CITATION_IMAGES_PER_ANSWER`），复用既有 mime 白名单与 `path_for`（未知 mime 在 path 解析前挡、SVG 已排除），`Cache-Control: no-store`（撤销后浏览器缓存会架空「失效即 404」，匿名场景更硬），部署 `MINERU_RETURN_IMAGES=false` 时投影不发别名、端点 404 短路。跨库资产边界：**冻结快照即授权**（生成答案时创建者参与集里就有它），不额外复核资产自身 notebook 参与权。**清单卡（`result_sets`）不进 v1**（用户裁决）但绝不静默丢弃：`PublicTurn.omitted_result_sets` 带计数供公开页披露。**问题与标题的「原样返回」各依赖一条写入侧长度闸**（平移报告那条 codex #525 R1 P2）：投影把每轮 question **与会话标题**逐字发给匿名访客，所以它们只有在写入那一刻就挡住超长文本时才是有界的。①**问题**——`AskRequest.question` 带 `max_length=ASK_QUESTION_MAX_CHARS`（`backend/app/models/ask.py`，与报告侧同值同理由），`/ask`、`/ask/stream`、`/ask/intent` 三个入口超限一律 422、绝不裁短了存；**MCP `ask_notebook` 同样执行**，且在工具体内自己检查、给可操作文案（交给 pydantic 会让 Agent 收到一坨 model dump，而且空库时可用性闸先触发、根本走不到校验）——长期 token 此前可提交任意长度问题，这是**刻意**的行为变化，已在 docs 登记。②**标题**——`ConversationRenameRequest.title` 带 `max_length=CONVERSATION_TITLE_MAX_CHARS`，`PATCH /conversations/{id}` 超限 422。**重命名是标题唯一能超过服务端自动取的前 60 字的途径**（`ensure_conversation` 写的是 `question[:60]`），所以这**一个**端点就是它的全部写入侧；MCP 面没有重命名工具，无需对等。取 200 而**不是** 4,000：那把尺是给问题正文定的，标题是一行标签，复用它等于宣称我们打算服务 4,000 字的会话名。两条闸的前端都同值拦住提交（`ASK_INPUT_LIMITS` 的 `questionMaxChars`/`conversationTitleMaxChars`，按 **Unicode 码点**数与 Pydantic 对齐，绝不替用户裁剪；**两条**提交路都要挡——提问是发送键与 Enter（Enter 的 handler 在 `AskComposer` 内，只 gate 按钮等于没 gate），重命名是保存键与 Enter，且输入框**刻意不挂** `maxLength`：那把尺数 code unit 且静默裁剪，一次犯两条红线）。仍有一处明知未闭合、如实登记的缺口：两条闸**之前**写入的行（更早的轮次可带更长的问题，更早改过的标题可更长），要在投影里限住它需给该轮加披露字段并改公开页（报告侧 `PublicReport.question_truncated` 付的正是这个代价），属独立一件事，不用静默截断糊过去。**两个入口、同一个调用**：会话列表卡片上的分享按钮发布**整条会话**（`expected_through_id` = 当前最后一条答案，即「更新到最新」的既有语义）；每条回答动作行里（**排在复制之后**）的分享按钮发布**到这条回答为止**，做法只是把 `expected_through_id` 换成这条答案的 id——不新增端点、不新增后端行为，`answer_id` 为空的生成中回答不出按钮。三条**只在边界模式成立**的界面契约：①`expected_through_id` 就是用户点的那条 id 本人，**绝不**在会话详情加载失败时回退成空串（空串让服务端按当前最新发布——界面写着「分享到这一条」，发出去的却是整条会话）；②后端水位是 **advance-only**（`share_conversation` 的回归检查，codex #522 R3），所以「已分享到更后面、又点更早那条回答」在服务端根本做不到，此支**一个发布按钮都不给**，改为说明当前链接已包含这条回答、还多包含几轮，并指出唯一出路是先撤销再从这条回答重新分享——给了按钮只会换回一句「这条会话已有变化」的 409，而实际上什么都没变；**该支的介绍语也必须一并改口**，不能一边在开头写「只包含这条回答以及它之前的问答」、一边在下面说链接更靠后——复制按钮就在这两句之间，用户读完前一句就把比承诺更多的轮次发出去了（codex #530 R1 P1）；**水位相对边界是五值而非二值**（`shareScopeState`：`unshared`/`at`/`behind`/`ahead`/`unknown`），介绍语按五值各写一句、绝不共用：它排在链接框与复制按钮**之上**，所以必须描述链接**当前**的内容而不是「按下去会发布什么」（只有 `unshared` 那句才是承诺，因为那时还没有链接可复制）。三条评审意见全部出在把这五种情形写成一句——`behind`（链接停在更早一轮、这条回答还没进去）会让人复制出缺了它的快照，`unknown`（水位指向本地 turns 里没有的答案：另一个标签页在详情读取之后推进了分享）曾被当成「没越界」而承诺一个更窄的范围，两者都必须各自出声（codex #530 R1 P1 / R2 P1 / R2 P2）；**范围声明在复制前必须复核**（codex #530 R3 P1）:token 是稳定的,所以另一个标签页在弹窗打开**之后**推进同一条分享时,链接当场就指向更多轮次而文案还停在初次加载那一刻。`doCopy` 先重读一次分享状态,与显示不一致(含已在别处撤销)就刷新界面并**拦下这一次复制**,窗口重获焦点时也对一次账(事件驱动,不轮询)。它**只**关得住按钮那条路——链接框是可选中的readonly input,手动复制拦不住,所以这是尽力而为的收敛而非保证;读不到状态按「不一致」处理。刻意**不**碰 `shareStateError`:发布本身是安全的(`expected_through_id` 显式送出,服务端advance-only 钉住或 409),危险的只有「照着一句过时的范围声明把链接发出去」,一次网络抖动不该顺手废掉发布；复核结果是**三值**（`same`/`changed`/`unavailable`）而非布尔:后两者都拦下复制,但「读不到」时界面**没有**任何改动,说成「已在别处变化、说明已刷新」就是编造一次并不存在的变化(codex #530 R4 P2)；③该支的披露必须按**完整** turns 统计而不是截断批次，否则会**少报**链接真实公开的附图/记忆条数（披露描述的是「链接里有什么」，不是「用户点了哪一条」）。边界解析不出（详情没加载出来、或该答案已不在会话里）退化成不带数字、但附图与个人记忆两面都提的兜底文案，且仍可发布。纯逻辑单点在 `conversation-share-disclosure.ts::resolveShareBoundary`；「更新」按钮的兜底文案另起一条常量，措辞里的按钮名必须与旁边按钮上写的公开范围一致；成功回执同理（边界模式说「已更新到这一条」，绝不说「已更新到最新」——a1..a3 里把 a1 的分享推到 a2，a3 仍未公开）。精确数值上限只登记在 `docs/product-and-api*.md`；守卫是 `frontend/tests/guards/katex-stylesheet-guard.test.mjs`（覆盖新 `/c/[token]` 页）、`frontend/tests/component/public-conversation-page.component.test.tsx`、`frontend/tests/unit/conversation-share-disclosure.test.mjs`、`frontend/tests/component/conversation-share-modal.component.test.tsx`（边界模式四支：expected 钉在点的那条、详情失败仍不回退成空、水位越过边界不给按钮、非边界模式逐字不变）、`frontend/tests/component/answer-share-button.component.test.tsx`（有无回调、排在复制之后、生成中不渲染）、`frontend/tests/unit/ask-input-limits.test.mjs`、`frontend/tests/component/ask-composer.component.test.tsx`、`frontend/tests/guards/conversation-title-limit-guard.test.mjs`（重命名 UI 在 `page.tsx` 里、没有组件接缝，按源码语义钉「无 `maxLength`／两条提交路同判据／不裁短」）与后端 `test_conversation_public_view.py`（含把两半钉在一起的 `test_public_question_is_bounded_by_the_write_side_rail` 与 `test_public_title_is_bounded_by_the_write_side_rail`）/`test_public_conversation_api.py`/`test_public_conversation_asset_api.py`/`test_ask_modes_api.py`/`test_conversations.py`/`test_memory_mcp.py`。
- **来源选择检索范围**：检索范围有**两个互相独立的维度**，都由问答意图预检/执行和新建深度报告共用、都默认全选——当前 notebook 每个可见导入来源一个复选框（`source_scope`），每个已挂载参考库**整库**一个复选框（`base_scope`，不展开到库内来源）。`include` / `exclude` 只能指向当前 notebook 的可见来源；API 入口冻结选择，并用当前可见来源总数在服务端计算、覆盖 `narrowed`，所以冻结后的全选 include（包括单篇 notebook）仍是整库图路径，不得清空历史或关闭图通道；全选运行会在服务端快照当时已有的隐藏 Memory/Knowhow 参与者（不暴露、不写入公开 scope），真正收窄才排除它们。全选仍是可分区候选与结果的冻结上限，并发新增来源不得混入；若执行时可见来源或隐藏参与者全集已漂移，无法安全预过滤的整图/PPR/关系/精确章节/整库画像通道必须在 I/O 前关闭。真正收窄时，来源谓词必须在候选进入 Top-K 前下推并在 hydrate/合成后复核：chunk ANN 通过持久化的逐行来源 sidecar 在 HNSW 内过滤，旧索引缺 sidecar 时只降级到有界来源内 FTS，重建/fold 后恢复；KG、PPR 与精确查找种子都结束后若证据仍完全为空，先确定性补一次有界原文检索再交给 reflect。事后过滤不能单独授权图扩展，因为未选候选会占 Top-K 或提供隐藏推理前提。用户勾选是检索范围的唯一来源，模型不得收窄或扩大；base KG 种子仍可不经组合图而直接映射 base 原文。**409 判据是两维同时为空**（本地有效范围为空**且**参考库有效范围为空），即判据是「这次勾了哪些库」而非「挂了哪些库」，Ask 三个入口与报告的创建/确认/生成三处都生效；请求没有**收窄**的那一维按 notebook 真实**证据宇宙**作答，两维都没收窄的请求交给既有可用性闸。**本地证据宇宙 ≠ 可见导入来源数**：Knowhow 格子、该用户的已确认 Memory 与本地图谱都没有可见来源行，所以 `NotebookSummary` 在 `ask_available` 之外并列暴露 `local_evidence_available`（由 catalog 复用可用性判定本来就要求值的三条本地判据算出、零新增查询，默认 False 并与来源数取**或**故只增不减）；真收窄仍以选择为准。**进入上限的隐藏参与者按种类分别定范围**：Knowhow 投影是 notebook 级共享的（表格本身对每位成员可见），Memory 投影**按创建者私有**、只进创建者自己的上限——归属谓词（`memory_items.created_by`，与 Memory 检索同一条判据）必须写在取数那条 SQL 里而不是结果侧，别人的 Memory 源 id 因而根本不进本进程；否则共享笔记本里任何一位成员的默认全选请求都能经普通候选生成、以及未收窄时重新打开的全图/PPR 通道读到别人的私有 Memory。隐藏那一半刻意**单独一条查询**（`hidden_source_ids(notebook_id, owner_id)`）而不与可见那半合并：紧凑 `include` 冻结只读所请求的 id 加一个计数，合并会把整库来源行重新搬回热路径，去回答一个本来只受投影数约束的问题；它也只在未收窄时才发。报告的解析范围在意图确认与生成前重验并贯穿规划、生成；任一维省略范围字段都保持历史整库行为，且绝不替用户伪造出一份选择。
- **参考库维度与 `restricted` 正交**：仅取消参考库勾选、本地来源全选时**不得**进入限定模式——那会关掉**当前库**的全图/PPR/关系扩展/精确章节/整库画像并让私有 Memory 完全不参与，用户不该为「少借一个参考库」付出「当前库检索能力被砍」的代价。当前库通道（PPR、私有 Memory、社区报告、弱支撑关系、精确章节查找、报告整库画像）只看 `source_scope_restricted()`；跨库通道（集合枚举与集合地图、federated 检索、社区扩展、图漫游、`follow_chain`、证据装配、**KG 可用性闸**）必须各自认库维度——最后这一个是判据而非结果过滤：本库无图、唯一带图的库被取消勾选时必须判「无图」，否则 `kg_required` 不翻真、graph 路径会跑在一份这次不许读的图上；它按**冻结的选择**（提交过库维度即认，不看是否收窄）经 `scoped_participants` 解析参与库，与候选检索共用同一份名单。库级收窄**只在参与库解析这一个边界**统一生效（一个列表、过滤一次、被多处读），因为**计数与行必须同一谓词**：只滤行不减分母会让走完的完整清单被永久判成 `concurrent_change`；`enumeration_active()` 不因库维度关闭，工具照常提供、只是作用域收窄。**泄漏面包括查询词本身**：社区扩展拿参考库实体名当查询词，这些词会进可见轨迹、进已用查询记录并回喂反思，所以收窄必须发生在**取实体名的入口**，光过滤结果无效。**权限参与集不受勾选影响**：`resolve_participants` / `mount_sql.py` 是检索与权限共用的唯一定义点，库级收窄只做在检索消费侧，否则用户会打不开历史答案里参考库来源的详情。**已登记接受的二阶代价**：图漫游与 PPR 的过滤在遍历/截断**之后**（前置会污染进程级图缓存或让每种勾选组合重建一次千万节点图），被排除库的节点仍占扩散名额/候选席位、仍可当中转跳，整图规模守卫同样保持 scope 盲——刻意接受，不是缺陷。回执 `AskResponse.retrieval_scope` 是只读展示字段：库名是授权时刻的持久化快照（不按当前挂载重新映射），检索侧从不回读，两维都没收窄时不产生；浏览器每次请求都会发两份范围，所以「不产生」的判据是**确有收窄**而不是「提交过」。**前端的严格推理门控同样按勾选集判**：`NotebookSummary.base_kg_notebook_ids` 把 `base_kg_available` 分解成「挂载的库里哪几个已建图」（零新增查询——`mounted_bases_row` 每行本来就带 `has_kg`，聚合布尔只是把它 `any(...)` 掉了；回填路径与那个布尔逐字相同，两者必须自洽：非空 ⟺ 为真，它们是同一次读取的两种投影）。「深入分析/知识图谱」的可用性判成「本库有图 **或** 本次勾选的库里有带图的」，「将借用参考库「…」推理」只点名**既被勾选、又已建图**的库——读聚合布尔会放行一个这轮取不到图的模式，并点名一个本轮不参与的库。该字段只能挂在 `NotebookSummary` 上，**不得**加到 `NotebookRef`（它同时是 `/notebooks/{id}/mountable` 的响应模型与 `MountedBase` 的基类，那两处并不计算这个标志）。数值上限只登记在 `docs/product-and-api*.md`。
- **来源删除/重解析不做请求内整库回填**：已回填 notebook 通过 `knowledge_object_sources` 反查受影响 KG 对象；未回填 notebook 的交互式删除/重解析用 keyset 分页的数据库原生 evidence 筛选当前 source、保持 marker 不变，绝不能在请求内逐对象反序列化并回填整本库。旧库筛选仍可能扫描该 notebook 的 KG 行，大库应显式离线运行 `backfill-source-index` 预建/修复。离线重建必须用 `source_index_backfills` 持久化 notebook 级代次、游标和计数：索引行与游标逐页同事务提交，重启不得清空已提交页面，完成标记直接跳过；`kg_mutation_seq` 漂移只落稳定失败码，并在下次运行按新代次从头重建。投影清理前先锁 source 聚合行，防并发抽取在游标通过后复活子行。大量对象的删除 SQL 每条最多 500 个 id（只约束语句/参数，不承诺常数时延或后台删除），引用图片用一次数据库往返取回并删除；前端必须立即显示该来源“删除中”并禁用重复删除，且以 notebook 级删除墓碑拦住导航/列表旧响应复活已删行。
- **element→chunk 反查索引**：`chunk_elements`（SQLite v46 / PostgreSQL v24）让每次问答的证据反查从「按索引代次全量扫 chunk + 逐行 `json.loads`」变成主键点查。分叉判据是持久化的 `unified_kg_state.chunk_elements_indexed` 标记（一次单行读）：已回填走点查，未回填**逐字**保持旧的版本缓存全量路径。活库够得着的每条 chunk 写路径——`replace_source_chunks`、knowhow 投影的 `insert_rows`/`delete_by_ids`，以及把 chunk 行直接搬进**已存在** notebook 的 knowhow 单表 transfer——都必须在**同一写事务**内维护反查行，删除/重解析/改格子经 `chunks` 的外键级联带走旧行（`PRAGMA foreign_keys = ON` 是前提，有回归护栏）。**已登记的唯一豁免**：整本深拷贝不复制 `unified_kg_state`，副本 marker 恒缺失、走旧全量路径，因此不必带反查行；反过来说，若将来把 `unified_kg_state` 加进深拷贝表集合，就**必须**同时拷贝 `chunk_elements`，否则副本会继承一个为真的标记而反查表是空的，点查静默返回空证据。历史行只由**显式离线**的 `batch_ingest backfill-chunk-elements` 投影，绝不从交互请求触发：notebook 级 `chunk_element_backfills` 账本逐页 keyset + 逐页短事务原子提交游标，重启续跑，完成标记直接跳过，`kg_mutation_seq` 漂移只落稳定失败码 `kg_generation_changed` 并在下次运行按新代次从头重建；账本不含 chunk 正文或异常文本。**终止判据只能是「游标走空」，绝不能是 `scanned >= total_chunks`**——总数在 begin() 冻结而写路径仍在插入，新 chunk 的 id 可能排在游标之前、被后续页顺带数进 scanned，按总数判就会在还有历史 chunk 没投影时翻标记（`total_chunks` 只用于进度显示）。点查按 SQLite `rowid` / PostgreSQL `ordinal`（插入序）排序——旧的「chunks 扫描序」本就不是契约，但消费方 ask_graph 的 BFS 兜底无 rerank、顺序决定截断，所以**确定性**是契约。PPR 的全库 membership（`_ent_chunk_map`）刻意保留旧路径（它本质就是全量）。
- **来源图片离线回填（`batch_ingest backfill-images`）**：历史部署把 PDF 用离线 MinerU 转成 markdown 后只上传那个 `.md`，而单文件 markdown 解析路径不传 `resolve_image`、空 alt 的 `![](images/<sha>.jpg)` 又被 `parse_markdown_text` 整块丢弃，所以这批来源既没有图片元素也没有资产。MinerU output 树里图片文件名就是内容哈希，按名找得回一部分。本阶段是**显式离线、零模型**的外科修复，红线四条：①**零 embedding 重算**——一次 embedder 都不调，也不写不删任何 `chunk_embeddings`/`element_embeddings` 行；②**零 KG 变动**——不碰 `knowledge_objects`/`extraction_runs`/`unified_kg_state`，`kg_mutation_seq` 不动，所以检索索引与社区划分都不会被判陈旧；③**chunk id 与 `chunks.text` 逐字节不变**——补回的图片只被 append 到锚点 chunk 的 `element_ids` **尾部**，绝不重分块（换 chunk id 会经 `chunk_embeddings` 的 `ON DELETE CASCADE` 把整本库的向量删掉重算）；④**只插入**，唯一的既有行改写是下面这条登记过的例外。**对原规格「只插入」的一次显式修订**：解析路径对**带图注**的相对路径图片（`![图 1 架构](images/a.jpg)`）会产出一条 image 元素并写下 `metadata.src`，但拿不到 `asset_id`（单文件 markdown 路径不传 `resolve_image`）——它不在「已补过」集合里（那条判据要求 `asset_id` 非空），按只插入处理就会给同一张图造出第二条元素行（两条都进 chunk、引用带图重复显示同一张图）。改为**就地补齐**：落资产后只 UPDATE 它的 `metadata` 补 `asset_id`，`text`/`id`/`created_at` 一律不动；不在任何 chunk 里才 append 进锚点 chunk；计数与插入分列（`enriched`）；同 src 多条取 id 序第一条。**就地补齐刻意不受对齐三道闸约束**：它按精确 `src` 相等找目标元素，目标不在任何 chunk 时沿**元素 id 序**向前回退找最近的已入 chunk 元素，全程不看 markdown 对齐——纯图片文档一条文本行都没有、覆盖率**恒为 0**，拿覆盖率闸拦它等于对这一整类来源永远补不了图，而它们正是本工具要修的主力；覆盖率下限与锚点新鲜度只作用于新插入。补齐的图注与插入的一并计入 `captions`（图注取自元素自己——补齐不改写 `text`），只数插入会让纯补齐的来源报「图注 0」。这个 metadata UPDATE 是**第五条**必须落在同一写事务里的写，双后端同修并纳入 AST 守卫。锚定是 markdown 行与该来源既有元素（按 id 序，也就是分块读取的同一个文档序）的**单调**双指针对齐：指针只前进，对齐一旦漂掉就自然停止匹配；有界回退后仍找不到所属 chunk 的图片记账跳过，绝不瞎插。**停滞是静默的**，所以对齐有三道配套闸，缺一不可：①markdown 侧永远没有对应文本行的元素类型（`image`/`figure`/`table`/`code_block`）**不消耗前瞻预算**——连续 8 张表格或 8 张带 alt 的图片就能把窗口吃干，此后指针永久停滞，而 `matched` 不再增长意味着文档后半段每一张图都锚到漂移点之前的元素、被 append 进错误 chunk，`skipped` 里一个字都没有（两种形态实测把覆盖率打到 0.33）；②逐张的**锚点新鲜度**（自锚点被匹配以来未匹配文本行超界 → `anchor_stale`）；③**整源覆盖率下限**（低于它整源跳过、记 `alignment_drifted`）。④扫描到结构块**即时**推进指针（一个 markdown 或 HTML `<table>` 块、一个围栏代码块、一条带图注的独立图片各推进**一条**元素）——否则物理上紧跟表格/代码块之后的图片会锚到结构块**之前**的段落、挂进错误的 chunk，而覆盖率仍是 100%，一个信号都没有；空 alt 的独立图片刻意**不**跨越（解析路径整块丢弃它，元素侧没有它可跨）。覆盖率因此是闸而不只是报表字段。「文档开头无锚点」（`no_anchor`）与「锚点不属于任何 chunk」（`no_chunk`）是两种处置完全不同的失败，reason code 分开；原先那个 `el-<sid>-0000` 合成基座是死分支，已删。只有**独占一整行**的图片会被补回（列表项/表格单元格/段落中间的内嵌图片记 `inline_image_skipped`），与在线 markdown 路径「只留 alt 文本、不落资产」的产品规则一致。正因为这条规则，行归一化把图片语法折成它的 **alt 文本**而不是整段抹掉（解析路径把内嵌图片的 alt 留在元素正文里，抹掉它这一行就永远匹配不上自己的元素，短文档里一行就能把覆盖率压到下限之下、整源被 `alignment_drifted` 误跳）；而行**分类**必须继续用抹掉 alt 的那份归一化——合并成一个会把每条独占成行的带图注图片变成「永远匹配不上的正文行」，9 张连续的能把 cov 从 1.00 打到 0.25。来源文件一律经 `SourceFileStore.resolve_path`（绝对原样、相对按仓库根）打开，**绝不**裸 `Path(file_path)`：历史行的 `file_path` 可以是仓库根相对路径，而这条离线命令从任意 CWD 启动都合法，裸 Path 会把整批历史来源误报成 `file_unreadable`。新元素 id 形如 `<锚点>-gNN`（C collation 下 `-`(0x2D) < 数字，故排在锚点与下一条之间），`created_at` **必须**回填成该来源既有元素批次的时间戳——详情分页按 `(created_at,id)` 排序，用 NOW() 图片会全部沉底，命令目录的 `source_element_generation`（= MAX(created_at)）也会平白漂移、把每一份待审候选作废。`sources.updated_at`、`chunk_elements` 反查行与元素插入**必须在同一个写事务里**（v46 红线；`updated_at` 的时间戳只能由仓储自己的时钟给——它是被当作**字符串**比较的变更信号令牌，离线调用方自带一个"看起来像 ISO"的短格式会写进一个比旧值还小的值）。这条原子性行为测试证明不了（把反查行挪进第二个写事务，行还是都在、用例全绿），由 AST 守卫 `backend/tests/test_image_backfill_transaction_guard.py` 按结构钉住。`chunked_at` 刻意不清，因此体检 H2（零元素）/H3（`chunked_at IS NULL`）不会把这批来源重新判成需要重新解析；H5（缺少元素向量）会如实数进新的带图注图片元素，那是诚实信号，其修复动作是既有的纯追加"补齐向量"。**停服 preflight 只有 PostgreSQL 有**，SQLite 上本命令可能与活服务并发，所以每源写事务的**第一件事是 CAS**：重读元素代次信号（`COUNT` + `MAX(created_at)`）与每个目标 chunk 的现值 `element_ids`，与计划快照不一致就抛 `app.domain.image_backfill.ImageBackfillConcurrentChange` 整事务回滚——否则一次落在计划与写入之间的重解析会把旧 `element_ids` 盖回换代后的 chunk、把图挂到已删元素 id 上，三种都不报错只是写脏。该源记 `concurrent_change`、与失败**分列**、**不进退出码**（补救就是重跑），而有真正失败的源时进程非零退出。CAS 读与那批写由**同一条** AST 守卫钉在事务体内——提到事务外就退化成 TOCTOU。图片 `src` 的集合键两侧共用**同一个规范形**（剥 query/fragment）：解析路径把 target 原样全量存进 `metadata.src`，只规范化一侧会让 `images/a.jpg?raw=1#x` 匹配不上自己的元素、被当成新图重复插入。资产必须先落（`AssetService.save_source_image` 自带事务），元素事务失败时回滚本源刚写的资产行与磁盘文件——`sweep_orphan_assets` 明确不扫 `source_id` 非空的行，孤儿留着永远没人回收。成功与失败两条路收尾都回收**本次调用亲手铸出来**、且无已提交元素引用的资产 id（计数 `orphan_assets_removed`）：它堵的是本工具唯一会漏的窗口——`save_source_image` **先提交行、后写盘**，写盘失败时抛异常而调用方拿不到返回值，所以 id 由它的 `on_row_created` 回调在**行提交那一刻**记下。判据**绝不能**放宽成任何形式的差集：SQLite 侧本命令与活服务并存，而在线解析**先落资产、后换元素**，执行期间冒出来的行可能是并发重解析建的合法数据（CAS 管元素与 chunk，不管资产）；文件名也区分不开——在线 MinerU 路径落的同样是 `Path(img_path).name`，一模一样的 `<sha>.jpg`。深拷贝是第二条理由：它为 `notebook_assets` 铸新 id 却**不**重映射 `source_elements.metadata.asset_id`，副本里每条来源图片资产行都「无人引用」。更早那些趟的残留仍按 docs 的判据人工清理。重跑幂等靠元素 `metadata.src`，比对的是"既有 image 元素且 `metadata.asset_id` 非空"，与引用带图的准入（`evidence_context._citation_image`）逐字同一条判据。**刻意登记的偏离**：标准分块管线（`chunking.build_chunks`）对既无图注又无描述的图片一律跳过，本阶段仍把它 append 进 chunk——判据是 markdown 里图片引用与该段文字的物理相邻关系（原始 PDF 版面序），是针对历史数据修复的一次性例外。每源张数与单图字节复用 `MINERU_MAX_IMAGES_PER_SOURCE` / `MINERU_MAX_IMAGE_BYTES`，`MINERU_RETURN_IMAGES=false` 直接拒绝运行，**空图片索引**（全部 `--mineru-output` 都不是目录，或任何 `images/` 下都没索引到文件）同样拒绝——带空索引跑下去是一次「看起来正常」的全库扫描，每张图都报缺图，运维会读成「原图确实找不回来了」。`--dry-run` 逐源打一行（覆盖率/候选数/锚定失败/缺图/图注），`--after-id` 是 keyset 断点续跑起点（中断后原样重跑安全但会全量重扫）；元素 id 的 `-gNN` 位宽是**三位**（每源上限默认 200，两位在第 100 张上让 `-g100` < `-g99`、元素顺序当场乱掉）——位宽固定而那个上限**部署可配且无上界校验**，两者在本工具这一侧闭合：同一锚点第 1000 张跳过并记 `anchor_suffix_exhausted`，绝不铸 `-g1000`（刻意不去收紧那个在线解析路径也在用的共享设置）；`--dry-run` 的 `captions` 取**计划**命中数而不是「实际落地」（dry-run 一张都不落，照后者会恒为 0、把 dry-run 的估算用途整个废掉）；不新增数字护栏、不新增前端（离线运维 CLI，同 `backfill-chunk-elements` 先例）。四个仓储方法双后端同修，但**刻意不进 `ports.py`**（那里 Protocol 方法总数是只许降的零余量上限），阶段按 `app/services/image_backfill_phase.py` 里的窄 Protocol 结构化依赖它们。
- **「待分析」不是 `NOT EXISTS(knowledge_objects)`**：正文极少、或整份是没有图注的图片的文档，分析跑完了本来就是零个知识对象。把它算成待分析会让信号**永远清不掉**——来源徽标恒显示「待分析」、看板恒提示「N 项内容未完成，继续分析」，而每一次增量分析都把这些来源按完整模型成本重跑一遍再得到零（用户读到的就是「一直分析不成功」）。判定「已分析·无知识」需要最近一次 `kg` 抽取记录**同时**满足：`completed`、消息以成功路径写下的零对象前缀开头、无失败窗口、非 partial 重试；`no-llm`（模型未配置）**刻意不算**——那种来源确实没被分析过，配好模型后必须被重新捡起，所以判据必须是**正向标记**而不是「completed 且没有失败标记」。权威表述只有一份 `app/models/sources.py::kg_analyzed_without_objects`，四个消费点：两条 pending 计数查询按方言镜像它（那个位置必须是一条 `COUNT`），来源投影与 `_kg_target_batches` 直接调它。`SourceSummary` 以并列的 `kg_analyzed_empty` 布尔上 wire（与 `kg_extracted` 互斥，合起来给徽标三态），前端唯一渲染路径是 `frontend/app/source-kg-badge.ts`——它必须给第三态带上自己的 class，因为 `.source-kg-badge` 的 `pointer-events:none` 会让浏览器直接跳过 `title` tooltip，而这一态的全部解释都在 tooltip 里。增量分析跳过这些来源，`mode="rebuild"` 仍选中它们（换模型或带 OCR 重新解析之后的回来路径）。守卫 `backend/tests/test_kg_empty_extraction_marker.py` 在真库上逐用例对账 SQL 镜像与 Python 判据，并钉住两个失败方向（少排除＝计数卡住；多排除＝真没分析过的来源被静默跳过）。精确判据只登记在 `docs/product-and-api*.md`。
- **来源详情有界加载**：浏览器打开来源详情时只取并渲染一页 element（默认 40、单请求最多 100），前后页按需加载；从引用跳转时后端解析包含目标 element 的页。旧全量读取只留给内部/兼容调用，禁止详情路径重新一次性拉取整篇大文档。
- **Memory 引用可追溯**：Ask 保存成 Memory 后，已持久化 citation 必须在 Memory 卡片展示来源标题/原始文件、位置和摘录；当前笔记本参与集内的活引用可打开精确 element，跨 notebook 传输后的引用明确标成仅存档，不得只显示引用条数。
- **MCP 冻结目录补充**：`mcp_server.CORE_TOOLS` 是精确 22-tool 内建前缀；`mcp_server.PUBLIC_TOOLS` 就是同一份列表（`PUBLIC_TOOLS = CORE_TOOLS = core_public_tool_names()`），静态文档/smoke guard、默认 live `list_tools()` 与 onboarding 都从它派生。不得维护第二份动态目录。
- **MCP 工具面与来源管理**：`/mcp` 的权威工具清单只有 `mcp_server.PUBLIC_TOOLS` 一份（当前 23 个：Memory/context 七、knowhow 四、引用点查 `get_cited_element`、来源管理六、构建三、库理解两 `get_notebook_profile`/`add_observation`）；第六个来源工具 `add_source_file` 以严格标准 base64 接收原始文件字节，并直接复用后端解析注册表，因此 PDF/PPTX/DOCX/表格/Markdown ZIP 与浏览器支持面同源。`backend/tests/test_memory_mcp.py`、`scripts/smoke_memory_mcp.py`（两者都拿它与实时 `list_tools()` 对账）与 `test_architecture_documentation.py` 的文档守卫都 `import` 它派生，别再另抄第四份——三份手抄清单可以互相认同一个陈旧值，同时与真实注册面脱节。scope 词表在 `AGENT_SCOPES`（后端）与 `AGENT_SCOPE_OPTIONS`（前端）两侧对等，新增 scope 必须同改；文档侧的 scope 校验也从 `AGENT_SCOPES` 派生（AGENTS.md 那句「The only scopes are …」按集合相等判，两份 SOP 的 scope 表另有中英对仗守卫）。四条红线：①**写类一律 owner-only**——`sources:write`（添加/重新解析）、`sources:delete`、`maintenance:execute`（触发图谱/检索索引构建）都要过 owner 守卫（`mcp_server._writable_notebook`／`user_can_access_notebook`），token 白名单里那个「owner 只是只读成员」的共享库读得到但绝不可写。**P2 之后这条与浏览器 HTTP 面刻意分叉、是要点名的新分歧**：HTTP 面的六个内容写能力（sources/kg/knowhow/knowledge/catalog + notebook:manage）已翻成 admin（组管理员经浏览器能写自己管理的共享库），而 Agent/MCP 面**不跟**——长期 token 是独立凭据，其 owner 可能在签发后很久才被授管理权，MCP 写工具删文档的爆炸半径正是这道 owner 门当初要防的（`deps._CAPABILITY_LEVELS` 与 `mcp_server` 双侧注释同记这条拆分，`notebook:delete` 两面都恒 owner，后续如放开单独一件事过评审）。对比：knowhow 代码写入是 notebook 成员面（设计 §⑥-4 明文不加 owner 轴），删文档的爆炸半径大得多，这处分歧同样是拍板取舍不是疏漏（两侧各有守卫：`test_source_writes_are_refused_in_a_read_only_shared_notebook` / `test_knowhow_code_write_is_allowed_in_a_read_only_shared_notebook`）。②**只能删 Agent 添加的来源**——判据是 v48 `sources.agent_profile_id` 非空的 `agent_created` 投影，与证明笔记本归属的是同一次单行读取；判据是「某个 Agent 添加过」而非「本 profile 添加过」（profile 会轮换，按 profile 判会留下永远删不掉的来源），列缺失时投影默认 false、fail closed。③**出处不被去重或拷贝洗白**——只在 INSERT 分支写入，所以同内容去重复用用户的行时该列保持为空、重传用户的字节洗不成 Agent 可删；深拷贝显式清空，副本一律视为用户添加。`ask_notebook` 的 `conversation_id`（同 owner+同 notebook 才接续，否则静默新建、靠比对回传值察觉）与 `citations` 回退列表（`memory_id` 非空行需 `memory:read`，无此 scope 整行在截断**之前**过滤且不计入截断计数，否则私有 Memory 条数会被算术还原）同属这条契约。④**`agent_observation:write` 是第二个绕开 owner 门的 Agent 写**（Agentic Memory P3；第一个是上面提到的 `knowhow:code`）——`add_observation` 向调用者自己在该库的观察队列追加一行，权限门是 `_selected_notebook`（读权）而不是 `_writable_notebook`（owner-only）。论证与 `knowhow:code` 同构：owner-only 红线守的是会改变全体成员检索的写，而这个工具的爆炸半径结构上只到 token 持有者**自己**的覆盖层——`agent_profile_id` 服务端派生、写入只追加进调用者自己的 `(notebook, owner)` 观察队列，读取（覆盖层巡固、`GET .../agent-observations`）同样按 `owner_id` 谓词收窄到本人——要求 owner 门等于禁止只读成员的 Agent 写这个成员自己拥有的私有覆盖层。`get_notebook_profile`（`agent_profile:read`）是纯读，不在此列；`_writable_notebook` 的 docstring 已把「唯一不覆盖的写」改成「两个」。精确数值上限只登记在 `docs/product-and-api*.md`。
- **Agent 观察队列与用户回答偏好（Agentic Memory P3）**：两个互相独立的面，只在 schema v55/PG v33 迁移上耦合。**观察队列**——外部 Agent 经 `add_observation`（scope `agent_observation:write`）向自己在该库的 `(notebook, owner)` 队列追加一行短文本，环形上限 200 条，服务端立即返回（零模型调用）。它是**不可信输入**，只喂该成员自己的覆盖层巡固（`agent_profile_job.py`）：巡固读到观察记录时在 user 消息前插一条 `system` 指令声明每行是数据而非指令，且一条观察只有与该成员自己的提问/报告相符才能支撑一个论断；仅有观察、没有提问/报告不触发巡固；观察绝不移动 `usage_gaps` 的零命中计数；渲染独立 600 字符预算，不占用提问/报告共享的 3,000 字符段。管理走「我的」半侧（`GET`/`DELETE /notebooks/{id}/agent-observations`，读权+行级归属即可，不需要 `agent_profile:write`）。只读版 `get_notebook_profile`（scope `agent_profile:read`）投影仅 `{label, value, updated_at}`，不带 `evidence` 来源 id，标注 `content_is_untrusted_evidence`/`citable: false`。**用户回答偏好**——每用户偏好文档 `user_profiles.search_profile_json`（`NULL`=未设置，同 `ui_mode` 契约），四个封闭字段（`answer_language`/`answer_shape`/`answer_detail`/`domain_terms`），逐字段 `origin: "user"|"job"` 区分用户显式设置与后台归纳，job 永不覆盖 user（`merge_field` 的唯一承重分支）。v1 归纳规则唯一一条且零 LLM：按该用户最近提问语言的确定性多数统计写 `answer_language`；档位众数与常用领域词刻意不归纳。**归纳出的 `origin="job"` 值绝不单独注入 prompt**——需用户显式确认（即一次 `origin="user"` 写入）才生效，镜像 P2 经验库「先接好管线、注入待验证」姿态：一个推断错的语言会与 `answer_prompt` 「按提问语言回答」的默认规则直接矛盾。渲染出的风格块只注入 Ask 的规划与答案合成 prompt（不进反思循环——措辞偏好与下一步该选哪个检索动作无关；深度报告 v1 不接），且结构上不含任何 source/notebook/scope 词（范围守卫）。部署开关 `USER_SEARCH_PROFILE_ENABLED`（默认 true）独立于 `AGENT_PROFILE_ENABLED`；账户菜单新增「我的回答偏好」，网页面板「我的检索心得」下新增「Agent 记录」折叠小节。精确数值上限只登记在 `docs/product-and-api*.md`「Notebook understanding blocks」与「User search profile」两节。
- **MCP 长任务心跳与 SSE 传输**：MCP 客户端不会无限等一次工具调用——Claude Code 用 *idle* 超时（若干秒内既无响应也无 progress 通知就中断），别的客户端是每次调用一个固定上限——而 `reasoning` 档的 `ask_notebook` 动辄几分钟，于是客户端会放弃一次服务端仍在**正常完成**的调用。所以 23 个工具**各有且只有一次**主阻塞调用走 `mcp_server._run_with_progress`，每 `PROGRESS_HEARTBEAT_SECONDS` 发一条 progress 通知，内容**只有**工具名与已耗墙钟秒数——绝不带问题原文、笔记本/来源名或任何笔记本内容（与观测事件同一条口径）。「每个工具恰好包一次」本身是不变式：它让 progress 值在一次请求内天然单调，不必引入任何 per-request 计数器；而 `report_progress` 在客户端没发 `progressToken` 时是 no-op，不要进度的客户端一分钱不付。**两个承重的坑，各有经变异验证的守卫**（`backend/tests/test_memory_mcp.py`）：①FastMCP 必须 `json_response=False`——JSON 模式下 SDK 会把每请求的流读干、只找响应，**沿途每一条通知都被丢弃**，`report_progress` 变成昂贵的 no-op、心跳静默失效（实测：同一次调用 SSE 模式官方客户端收到 4 拍，JSON 模式 0 拍）；②工作必须在**子任务里捕获异常、任务组关闭后再重抛**——anyio 4 会把任务组**体内**抛出的异常包成 `ExceptionGroup`，而 FastMCP 把工具抛出的东西直接变成 Agent 读到的错误文案，天真写法会让**每一个**工具失败都报成 `unhandled errors in a TaskGroup`，而不是工具自己写的那句话。SSE 模式唯一的用户可见代价：`POST /mcp/` 必须 `Accept: application/json, text/event-stream`，否则 406——Streamable HTTP 规范本就要求两个都发，SDK 客户端、测试与 SOP 的 curl 也都这么发。心跳**不是**我们这边的超时：这个面上没有任何东西会放弃仍在运行的工作，客户端那条上限仍归客户端配置（SOP 登记了 Claude Code 的 `"timeout"`／两个环境变量、Codex 的 `tool_timeout_sec`，以及反向代理这第三道超时——响应已带 `X-Accel-Buffering: no` 与 15 秒 SSE 保活，缓冲了这条流的代理会**无声**架空心跳）。间隔数值与客户端/代理指引只登记在 `docs/product-and-api*.md` 与 `docs/agent-mcp-memory-sop*.md`。
- **Agent 自配置交接**：签发回执显示匿名机器说明 `GET /api/agent-mcp/onboarding`，用户把该链接与 token 分开交给 Agent。说明把 `MCP_PUBLIC_URL` **逐字**印成要配置的地址（绝不改写成带斜杠的变体——那是部署公布的契约，代理可能只路由这一条精确路径），同时写明打到后端的 `POST /mcp` 是 307 指向 `/mcp/`，好让跟不了重定向的客户端有解法；工具清单从 `mcp_server.PUBLIC_TOOLS` 派生，可在服务 warm-up 期间读取；route 对任意 query string 或 Authorization header 直接 400，绝不接收、拼入或回显 bearer token。`MCP_PUBLIC_URL` 已从内部 metadata 跨入匿名 Agent 指令面，启动只接受绝对 `http(s)`、path 精确 `/mcp`、无空白/控制符/反引号/userinfo/query/fragment 的 URL，非法值必须 fail-fast，不能让部署变量注入说明正文。Codex 的 `bearer_token_env_var` 只持久化变量名：Agent 子进程不能把 `export` 注入当前父进程或热加载工具，`codex mcp list` 也只证明配置存在；说明必须报告剩余的安全凭据/重启动作，直到新 session 成功调用 `list_notebooks` 与 `select_notebook` 才能声称接入成功。Claude Code 侧同理但机制不同：渲染出的 `claude mcp add` 命令带**单引号**的 `${SILICON_NOTEBOOK_AGENT_TOKEN}` header——Claude Code 在连接时按启动它的进程环境解析它，所以凭据不落 `~/.claude.json`；裸 `<AGENT_TOKEN>` 只作为不支持插值的客户端的兜底。未定义的变量会被原样发出、只以坏 token 失败且配置阶段不报错，这也是「新 session 真的跑通那两个调用之前不得声称成功」的另一条理由。
- **MinerU 远端重试**：自建 `MINERU_MODE=http` 与 mineru.net 云端适配器共用 `MINERU_MAX_RETRIES`（额外尝试数，`0..5`，默认 `2`，即总共最多 3 次）。只对网络/超时、HTTP 408/425/429/5xx 与空/非 JSON 响应做以 1 秒起始的有界指数退避（默认延迟为 1、2 秒）；明确 4xx、云端终态失败与业务解析拒绝仍立即失败，本地 `cli` 子进程不重试。测试必须显式清空 token，不能从仓库根 `.env` 回读真实凭证或发真实网络请求。
- **MinerU 最终降级**：远端 URL/上传文件在重试耗尽、业务失败或结果映射为 0 元素后，摄取必须回退该格式对应的本机库解析（每种格式各有自己的分级本地链：PDF 走 PyMuPDF4LLM 版面感知 Markdown → pypdf 仅作依赖/解析器异常的最后兜底；DOCX 走 mammoth 语义化 HTML → python-docx；PPTX 走 python-pptx → 原始幻灯片 XML；XLSX/XLSM 走 openpyxl），不能把可下载的 URL 直接落成失败。成功降级的来源保持 `extracted`，原始诊断只留 pipeline log/source `error_message`，对外只给稳定的 `parse_quality_warning`；来源详情必须提示版面/公式/表格/OCR 可能异常，并给 owner 重新解析/删除入口。后续 MinerU 重解析成功必须清空诊断与警告。**「MinerU 值得一试的后缀」只有一份真源** `parsers.MINERU_CAPABLE_SUFFIXES`（`.pdf`/`.docx`/`.pptx`/`.xlsx`/`.xlsm`）：云端上传分支只对它发起——把 `.md`/`.csv`/纯文本上传 mineru.net 等于把用户内容外发给一个根本解析不了它的第三方，还白付一次网络往返与重试预算。**降级警告按刻意更窄的** `parsers.MINERU_FALLBACK_WARNING_SUFFIXES`（`.pdf`/`.docx`/`.pptx`）**判定**而非只认 `.pdf`、也不是整个 capable 集：docx/pptx 的降级此前静默、必须点亮；而 xlsx/xlsm 的 openpyxl 兜底对单元格值全保真，警告是噪音——部署的 MinerU 若不支持工作簿，它还会永远点亮且重新解析也消不掉。MinerU 元素标签/`source_format` 前缀一律取自 `parsers.mineru_label_prefix(file_name)`（含两条云端分支）：写死默认值会让 cloud-only 部署上传的 `.xlsx` 报成 `PDF p.1 …`。**工作簿的 MinerU 非空产出必须先过行 + 格覆盖对账才被采信**（`MINERU_WORKBOOK_MIN_ROW_COVERAGE`，协议边界，行与格两个维度共用同一阈值；纯本地流式计数、零模型零网络，两个分母同一次遍历数出）：MinerU 按渲染页出表，打印区域/列宽可能整列整 sheet 丢内容**且不报错**，采信残缺产出就是连降级警告都不会亮的静默数据损失。**两个维度缺一不可**——只数 `<tr>` 对最常见的宽表残缺完全瞎（每一行都在，页宽之外的列整片没了）；非表格文本元素每条最多只向两个分子各计 1（MinerU 的 sheet 标题/游离单元格要计入，但虚增必须有界），image 元素两个分子都不计入（图片不对应任何物理行或格，计入它等于让「几张图 + 少量行」刷够分子）。任一维度覆盖不足即整份丢弃回落 openpyxl（全保真，丢的只是表格 HTML 结构、绝不丢数据）；openpyxl 缺失/读不了则 fail-open 采信。**对账不是本地解析路径专属**：云端上传的**文件**分支对 `parsers.MINERU_WORKBOOK_SUFFIXES` 经公共入口 `parsers.mineru_workbook_output_accepted` 跑同一道对账——cloud-only 部署根本不经过 `parse_xlsx`，不接就是给护栏留了个后门（URL 分支保持 PDF 语义、不动）。两条路径的映射都**分两段**：先用**不带 `persist_image`** 的探针映射喂对账，通过才带图片重映射一次——`mineru_content_list_to_elements` 会把图片作为副作用写成资产行/磁盘文件，一次性映射会让被拒收的产出留下孤儿资产。稳定前缀常量 `PDF_PYTHON_FALLBACK_WARNING_PREFIX` 的名字与取值一律不许改：四个存储层、shadow parity 与既有已入库行都靠它前缀匹配，只有正文措辞去 PDF 化。旧版 .xls 由 xlrd 直接本地解析（不经 MinerU；.doc/.ppt 仍拒绝并提示另存为）。
- **Markdown data URI 图片摄取**：markdown 来源 `![alt](src)` 复用 MinerU 图片护栏而非另开一套——`src` 为 `data:image/{png,jpeg,gif,webp};base64,...`（同一 mime 白名单）时经与 MinerU/DOCX/PPTX 共用的 `persist_image` 闭包落资产，`MINERU_RETURN_IMAGES`/`MINERU_MAX_IMAGE_BYTES`/`MINERU_MAX_IMAGES_PER_SOURCE` 同等生效（该开关现在门控所有来源类型的图片持久化）。base64 载荷绝不进元素 `text`/`metadata` 与 KG 抽取窗口：`kg/parsing.py` 的 image 元素只用 caption 文本、且把跨度收缩到 caption 长度，防止大图字节被反复切进多个抽取窗口。有 alt 才产出元素、进 chunk 检索；alt 与图片描述块都没有、又未成功落资产的图片不产出元素（带图片描述块的无 alt 图片是下一条的例外）。只处理独占一整段的图片，列表项/表格单元格内的内嵌图片只留 alt 文本、不落资产。
- **Markdown ZIP 后端摄取**：`.zip` 已进入后端解析能力注册表，原包按一个来源保存，由内建 `markdown_bundle` 在后台稳定遍历所有 `.md`/`.markdown`；图片路径相对各自 markdown 成员解析，命中 png/jpeg/gif/webp 后一律走既有 `persist_image` 资产端口，元素记录 `bundle_path`。ZIP 从不解压到宿主文件系统；危险/重复路径、加密/不支持压缩、无 markdown、条目或解压总量超限都拒绝整包，单张缺失/远程/损坏/不支持图片则 fail-open 为只有图注/描述的 image 元素。重解析继续复用同一来源锁与资产代际替换。拖入文件夹仍走浏览器端 data URI 内联与持久回执，不能把那条兼容路径重新当成 ZIP 主流程。精确护栏只登记在 `docs/product-and-api*.md`。
- **公式渲染**：紧邻正文、独占一行的单行 `$$...$$` 仍按块级公式处理；Ask、报告、Memory、Knowhow 的宽公式只在所属内容块内横向滚动。来源详情、知识对象标题及知识图谱「出处」卡中的 `formula` 元素共用 KaTeX 渲染；直接调用 KaTeX 前先剥除包裹整值的 Markdown 数学定界符，解析失败必须显示原始内容，不能留下空白或仅错误态的可视化。
- **Markdown 单波浪线不是删除线**：渲染模型产出文本的 GFM 插件真源只有 `frontend/app/markdown-gfm.ts` 的 `remarkGfmPlugin`＝`[remarkGfm, { singleTilde: false }]`，Ask 回答/Memory 卡片、深度报告、报告公开分享页、问答会话公开分享页共用，调用方不得再传裸 `remarkGfm`。remark-gfm 4.0.1 默认 `singleTilde: true`（github.com 行为，GFM 规范不允许），**单个** `~` 就是删除线定界符：中文技术答案里 `7~5nm`、`80~90%`、`2~3 周`、`~3GHz` 这类写法同段出现两个，中间整段正文就被 `<del>` 划掉，无任何报错。规范的 `~~删除线~~` 逐字不变。唯一豁免是 knowhow 格子预览（`knowhow-cell-editor.tsx`）——它与 `md_normalize.py` 是按「单 `~` 是删除线」互相论证的孪生实现，单方面改会让护栏与渲染口径分叉，属独立一件事。回归门 `frontend/tests/guards/markdown-single-tilde-guard.test.mjs`（裸 `remarkGfm` / 真源被翻回 true / 绕过 JSX 直接 import 三条，外加三个渲染面的空转保护）与 `frontend/tests/component/answer-markdown-tilde.component.test.tsx`。
- **文档同步**：影响安装、产品行为、架构或开发约束时，`README.md`、`README_zh.md`、`AGENTS.md`、`CLAUDE.md` **四份**一起改，并同步 `docs/` 下负责该主题的中英文权威文档。根 README 只保留精简入口，详细契约不得重新堆回 README。漏掉本文件，Claude Code 侧的规范就会悄悄过期——那正是本文件存在的原因。外部 Agent 的首次接入步骤由 `docs/agent-mcp-memory-sop.md` / `_zh.md` 成对维护；界面路径、Codex/Claude 配置、scope 表、`scripts/example_mcp_memory_client.py` 的可运行命令、Memory 人审门与撤销说明必须和产品/API 契约同步。
- **模型服务配置热加载**：`MODEL_SERVICES_CONFIG` 的 chat 服务可配置固定 `top_p`，它覆盖调用默认值并进入实际请求/缓存身份。进程监视非空 TOML；watcher 连续两次观察到同一变更签名后才解析，并在发布前复核读后签名仍相同，避免原地写入短暂暴露的移动快照；空/仅注释配置在 reload 中按非法版本拒绝，进入离线模式须清空 setting 并重启。缺失、半写或非法版本保留上一份有效配置并只发脱敏诊断。显式强制 reload 跳过两次观察，但仍做读后复核。新调用解析新绑定/新 runtime 代际，已提交任务留在原代际完成；状态页必须读取实时 registry，业务服务不得缓存或直连物理 raw client 绕开这条边界。
- **密码管理**：自助改密 `PATCH /me/password` 校验当前密码后改写，并吊销该用户**其他**浏览器会话（`keep_token` 保留当前请求的会话）；管理员重置 `POST /admin/users/{id}/reset-password` 吊销目标**全部**浏览器会话，且必须由真实登录会话发起（auth_optional 匿名回退 403）。Agent 长期凭据（`agent_access_tokens`）刻意不在两种吊销范围内。登录必须走 `login_with_password`（验证+建会话单写事务，SQLite 写锁 / PG 对 users 行 `FOR UPDATE`），与改密在同一把锁上串行——拆成 authenticate + create_session 会让旧密码登录的会话逃过吊销（codex R1 P1）。内置管理员 `user-local` 两条路径一律 409（`BuiltinAdminPasswordError`）——它的密码每次启动都被 seed 按 `SILICON_NOTEBOOK_ADMIN_PASSWORD` 重写，在线改动会被下次重启静默回滚；前端对它隐藏「修改密码」入口、用户总览该行密码列显示「受保护」，本人行显示「本人」（管理员重置自己会当场吊销自己全部会话，UI 不提供该入口）。
- **用户使用总览**：先对 `/admin/users` 返回的完整用户集合排序，再做前端分页；默认每页 20 条，可切换 20/50/100 条。用户名、角色、注册时间、各类用量、最近活跃和文档上限表头可切换升降序，排序或翻页时清理已隐藏行的临时交互态。问答用量及展开后的笔记本明细统一返回/展示 `questions`，按归属该用户的持久 `ask_jobs` 提交次数计数（失败/取消任务也算一次已提交提问），不能拿 `conversations` 会话容器数冒充提问次数。用户总数包含其在加入的只读共享笔记本中的提交；展开清单刻意沿用 owner-only，只分解自有笔记本内的提问，不能假设其合计等于用户总数；报告数同此口径（总览按 `reports.created_by` 计数、含共享笔记本内自建的报告，明细仍 owner-only，合计同样不要求等于总数）。旧 `conversations` 响应字段仅为兼容保留并标记 deprecated，前端不再用它展示问答用量。
- **用户活动日志页**：`/dev/logs` 默认视图从「模型调用流水」升级为「用户活动」，顶部新增视图 tab：活动（默认）/ 模型调用（原样保留、逐位不变）。活动视图三栏——左「范围」（该用户的笔记本清单 + 展开来源清单）、中「活动流」（提问/来源/报告三类按时间倒序混合，`(created_at DESC, id DESC)` keyset 翻页）、右「详情」（随选中项切换）；点笔记本过滤中栏，点来源只打开右栏详情（不过滤中栏）。三个新增只读端点（`GET /admin/users/{id}/activity`、`.../notebooks/{nb}/sources`、`.../asks/{job_id}`）权限口径为 self-or-admin（镜像 `debug_logs._resolve_owner`），另受独立部署开关 `USER_ACTIVITY_VIEW_ENABLED` 门控（默认 **true**——活动是默认 tab，若沿用默认关闭的 `DEBUG_LOGS_ENABLED` 会让普通部署一打开页面就 404），与 `DEBUG_LOGS_ENABLED` 相互独立（该开关只继续管「模型调用」视图背后的 `/api/debug/logs/...`）。前端没有别的途径拿到这个部署时取值，`GET /system/config` 因此把它作为 `user_activity_view_enabled` 一并下发；关闭时 `/dev/logs` 隐藏「活动」tab、`view` 默认落到 `llm`（`?view=activity` 深链同样归一），字段缺失（旧后端）按 `true` 处理而不藏掉可用 tab。左栏复用的 `GET /admin/users/{id}/notebooks` 同样是 self-or-admin 而非仅 admin——普通用户查看自己的活动必须能读到自己的笔记本清单。提问/来源/报告一律按该用户自有笔记本 owner-only 收窄（与用户使用总览「展开清单 owner-only」同口径），隐藏合成来源（`memory`/`knowhow`）不进清单；`list_user_notebooks` 的报告计数同样按 `created_by = 该用户` 收窄（与 `questions` 既有谓词、活动流自身报告条目同口径），共享笔记本里别的可写成员建的报告不再计入 owner 表头。`since`/`until` 半开区间，两个数据库后端共用 `parse_activity_instant`；畸形时间输入直接报错、不静默放宽窗口；SQLite 侧用 `julianday()` 按绝对时刻比较（裸 naive 历史文本按 UTC 读，与带 offset 的新行统一口径）。来源活动条目不回传原始 `error_message`（只给派生的 `parse_failed` 布尔，同 `ScopedSourceDetail`）。`SourceSummary` 新增 `display_title` 字段（`source_display_title` 单一实现，论文标题优先）——新增而非改写既有 `title`，来源页签零回归。报告条目耗时沿用 `generation_started_at → updated_at` 红线。明确未做（P2/P3，非遗漏）：模型调用挂到触发它的提问下（需要新写侧 `_log_context` 透传，历史日志无法回填）、`ask_stage`/`pipeline` 阶段耗时挂进详情、左栏点来源过滤中栏、来源清单翻页。
- 完成 `silicon_notebook_fangan.md` 里定义的特性时，同批更新 `fangan_done.md`。
- 提交的文档保持通用口径：绝对解释器路径、本机端口占用这类**机器特定细节不进 git**。

### 硬门

- 门禁分为 G0 目标测试、G1 `bash scripts/check.sh` 标准门（编辑期及每次 PR/push，默认 12 个后端 pytest worker + 语法/契约/harness + 前端 test/production build/补测试文件类型诊断的 `npm run lint` 三条并发泳道；Node/Vitest 各限 4 workers，Apple Silicon warm 目标 ≤60 秒）、G2 `bash scripts/check_extended.sh` 扩展门（再补跑 slow 真实索引/性能测试、graph_index_contract 冷图/索引契约与 architecture_contract_heavy 全仓语义扫描重活半（单测 >2s 的 8 个，见 `backend/tests/conftest.py::_ARCHITECTURE_CONTRACT_HEAVY_TESTS`；其余 56 个轻活 architecture_contract 测试已随 G1 每次 PR 跑），每天 18:17 UTC/北京时间次日 02:17 一次，也可手动触发）和 G3 独立 PostgreSQL 集成门。CI 另有 `level-1-frontend-node26`：与 G1 同触发，在 Node.js **当前**大版本上重跑前端泳道与生产构建。它不是第四道门禁，而是补上「文档承诺 ≥ 20、G1 只钉 22」的验证缺口——Node ≥ 24 自带的 Web Storage 全局会在 vitest 的 jsdom 环境里盖住 jsdom 那份并返回 `undefined`，凡是读 `localStorage` 的组件测试都会在开发者本机整片红而 CI 全绿；`frontend/test-support/setup.ts` 只在内建 storage 取值为 `undefined` 时补回真 jsdom storage 与配套 `Storage` 类（否则 `vi.spyOn(Storage.prototype, …)` 静默打空），Node 22 行为逐字不变。G1/G2 的 backend marker 必须精确互补。测试加速保持断言和生产默认值不变：全仓语法扫描按进程复用解析结果，纯缓存策略不搭建无关数据库/索引；普通 UT 与 G1 测试必须环境自足，不绑定宿主端口、不依赖环境服务；只有合同本身属于进程级行为时才保留自包含的子进程/信号覆盖；并发顺序用 event/barrier 而不是固定 sleep 证明；后端普通仓储测试按 worker 复用一次当前空 SQLite schema、再为每条测试复制成独立可变文件，迁移/升级/快照契约模块必须登记 `_REAL_SQLITE_MIGRATION_MODULES` 并跑真实迁移梯；仓储密集测试只可在 pytest autouse fixture 中降低默认密码派生成本，认证 helper 仍验证生产默认，比较凭据字段的快照模块必须登记 `_REAL_PASSWORD_HASH_MODULES`；分波次排队时由控制线程运行被测同步编排并在观测到容量后用 event 放行，不能让下一波误入 cyclic barrier 的超时；测试启动的进程级延迟任务须在共享 teardown 中取消待执行项并等待活跃项收敛，不能只清理由某个局部 repo 可见的调度器；与规模无关的边界分支只降低测试局部阈值并另断言生产 floor，共享同一不可变产物的多组断言只做一次真实构建，纯算术/观测分支用最小接缝且邻近集成测试仍真实构建、打开和查询产物；`next build` 未启用 `ignoreBuildErrors`、仍是生产代码类型检查的权威，但 Next 的构建期类型检查会**静默丢弃** `*.test.*`/`*.spec.*` 文件与 `__tests__`/`__mocks__` 目录里的全部诊断（`runTypeCheck.js` 的 ignoreRegex，Next 15.5 实测）——只出现在 `frontend/tests/**` 的类型错误对 build 完全不可见，因此前端泳道在 build **之后**补跑一次 `npm run lint`（`tsc --noEmit`，唯一看得见测试文件的检查；排在 build 后是让 `.next/types` 刚被重新生成、脏树上的陈旧生成物不会假红；`incremental` 使 warm 重查 <1 秒、冷 ~5 秒）。原先「两遍解析同一程序纯属重复」的约束因两遍覆盖面并不相同而废止，守卫已翻转为钉住 lint 必须在场且在 build 之后。
- **Codex 权限规则**：GitHub 网络操作（`git fetch`、`git push`、`gh auth/repo/pr`）必须直接申请沙箱外执行；普通本地只读 Git 检查与环境自足的 G1 门禁在沙箱内完成。
- 数据库专项门只覆盖直接 PostgreSQL 后端；已退役的 SQLite 后端实现专项测试、SQLite→PostgreSQL 导入/正向 shadow 测试与跨后端 parity 测试不得重新加入当前套件。
- **schema**：加表或改结构必须**追加** `_migration_N` 并 bump `SCHEMA_VERSION`，不要塞进已封版的旧迁移——版本闸会对已部署库短路，`IF NOT EXISTS` 救不了没被执行到的语句。当前 SQLite schema 为 v59；PostgreSQL checksummed schema 为 v37，正向 shadow 当前配对同样是 SQLite59/PG37/epoch1。除关系端点 keyset 覆盖索引和关系补全的来源代次水位外，两侧还包含生成中 Ask 的浏览器提交时间 `ask_jobs.asked_at`、图谱质量分析的三张预计算产物表、按 `(source_id, element_type, created_at, id)` 的集合枚举索引 `idx_source_elements_source_type`，排除隐藏 Memory/Knowhow 投影的部分可见来源身份索引 `idx_sources_visible_identity`，以及命令目录抽取的 `catalog_jobs`／`catalog_candidates`（前者带按来源的 `queued`/`running` 条件唯一单飞索引与记录创建时刻来源代次的 `source_generation`；后者**刻意不给 `job_id` 加外键**，否则 `catalog_jobs` 不再是叶表，正向 shadow 就没有可用的 UNIQUE 停车方案）与它按标题解析目标表用的 `idx_knowhow_tables_nb_title`（`knowhow_tables(notebook_id, title, created_at, id)`，v39 里唯一建在既有表上的索引）。v40/v18 另增加只写的来源局部事实 `knowledge_source_facts` 与证据绑定 `knowledge_source_fact_elements`：同一事务校验当前 extraction generation 和证据来源，未接地对象不得进入来源事实。v41/v19 再增加 `knowledge_source_fact_backfills`，只供显式离线、可恢复、无模型的历史投影：每本库先构建一次来源反查索引并在后续命令复用完成标记，再按来源做有界 keyset 分页和逐页短事务；只有 owner 与全部证据元素都能证明属于同一来源的对象才投影，歧义数据标 `incomplete` 而不猜测；账本和只读审计只暴露游标、计数、版本、稳定失败码、状态与有界 id，审计独立对账有效 KG 代次、投影版本和持久事实数量，不信任账本上的 `complete`。v42/v20 增加 notebook 级 `source_index_backfills` 执行账本，逐页原子提交来源反查索引行与 keyset 游标，重启续跑已提交页面，KG 代次漂移失败关闭且不记录异常正文；v43/v21 增加报告公开分享 token；v44/v22 增加 `chunks.question_indexed_at` 与 `chunk_questions` 生成问题索引，正向 shadow 按 bytea/timestamptz 规则保真复制；v45/v23 增加 `user_profiles.ui_mode` 界面模式偏好可空列（NULL＝尚未选择，读路径回落「自动」，迁移不回填既有行）；v46/v24 增加 element→chunk 反查表 `chunk_elements`、它的离线回填账本 `chunk_element_backfills` 与分叉读路径的 `unified_kg_state.chunk_elements_indexed` 标记列；v47/v25 增加笔记本本地 `notebook_object_schemas`，以 owner 管理的同名行覆盖管理员全局基线，但不扩张四类来源抽取契约；v48/v26 增加可空的 `sources.agent_profile_id`（Agent 添加来源的出处标记：只在 INSERT 分支写入，同内容去重复用既有行时保留首写者出处，深拷贝显式清空，故副本一律视为用户添加；无索引/无唯一约束/无外键，投影成 `agent_created` 布尔）；v49/v27 增加群组知识共享的三张表 `groups`／`group_members`／`notebook_grants`——`notebook_grants.principal_id` 是**多态**引用（user_id｜group_id｜`everyone` 存 `''`），刻意**无 principal 外键、无 CHECK**，且必须保持 `NOT NULL DEFAULT ''`：NULL 不参与唯一比较，会让 `everyone` 主体整个逃出 `UNIQUE (notebook_id, principal_type, principal_id)`（重复授权可累积、撤销撤不干净），而 NOT NULL 还把正向 shadow 的停车列让给了 `principal_type`（SENTINEL_TEXT）。`everyone` 判据只能写 `principal_type='everyone'` 的四值精确匹配，绝不能从 `principal_id` 推断（`IS NULL`／`=''` 都不行）——停车会给冲突行的 `principal_type` 暂写哨兵串，精确匹配让停车行 fail-safe。深拷贝**不带**授权边（照 `notebook_members` 先例：访问控制状态不是知识，副本由新 owner 重新授权）；v50/v28 增加成员贡献审批流表 `notebook_share_requests`（`notebook_grants` 的兄弟表，两个 FK 均 CASCADE）——审批流状态机 `pending → approved/rejected` 单向，撤回是申请者走整行 `DELETE`、不写第三个状态，故 `decided_by`/`decided_at` 语义纯粹是「组管理员做出的决定」。`decided_at` 只允许写 SQL `NULL`（未决定）或 ISO 时间戳，**绝不能写空串**：它是本表唯一进入 shadow 正向复制的可空时间列，PG 侧 `timestamptz` 收到 `''` 会直接类型报错、poison 整条正向复制通道，且刻意**不**登记进 `POSTGRES_EMPTY_TIME_SENTINELS`。防重复 pending 的部分唯一索引 `uq_share_requests_one_pending`（`(notebook_id, group_id, status) WHERE status = 'pending'`，三列版把第三列 `status` 让给 shadow 停车的 SENTINEL_TEXT 候选、不锁死入向 FK）保证同一 (库, 组) 至多一条在飞申请；`status` 消费方只许精确匹配 `pending`/`approved`/`rejected` 三值，绝不能用 `!=` 当「已决定」判据（停车会给冲突行的 `status` 暂写哨兵串，否定式判据会把停车行误判成正常状态）。深拷贝同样**不带**申请。v51/v29 增加 Agent 库理解的两张表 `agent_notebook_profile`（`PRIMARY KEY (notebook_id, owner_id, label)`，`owner_id=''` 为共享底座、非空为该成员的个人覆盖层，同样 `NOT NULL DEFAULT ''` 哨兵而非 nullable）与 `agent_profile_jobs`（`PRIMARY KEY (notebook_id, owner_id)`，单飞由 PK 行 CAS 承担）；两张表的 replication key 逐字等于声明 PK，停车策略因此自动落到 `REPLICATION_KEY`，不需要哨兵列、不需要独立 UNIQUE 索引、不需要叶表 delete/reinsert；变更历史用块表内的有界环形 `history_json` 列（20 条），不新建独立历史表——P1 界面只有看/改/清空/手动重建，没有历史回看，一张可查询流水表买不到任何 P1 能力。深拷贝不复制这两张表（副本从零重新形成理解，job 行是过程状态）。v52/v30 给 `conversations` 加会话公开分享的三列 `share_token`（可空，部分唯一索引 `idx_conversations_share_token WHERE share_token IS NOT NULL` 只覆盖已发放 token、NULL 停车与 `notebooks.share_token`/`reports.share_token` 同款）＋读取水位 `shared_through_at`（时刻字面值，不是外键——存 answer id 会在该 answer 删除后失去意义）＋展示用 `shared_through_id`；token 挂在行上不另开侧表（同 `_migration_43` 报告 token 先例，会话删即带走链接）。**深拷贝无需处理**——`_COPY_VALIDATED_TABLES` 本就不含 `conversations`（会话不随副本走），不存在「副本要不要带 token」，但迁移注释里写明了这条以防后来者照 notebooks/reports 先例去加多余清空。v53/v31 增加 `agent_profile_jobs.claim_token`（巡固链路的**认领代际**，`TEXT NOT NULL DEFAULT ''`，无索引/无唯一面/无外键）：每次 `claim` 现铸新值，`settle` 与 `write_block` 都把它当 CAS 条件的一部分，关掉 P1 只按 `status` 单飞留下的 ABA——成员移出再加回来得到的是主键逐字相同、`runs` 归 0 的新行，所以主键与 `runs` 都当不了代际；时间戳同样不行（SQLite 时钟截秒，与 `memory_revisions` 同一条教训）。`settle` 因此从二值变**三值**：`settled` / `gone`（行没了，只有成员移出会删它 → 抹掉这一轮重建的块）/ `superseded`（行还在但属于更晚的认领 → **绝不能**抹，新一代可能已经写好了块）；判别读必须与 CAS 同事务，出了事务一次并发移除就能把 `superseded` 读成 `gone`，把「放手」翻成「删除」。`write_block` 的 `claim_token` 可选，非空时在同一写事务内校验并抛 `AgentProfileClaimSuperseded`，空值是用户自助编辑那条路（它本就不持有认领）；`sweep_stale_on_start` 刻意不看 token。不新增表/索引/unique surface。v54/v32 增加 `retrieval_experiences`（Agentic Memory P2 检索策略经验库）：**部署级全局**表，无 `notebook_id`、无 owner 列、无外键——存的是「怎么查」的通用打法而不是任何人的内容，深拷贝结构上够不着它（与 `groups`/`group_members` 同一句论证），`merge_dbs.py` 归全局并集表。单列内容寻址 `TEXT` 主键（情境指纹+动作的确定性哈希）逐字等于声明 replication key，自动落 `REPLICATION_KEY` 停车，无哨兵列、无索引。不新增索引/约束。正向 shadow 不变量随之变为 81 张业务表、110 个 unique surface，分支计数闭包上界仍为 12 个 row slot。v55/v33 增加 `agent_observations`（Agentic Memory P3 观察队列）与 `user_profiles.search_profile_json`（每用户检索/回答风格偏好，可空列）：前者叶表，一个出向 FK 到 `notebooks`、无入向 FK，幂等唯一索引 `idx_agent_observations_request`（`(notebook_id, owner_id, agent_profile_id, client_request_id) WHERE client_request_id IS NOT NULL`）与 `idx_conversations_share_token` 同款走 NULL 停车，停车列必须是 `client_request_id`——`notebook_id` 带 FK（SENTINEL_TEXT 排除），`owner_id`/`agent_profile_id` 是隐私/作用域列且无四值精确匹配消费保护；另建一条非唯一索引 `idx_agent_observations_scope`（`notebook_id, owner_id, created_at, id`）支撑环形淘汰删除与有界读取（100k 行实测：淘汰 9.5ms→1.1ms、读 3.2ms→0.07ms），不进 unique surface；`id` 为 `NOT NULL`（`TEXT PRIMARY KEY` 在 SQLite 不隐含 NOT NULL，一行 NULL id 会让环形淘汰的 `NOT IN (SELECT ... LIMIT N)` 对每行判 NULL、DELETE 静默变 no-op）；`created_at` 只允许 ISO 时间戳、绝不写空串，刻意不进 `POSTGRES_EMPTY_TIME_SENTINELS`（与 `decided_at` 同一条教训）；深拷贝不复制（同 `agent_notebook_profile`/`agent_profile_jobs` 先例，观察是过程状态不是知识）；`merge_dbs.py` 按 notebook 归属并入 `NOTEBOOK_SCOPED_TABLES`（不是全局表，与 `retrieval_experiences` 明确区分）。后者不进唯一面/FK/`POSTGRES_JSON_COLUMNS`/`POSTGRES_EMPTY_TIME_SENTINELS`，`NULL`＝尚未设置，读路径回落「未设置」（同 v45 `ui_mode` 先例）。正向 shadow 不变量随之变为 **82 张业务表、112 个 unique surface**（新表声明的 PK 加它唯一那条部分索引），分支计数闭包上界仍为 12 个 row slot——新表只带一条出向 FK，不抬高闭包。v58/v36 是可插拔索引管线的列级迁移：`notebooks` 加 desired 选择 `indexing_pipeline`（可空，NULL＝内建）与 `indexing_pipeline_version`/`indexing_pipeline_generation`/`indexing_pipeline_job_id` 三个 `NOT NULL DEFAULT` 授权列，`unified_kg_state` 与 `extraction_runs` 各加 `(indexing_pipeline_id, indexing_pipeline_version)` 已发布产物身份两列；只加列、不加表/FK/唯一面。v59/v37 增加 durable staging 两张表 `indexing_pipeline_stages`／`indexing_pipeline_stage_sources`——manifest 里是 **`TableClass.LOCAL_EPHEMERAL`**：存在于两侧配对 schema、参与 totality 校验（应用表 totality 因此为 84＝82 复制＋2 本地暂存），但正向 shadow 的 COPY/capture/apply 一律不碰。不复制的理由有两条：stage 行是**单个后端本地 worker 的租约状态**，跨后端切换重放它等于把别人的在飞任务搬过去；且它们对 `kg_build_jobs`/`notebooks`/`sources` 的入向 FK 若进入停车分析，会让 `kg_build_jobs` 既有的单 running 唯一面不可停车——目标侧这两张表恒空（没有复制写者），排除后 delete/reinsert 停车仍安全（`_build_unique_surfaces` 已按 `spec` 成员资格跳过 ephemeral 表的约束/索引/入向 FK）。**复制面不变量因此保持 82 张业务表、113 个 unique surface、12 个 row slot 不变。**
- **群组邀请 schema**：v57/v35 在 `groups` 根行增加可空的 `invite_token`、`invite_created_at`、`invite_created_by` 与部分唯一索引 `idx_groups_invite_token WHERE invite_token IS NOT NULL`。raw token 留在根行是为了让有权管理员重新打开同一条链接；换新、撤销和删组都原子使旧链接失效。`invite_created_at` 只能是 SQL NULL 或 ISO 时刻，绝不能写空串。本迁移不加表、不加外键；当前正向 shadow 不变量为 82 张业务表、113 个 unique surface、12 个 row slot。
- **来源事实回填**：`knowledge_source_facts.projection_origin` 是区分在线事实与历史投影的唯一依据，禁止从模型生成的 `local_object_id` 前缀推断；当前代次的在线事实即使对应全局对象已删除也必须独立计数。`knowledge_source_fact_backfills.incomplete_reason` 保留稳定投影缺口，`failure_code` 只记独立运维失败，失败后续跑不得用后者覆盖前者。
- **界面词汇**：面向用户的文案只用「界面词」，不得出现 `projection`/`tier`/`canonical`/`chunk`/`KG`/`schema` 这类内部黑话。真源是 `AGENTS.md`「界面词汇表」，`scripts/check_ui_vocabulary.py` 是硬门。**唯一放行的英文界面词是「图谱 Schema」**（图谱对象类型/字段管理，原「内容类型」，现从知识图谱视图头部进入）——守卫的 `SANCTIONED_UI` 只放行这一个复合短语（带 CJK 前置断言，不吞「知识图谱」尾字），裸 `schema`/`Schema` 仍拦。
- **图谱类型覆盖**：管理员维护的全局类型是所有笔记本的默认基线；笔记本 owner 可在当前笔记本内 copy-on-write 改写继承类型、局部停用或新建本库类型，删除覆盖表示恢复继承，只读成员只能查看。全局写接口仍只认管理员，笔记本写接口仍走 owner 守卫；归纳候选按笔记本/创建者隔离，候选态只供审核、批准前绝不能遮蔽有效定义。全局/本地写入跨两张注册表串行并复核冲突，模型归纳返回后必须在落库事务内重查；合库预检遇到同名且语义不同的全局定义必须失败，不能静默取任一侧。Ask 知识块、知识浏览/类型标签必须读取同一份 notebook 有效注册表，不能回退到裸全局表；来源的核心图谱抽取仍由独立的四类边契约约束，本注册表变更不重写历史对象也不扩张该抽取器。存在该类型知识对象时不得删除 notebook-only 定义。
- **错误文案**：deny by default，信任按**出处**判定而非文本形状。后端中文用户文案必须走 `backend/app/api/deps.py` 的 `user_error()`（打 `X-User-Message` 头），前端翻译只在 `frontend/app/errors.ts`。
- **Knowhow 导入校验**：属性按行/按列必须在解析前按用户选择统一；横向合并记录分组导致展开后首行重复时，应提示改选「属性按行」。只有 `GridParseError` / `KnowhowImportValidationError` 的固定可操作文案可经 `user_error()` 上屏，其他 `ValueError` 保持诊断专用。
- **`object_type` 标签**：后端 `OBJECT_TYPE_LABELS` 与前端 `KG_TYPE_LABELS` 必须逐字一致，改一侧就要改另一侧。
- **架构守卫**是语义化的（`{path, scope, kind, target}`），**不含行号**——仓库里任何提到「行号钉死」的注释都是过时残留。重生成走 `--rebaseline-surface` / `--rebaseline-callers`；新端点必须跑默认模式的 `scripts/generate_repository_contract_fixtures.py` 刷 `api_contract`。
- **模块化扩展底座与 Phase 1 retrieval host**：稳定跨层值只放 `backend/app/domain`，该包不得 import services/repositories/API/infrastructure/extensions/features；`repositories/ports.py` 不得 import `app.services.*`，backend 静态 import 图必须保持 0 SCC，repositories→services 债务上限只许降。`extension_sdk` 定义低依赖 manifest、四类 contribution（Provider / ProviderChain / Contributor / Observer）、point-specific retrieval contract 与脱敏 failure；capability `requires` 与插件 `depends_on` 分开。`extensions` 拥有启动冻结 registry、capability 判定目录和共享 `RetrievalContributorHost`，唯一外层组合根是 `app.bootstrap`；workflow 只依赖 `app.domain.extensions.RetrievalContributorHostPort`，不得把 registry/SDK 当 service locator。required capability freeze 时只查判定入口，availability 每次实时算；probe 只拿无 I/O metadata/cancellation，至少一项 available 后才构造执行 context，插件只获 manifest 已声明且实时可用的窄端口。一个 capability 不可用时只关闭对应 contribution，workflow 仍须进入共享扩展点并保留其他 contributor 输出。invocation 路由与 core-owned admission policy 启动时快照，implementation 后改字段无效。无适用 contributor 时，在 context/cancellation/availability/clock/event/fan-out/I/O 前原样返回 baseline。proposal 必须先经 deployment cap，再做一次 core-owned batch hydrate；插件 value 不具权威性，畸形字段 fail-open，禁止 per-hit N+1。Point-specific proposal source 与通用权威 reader 是两个 domain port：内建权威证据只读请求内存，其他未解析 proposal 才走一次带 notebook/source SQL 上限的批量读取；强合同 lane 使用 atomic admission，一项不合格整批不接纳。Selected-source graph 与 generated-question recall 是前两个生产内建 contributor，分别只消费 `selected_evidence` 与 `chunk_candidates`。插件只拿请求级 access port，绝不拿 baseline、query、graph service、settings、model client、repository、matrix 或 connection。Graph bridge 保留原 activation result，故 attestation、rollout、scope drift、baseline duplicate-support overlay、独立 token budget、baseline manifest/eviction guard、status 与整段失败语义逐字等价，Ask/Report 不得再直接调用 activation service。Generated-question bridge 固定在 MMR/fusion 前，在隔离 baseline copy 上暂存 collision support，使用请求内存做权威复验且不新增 SQL，只在 host 完整接纳后提交；`off`/trigger 已满足/空索引/越界/失败原样返回 baseline，`shadow` 只评估不改输出，`on` 只能在 baseline 后追加原 chunk。question scan 必须在 `LIMIT` 前应用 notebook/source 与 retrieval-run actor 的私有 Memory 谓词，actor 是 core-only run state 且不得进入 telemetry。SQLite/PostgreSQL 都提供无 I/O 的当前执行上下文连接探针；持有 SQLite transaction boundary 或 PostgreSQL pool lease 时，Host 必须在 contributor fan-out 前阻断，并保留 pool-size-1 conformance。核心取消在单/多 query 路径都传播，插件失败/超时才 fail-open。工作流若已知本次调用无法提供某个 point-specific access capability（特性未配置），可经 host 只读启动冻结快照（不碰请求状态、时钟、I/O 与 capability 判定）的 `has_contributions` 查询提前判定该扩展点是否已休眠，并把该 capability 作为 `disabled_capabilities` 声明给 `run`；休眠时在 call/context 构造前原样返回 baseline。两条生产 lane（`selected_evidence` 与 `chunk_candidates`）现在都在各自的 disabled 分支这样做——`selected_evidence_lane_is_dormant`（`source_graph_activation.py`）与镜像它的 `generated_question_lane_is_dormant`（`generated_question_contribution.py`）分别在 `_activate_selected_source_graph` 与 `_run_chunk_candidate_contributors` 里、构造 `*ContributionCall`/`*_call_context` 之前调用。被 `disabled_capabilities` 过滤掉的 contribution 不发 unavailable 事件——「部署未配置该特性」是调用方事实而非一次实时判定失败。架构守卫还禁止插件实现 import 具体 repository/facade/runtime。公开 facade 面只可收缩；同一守卫还检查 `app.core`/`app.models` 对 `app.services` 的反向依赖——allowlist 当前为空，任何 core/models→services 边都是违规（历史上仅有的两条边已分别修复：`app.core.llm` 的取消原语搬进 `app.domain.cancellation`，`app.models.agent_profile` 的 `PROFILE_LABEL_ORDER` 常量搬进 `app.models.agent_profile` 自身，`app.services.agent_profile_block` 改为正向 import）——并把一批热点函数的行数钉在零余量上限上（只许降、降了必须同步 baseline），`backend/app/repositories/ports.py` 的 Protocol 方法总数同样钉在零余量上限上（同一条只许降的规则）。每个模块化架构 PR 两路独立 subagent review、CI 全绿后 squash merge。
- **部署插件（`EXTENSIONS_CONFIG`）**：只从该变量点名指向的一份 TOML 文件、且未标 `enabled=false` 的条目装载，绝不扫描目录/读 entry points/看第二个环境变量。`trust` 是三值类型（`builtin`/`deployment`/`isolated`），但启动 registry 只接受 `builtin` 与 `deployment`——`isolated` 是留给未来进程隔离档的保留值，当前一律拒绝。bundle 只能为自己 manifest 的 `provides` 声明的 capability 名供 probe，与 core 的 `point:name` capability 同名即启动失败。插件自己的 HTTP 路由（即它的 API extensions）只挂在 `/api/extensions/{plugin_id}` 之下、经 router 级会话认证；**插件触达的每个 core 端口都自己对当前请求用户做授权判定**（挂载处的 `{notebook_id}` 路径形状守卫只是纵深防御，不是授权边界本身），插件拿不到 repository、全局 `Settings`、model client、FastMCP host 或原始 bearer token。拒绝日志只含 plugin id 与稳定 reason code，绝不含 settings 值、模块路径、文件路径或异常文本。`examples/extensions/arxiv-search/` 是这一档的一份完整**出厂关闭**样板，验证分三层，任何一句「不进任何测试泳道」单独拿出来都不成立：**插件包本身**（`examples/extensions/arxiv-search/` 目录下的文件）不被任何默认部署或默认前端构建装载/同步；它的**源码级测试与守卫**（`backend/tests/test_arxiv_sample_plugin*.py` 加 `frontend/tests/{unit,guards}` 下的两份）随 **G1** 每次 PR 照常跑，与仓库里其它测试同待遇；**同步进树后的整链验证**——真同步、非空的 `frontend/features/ui-plugins`、以及只有插件被同步进去才有东西可扫的守卫套件——由 **G2** 的 `scripts/check_sample_plugin.sh` 泳道承担。它自己的数值上限与已登记局限归它自己的 README 对（**绝不**进 `docs/product-and-api*.md`），`test_arxiv_sample_plugin_e2e.py::test_the_package_runs_from_outside_the_repository` 是零补丁承诺的机器半；它刻意把测试放在 `backend/tests/` 而不是包里（偏离 SOP §5.3），因为后端泳道只收集 `backend/tests`。
- **Ask reasoning application stages 与完成后扩展**：`backend/app/application` 只持低依赖、不可变 stage envelope；当前 `app` import allowlist 精确到 `app.application`、`app.core.ask_retrieval_policy`、`app.domain.cancellation`、`app.models.ask`，裸 `import app` 与无别名的 `import app.<allowed-module>` 都会绑定 root package、因此同样禁止；显式 alias 的 allowed submodule 可用。新增合同模块必须显式扩守卫，不能直接开放一个可能反向依赖 service 的整包。严禁依赖 services/repositories/SDK/extensions/features/API/bootstrap/main/migration/eval/scripts。Reasoning Ask 显式交接 prepared input、retrieval evidence、response draft 与 committed answer；其中 **response draft 由可注入的 `ResponseDraftStage` 产出**（seam 入口 `execute_response_draft_stage`，输出类型不符即 `StageBoundaryError`），默认实现 `DefaultResponseDraftStage` 就是既有内联的合成/绑定逻辑逐字搬出——它只收一份冻结的 `ResponseDraftInput`（source-graph 激活与检索 fail-open 降级**之后**的证据，加上检索前就已定下的披露事实：memory hits、结构化 Knowhow 批、两个 completeness/kg 旗标），只欠一份 `ReasoningResponseDraft`，且**经 envelope** 拿不到 repository/model client/settings/persistence 座位——出厂默认实现仍经 `self` 使用 AskService 自己的座位（它本就是该服务的方法），注入实现只得到 envelope + runtime，且两者都只在编排器紧邻进入 seam 之前复核过权威后才被放行。取消在 seam 前（合成的第一次 schema 读之前）与提交边界各检查一次；提交边界按 prepared 复核 mode 与身份元数据（`notebook_id`/`question`/`conversation_id`/`user_id`/`job_id`/`asked_at`，与 stage 拿到的同一份冻结 `PreparedReasoningAsk` 逐字段比对，不一致即 `StageBoundaryError`，防止注入实现把答案悄悄写进另一个 job/conversation/notebook），`model_errors` 由 core 在 stage 返回后、检索 ContextVar reset 之前从请求局部报警 sink 统一填充，stage 不必也不应自己写它。冻结的响应 envelope 独占传递 typed `AskResponse` 对象图，不做 JSON round-trip/deepcopy，因为 collection card 与 citation list 刻意共享 citation 实例。point-specific runtime 必须一路携带同一个 source scope、`ask_reasoning` request-local retrieval run（共享 leaf-I/O semaphore 的所有者）、取消 token、trace sink 和 AskService 注入的同一个无 I/O 连接探针；AskService 与 typed retrieval seam 都独立校验 run kind/非空 actor，并绑定 scope notebook、run cancellation、run actor 与非空 stage notebook/取消权威/持久化用户。权威被替换或跨 stage 持有数据库连接时必须抛 `StageBoundaryError`，不得被检索 fail-open 吞成空候选。持久化前保留最后一次取消检查，原子 save 完成后终态胜出。Stage wrapper 不得取得外层 fan-out slot，只有既有 KG/chunk/element/PPR leaf 保留原槽位；Report 仍使用可变 `ReasoningResult` 工作副本。流式 Ask 只在持久答案、job 终态、取消注销和浏览器 `final` 事件之后进入启动冻结的 `ask.completed_observer` host。三个内建 observer 保持 agent-profile → retrieval-experience（仅 reasoning）→ search-profile 的既有串行顺序和 I/O，分别只获 notebook+actor / 零身份 / actor；每条独立 fail-open、普通失败不能反转 `done`，stream sentinel 仍在其后，终态后不再读取请求取消。同步 POST Ask、MCP Ask 与 direct compatibility 路径保持原通知口径；workflow 只能 import domain host port，不得 import SDK/registry/concrete host。公开 facade 面只可收缩；退役后必须显式从 `scripts/architecture_boundary_baseline.json::facade_public_surface.allowed_names` 删掉名字，否则守卫不报错、只是账本失真。Reasoning Ask 显式交接 prepared input、retrieval evidence、response draft 与 committed answer；冻结的响应 envelope 独占传递 typed `AskResponse` 对象图，不做 JSON round-trip/deepcopy，因为 collection card 与 citation list 刻意共享 citation 实例。
- **Ask 完成后预算**：`ask.completed_observer` 使用部署配置的协作式 deadline；已经开始的同步 callback 安全完成，deadline 后不再启动后续 contribution。预算只能保护 final 之后的 hook，不能改写已持久答案、引用、检索结果或 job 终态。
- **Deep Report application stages 与终态扩展**：planning、generation、core final audit 通过 `backend/app/application/report_pipeline.py` 的不可变 envelope 交接；planning/generation 各持独立的新 retrieval run，stage wrapper 只验证同一 scope、run kind/correlation/actor/cancellation、注入连接探针及无连接边界，不取得外层 leaf slot，也不 JSON/deepcopy 大 evidence 图。all-section retrieval barrier、一次 report-wide synthesis、并行 drafting、final editor、claim ledger、citation remap、整篇图片 batch、零正文失败和 retry 仍由 core 按原顺序拥有；final editor/audit 不得改写章节 Markdown。只有 SQLite/PostgreSQL 同一条 `generating → done` 原子 CAS 成功才产生 `CommittedReport`；取消或旧 worker 输掉 CAS 时 hook 必须为零。`report.completed_observer` 只在 done、generation gate 与 scope/retrieval/model 上下文释放后执行；取消注册保持活动到该 hook 完成后才按 identity 注销，以保留既有 active-job 窗口；observer 不能改正文／引用／参考文献／终态，内建 observer 仅通过 opaque at-most-once access 保留 agent-profile 完成信号。Report 使用独立部署 deadline，hook 不得取得 core 连接、启动 retrieval/model 或反转持久终态。
- **通用 admission 读取边界**：Report 的 fallback chunk read 必须取得共享 retrieval leaf gate；即使兼容调用没有冻结 source scope，SQL 也必须按 actor 过滤私有 Memory source，同时允许可见来源与 notebook-wide Knowhow。Graph 的请求内存权威复验不得因此多占 slot。
- **knowhow 变更历史**（`knowhow_changes`/`knowhow_milestones`，schema v26）：knowhow 表的**每条写路径**必须在写事务**最后一步**经模块级 `record_change` 追加一条流水（存 before/after + 变更后整表指纹，复用传输守卫的 `_FINGERPRINT_SQL`）；`backend/tests/test_knowhow_history_coverage_guard.py` 是硬门，对 SQLite 与 PostgreSQL 两份 `KnowhowStore` **各扫一遍**，新增写方法漏挂就报红（判据是 `record_change` 落在写事务 `with` 块**体内**——挪到块外就丢了原子性、照样红；不钉的是它在块内的位置，「挂在最后一步」这半条仍靠评审）。回退是逆序 delta 重放 + 前后置指纹守卫（行/列复用原 id 保引用与代码附件）；里程碑创建与 `create_milestone` 的复检必须在 `BEGIN IMMEDIATE` 之内，清理只删最老连续前缀且永留 head。完整契约见 `AGENTS.md`「Architecture Baseline」的 knowhow 历史条目与 `architecture.md` § 3.7。
- **Knowhow 批量规整/审计/列宽**：候选生成并发固定为 `min(3, knowhow_reformat 实时服务容量)`，状态失败回退 2；相同 `(column_id, trimmed 原文)` 只允许 single-flight，只有成功且未 stale 的结果可复用，取消后停止调度并丢弃迟到 epoch。保存仍按既有完整物理/共享单元串行，必须保留 frozen snapshot、`expected_before`、anchor/精确成员守卫、409 stale 和关闭弹窗后 reload。diff 在同一弹窗做有界 Markdown 行级+token 级比较，超长降级；若批次已观察 stale，打开已保存项必须先关闭弹窗、等待带 epoch 守卫的父表 reload 完成，再按新 detail 重算并打开 `position,id` 最小代表格，禁止闪开旧目标；reload/epoch 失败或行/列消失时不得打开，并走现有可恢复的表操作错误横幅。整体确认不变成逐项 accept/reject。主表只从有界可见行样本 memoize `colgroup` 宽度，且必须在 Markdown 正则/分行/grapheme 之前截断固定 code-unit 前缀，保留 fixed layout、横向滚动与 sticky 首列。owner/权限/`created_by` 继续用稳定 user id；普通 session 审计 label 统一 username→display_name→id，Agent 用 profile_name，复制/导入/转移必须拆 identity id 与 actor label；历史 user-id 只在读时有界批量解析且同步 identity 查询必须在线程池。`origin=agent` 的 change 只解析 `payload.before` 中被替换的旧人类 updater，actor 与 `after/current` Agent label 原样保留。禁止 N+1、破坏性迁移或为显示重写参与 fingerprint 的 `knowhow_cell_code.updated_by`；单格代码 GET/PUT 响应必须返回可读 `updated_by`，保证 session 保存后立即显示来源。
- **knowhow 知识对象检索**：`KNOWHOW_KG_NODE_RETRIEVAL_ENABLED` 默认 true；格子对象进入 reasoning/graph 的节点检索并保留行跳转引用，false 只回滚该直接节点路径，格子 chunk 仍可搜索。动态类型必须按 `knowhow_tables.hidden_source_id` 收窄；chunk-vector→KO 旁挂版本同时覆盖图变更序号和 Knowhow 向量代次（纯向量修复不 bump KG），并随 KG 变更显式失效。
- **图谱质量分析视图预计算**（`kg_community_edges`/`kg_source_profiles`/`kg_analysis_artifacts`，schema v36）：三张产物表在 `rebuild_communities` 里顺带产出，并与**板块划分本身 + `community_seq` 戳**一起在**同一个**写事务里发布——绝不能拆成「先提交板块、再提交产物」，那会在生产上留下分钟级的「新板块 + 旧账本」窗口；但产物的计算必须整个待在那个事务**之外**（写锁是进程级的，全表扫进事务就锁死全库写入）。因此来源画像的 SQL 不 join `community_members`（那一刻板块还没落库），canonical→板块 由内存 membership 完成，结果逐字等价，在线端点（`GET /notebooks/{id}/kg-analysis[/sources]`，只需 notebook 读权限）只读产物、不做全表扫。**预计算有自己的新鲜度闸**，判据是「账本缺失或账本 seq ≠ 当前 `community_seq`」，绝不能跟社区图共用一个闸——已经 rebuild 过的库 `community_seq == kg_mutation_seq` 早已成立，共用闸会让账本永远短路成空、只能靠 `force=True` 恢复。`kg_source_profiles` 是唯一允许合法缺席的一份（零板块库写一张全 0 行的表等于撒谎，应让它缺席）；`kg_community_edges` 有硬行数上限，截断必须显式记账（`edges`/`edges_total`/`truncated`），不静默丢行——但那个上限只管**落库多少**，聚合阶段的有界性另由「边消费边释放」保证（折叠用 `popitem` 就地搬空输入边图，不得遍历后保留：两份同量级结构同时驻留就是生产库上的 OOM）。收敛率必须**按对象类型分列**返回，四类混算会把 concept 真实收敛率稀释约 3 倍。**新鲜度有两条世代线**：`kg_mutation_seq` + 依赖合并的四份另盖 `built_at_cluster_seq`；其中依赖板块的两份盖的是**板块划分**建在哪一代合并结果上，只补账本那一轮显式记 `null`（划分是库里现成的、建在哪一代无处可查），于是 `stale` 是三值的，`null` 绝不能被显示成「与当前一致」。完整契约见 `AGENTS.md` MVP Scope 与 `docs/superpowers/specs/2026-07-25-kg-analysis-view-design.md`。
- **数据库后端选择**：DATABASE_URL selects the formal repository backend through one repository factory. Exactly one active repository backend is selected centrally from `DATABASE_URL`. SQLite and PostgreSQL are both available direct backends; SQLite remains the shipped default. 选择只存在于 factory；service/store 不判断 dialect，也不 import 对侧 adapter。`SHADOW_DATABASE_URL` 单独设置仍不参与选择/同步；SQLite59/PG37/epoch1 临时 shadow 边界已有 preflight/control/guard、run-bound 原子 snapshot、有界可续跑 baseline COPY/H0，以及 fail-stop 单消费者正向 apply 原语。正向引擎从 checkpoint+1 连续读全局 SQLite seq，在短只读 snapshot 中仅为 upsert hydration 当前行，delete 保持 key-only、hydrated bytes 为零，并显式保留七张表的 rowid ordinal；同一 stable key 在 accepted prefix 内保留最后 event 并按全局最后 seq 排序，raw seq/checkpoint 仍连续，每个 identity 的最终 actual apply 覆盖 synthetic dependency contribution，只有 dependency-only identity 才计一次 synthetic 行与 bytes；短读窗口若在 allocated high-water 前结束，会在 hydration/apply 前立即判为 suffix gap；满窗口低于 high-water 时在同一 snapshot 探测相邻 seq，缺失即失败；批次硬上限 4096 events/64 MiB；仅一个 final bundle 可独占超限，同 key replacement 若在已有其他 actual bundle 时使 bytes 超限则回滚并留到下一批。FK 父闭包只读同一已验证 source snapshot，每事件最多 64 行；固定 v37 复制面图按 FK constraint branch 计数的上界仍为 12 个 row slots（群组四表与 Agent 库理解两张表都是浅层，v49/v27、v50/v28 与 v51/v29 均未抬高它；v52/v30 只给 `conversations` 加列、v53/v31 只给 `agent_profile_jobs` 加一列，都不加表也不加外键，v54/v32 新增的 `retrieval_experiences` 是无入向/出向 FK 的叶表，v55/v33 新增的 `agent_observations` 只带一条出向 FK 到 `notebooks`（无入向 FK，仍是叶表）而 `user_profiles.search_profile_json` 只加列不加 FK，v56/v34 仅新增无 FK 的 `groups.owner_id` 文本指针，v57/v35 只在同一根行增加邀请列与唯一索引、不加 FK，v58/v36 只加列，v59/v37 的两张 staging 表是 LOCAL_EPHEMERAL、不进复制面与停车分析，这些变更同样未抬高），依赖行计入 bytes 且批内去重，不扫描 suffix log。PG 仅延后 FK/UNIQUE ordering SQLSTATE；CHECK/NOT NULL 立即 poison。精确 PG37 catalog 派生的 113 个复制面 unique surface（LOCAL_EPHEMERAL 表的约束不参与）均有静态停车方案：nullable 列用 NULL（`idx_conversations_share_token` 也走 NULL 停车，与 `notebooks`/`reports` 的 share_token 索引同款）；无 FK/CHECK 的 text/bigint 列使用按其他唯一列的非 NULL 等值/NULL `IS NULL` 和固定 predicate 限定范围的确定性候选（`C` collation 文本 max 拼 `chr(1)`，或先走可索引 bigint MIN/MAX 快速路径选择 min−1/max+1，仅在两个 int64 边界都已占用时扫描首个 gap）；仅对无入向 FK 且批内存在 current-final 恢复行的叶表做同事务 delete/reinsert。停车状态以 `(unique surface, row identity)` 为单位，单个 stagnant pass 停车所有可独立停车的冲突，final apply 成功后清除该 identity 的所有停车面。限制为 8 passes、32 actual statements/apply、16384 actual statements 总量；每次候选查询都计入预算，ordering、statement、pass、候选搜索或候选 UPDATE 容量耗尽均 non-poison，而最终窗口仍无法停车的 UNIQUE 冲突按最早实际 seq poison。`run_forever` 从 256 events/8 MiB 自适应倍增到硬上限，仍阻塞则 non-poison。PG apply 事务 claim worker 后、业务 DML 前复查既有 run/direction poison；poison 发布在 binding/checkpoint 校验后锁定检查该方向任意既有记录，完全相同视为 ACK-loss 成功，不同则 stale 且绝不新增第二条；apply、ack-loss 识别和 poison 发布都绑定 snapshot source/target 与 live target identity，snapshot 与业务 apply 前均要求 `progress.applied_seq == checkpoint.last_seq`；事务按 migration→control→run→worker→checkpoint 锁序，锁 ledger+82 表并复核精确 catalog，再同事务提交业务收敛、脱敏 progress 与 checkpoint CAS。`ProgramLimitExceeded`/`DataError` 候选失败按 non-poison capacity 处理，`QueryCanceled` 保持瞬态并整事务有界重试，SQLite path/file binding 失败通过专用 identity 异常分类，不依赖异常文本；已证明的确定性错误只记录实际阻断 seq 的一条脱敏 poison。每个有效 batch 结局恰好记录一条 metric，batch events 使用实际 accepted/observed raw-event 数而非 lag，并尽可能保留 retries；指标不得带表名、key、行值、URL、worker id 或异常文本。禁止持有 SQLite 时等待 PG。显式运维 CLI 负责 preflight/start-forward/status/verify；前台 worker 使用数据库时钟排他 lease、SIGTERM/INT 批次边界，并仅在 FULL 校验、barrier/replay/poison、至少 7 天/100,000 events tail 等边界之后保守清理。这只是 SQLite-active 正向 shadow；cutover、反向复制和自动 `DATABASE_URL` 交换仍未实现。PG 向量存 `bytea`，不需要 pgvector，生产仍固定 `--workers 1`。
- **Shadow 校验**：在 SQLite 只读 snapshot 记录 `Hv` 并流式落入 owner-private 临时 spool，释放 SQLite 后再等 PG checkpoint；以 `REPEATABLE READ, READ ONLY` 固定 `Ht`，再用新 SQLite 事务扫描 `(Hv,Hseen]` 的 retained dirty key，只排除这些 concurrent key，且 verifier barrier 保持到报告提交。Structural 覆盖精确 schema/guard、stable key/hash、FK/unique/cascade、storage-root 文件引用；Full 增加领域投影、float32 bytes/dimension/norm/抽样 cosine 和固定中英检索门禁；Cutover 复核 write-frozen，并要求 `Hv=Ht=MAX(seq)`、零 concurrent、100% coverage 与前一轮完整 full/cutover。报告不得含原文、Memory、token、密码或 URL；仅同级或更强的 clean run 可 supersede drift。
- **SQLite source open 分类边界**：只在 `open_fresh_live_sqlite` 调用处把非瞬态 `sqlite3.OperationalError` 归为 source-binding identity；locked、busy、interrupted open 仍按瞬态整批重试，后续 SQLite operational error 保持原 schema/query 分类。
- **PostgreSQL 大库词法索引**：大型 live PostgreSQL 通过 `scripts/build_postgres_retrieval_indexes.py` 运维安装 `public.btree_gin` 及 notebook-aware 的 knowledge-object name / chunk text 复合 GIN，禁止塞进事务型启动 migration。工具默认只读检查，逐条 `CREATE INDEX CONCURRENTLY`，中断留下 invalid 工件时可续跑；旧全局索引默认保留，只有两条替代索引都验证后才允许运维方显式删除。词法 SQL 的 notebook/status 谓词、similarity、limit 和排序必须保持不变；PostgreSQL conformance 必须逐项比较候选并证明 planner 能用两条复合索引。
- **纯性能检索等价契约**：增量 KG fusion 已有可用 ANN 时，不得再为互斥的无 ANN brute-force 分支全量 hydrate 既有 concept payload；无 ANN 的 threshold/skip 行为保持不变。生产 scale-PPR hydration 在算出同一全局 min/max 后只以有界内存选配置的 stable Top-K；无界 `scale_ppr()` 诊断 API 继续保留，有限结果必须与原全量稳定排序的精确前缀一致，包括 ties 与全零分数。
- **PostgreSQL KG 名词法 KNN 早停**（`POSTGRES_LEXICAL_KNN_ENABLED` **默认开**，设 false 回滚）：现状 `ORDER BY similarity` 的 LATERAL 无法早停，9.1M 行 base 上常见短词单词项 7.4s、多词项超时、整条词法臂 fail-open 阵亡；GiST `<->` 距离序按相似度增量吐行、到 LIMIT 即停（同库实测 123ms，60×，planner 经 ANALYZE 后自然选中）。**两阶段是证明不是启发**：只走 `%` 臂的 KNN 页取满 = legacy top-k 内不可能有 ILIKE-only 行（其相似度必低于 `%` 阈值），两径至多在等相似度并列类内选不同成员——且 KNN 路径在并列类内**自身不是 run-to-run 稳定的**（并列成员随 GiST 遍历序变化；实测 base 有 285 行同名 "DAC" 的 sim=1.0 类），召回 A/B 必须多次采样。这是**登记接受的取舍**（2026-08-06 拍板：60× 收益优先，默认开；缺索引的部署零行为差异，需要位稳定候选集的部署用开关回滚）；页不满**没有成本承诺**（`%` 臂短说明不了 ILIKE 臂基数），phase 2 就是 legacy 的既有代价，词项用 legacy 语句整体重跑并**替换**（不是并集），rank 重映射必须保留。路由三条件缺一即回 legacy 且 SQL 逐字不变：开关开、run 未按来源收窄（受限 run 的词法是唯一候选来源且 scoped 形状无 bench）、库规模 ≥ `POSTGRES_LEXICAL_KNN_MIN_ROWS`（默认 50 万 nodes+chunks——GiST 索引没有 notebook 键，KNN 是**全库**距离序逐行过滤，只有主导份额的库才划算；判据**不能**用 `copyable`，5001 行就过那道闸，小份额库会在别人的行里翻找自己的候选、丢掉复合 GIN 快路径）。判据在 `_lexical_knn_allowed` 一个可测 helper 里，且必须在调用方**打开连接之前**算（它读 copy-stats 的版本信号、自己要开连接；持连接时算 = 嵌套获取坐死连接池，`_lexical_corpus_langs` 是同一约定的先例），异常按 False 收；SQLite 适配器收下 `allow_knn` 并忽略（签名对等，service 不判 dialect），且经 `lexical_knn_capable = False` **能力声明**让判定在读任何规模统计之前零成本短路——问「绑定的适配器能不能」不是判 dialect。启用还需一个**精确形状匹配**的 GiST 索引（`gist_trgm_ops` 单键、键定义**等值**比对、`indcollation` 必须是 `pg_catalog."C"`——教科书写法漏掉 COLLATE 会让 planner 拒用而探测误报可用 = 每词项一次无索引距离排序的静默回归；partial 或全量皆可）——按形状而非名字探测（生产 bench 索引可直接转正），缺索引静默走 legacy；探测按 (dbname, 表 OID) 进程级缓存**且不失效**，回滚顺序因此是先关开关重启、后删索引。chunk 正文与精确通道**刻意不接**：整段文本对短词的 trgm 签名是另一个长度域，无 bench 不搭车。
- **Shadow baseline 安全边界**：snapshot 目录必须 owner-only 且不可为 symlink；snapshot/live fence 必须 fresh 打开 `SqliteDatabase.db_path` 当前文件，禁止复用线程缓存连接，并在 open/transaction 前后及 snapshot 发布/PG commit 前复核 resolved path + `(st_dev, st_ino)`；这是合作式运维边界，检查窗外替换文件不受支持且下一次检查 fail closed。COPY 的所有业务 SQL 全限定到 run 绑定 schema，起始绑定、每个提交批次/完成点和最终 H0 前均在短 `BEGIN IMMEDIATE` 中复核 live capture 仍启用，且不得把该 SQLite 栅栏跨 PG prefix proof/`ANALYZE` 持有。JSONB prefix proof 只在 JSON 子树内统一有限 int/float/Decimal 的精确十进制语义（bool 排除、负零归零），普通 SQL 数值列仍保持类型差异。Resume 使用有界 named server cursor，PG statement 有 timeout，取消在 proof/migration/analysis 间轮询；起始/最终完整验证由 checksummed packaged migrations 派生并覆盖 v32 table/column/PK/FK/unique/check、operational+GIN index 与 `public.pg_trgm`，逐批路径不得反复扫描 81 表 catalog。
- **最终 H0 lease**：最终 SQLite fence 不是瞬时检查；只能在 PG 双锁/run/table lock 与 81 表长 proof/`ANALYZE` 全部完成后取得，必须保持到 PG H0 checkpoint + run progress 事务实际 commit 成功再释放。PG 事务或 commit 失败时 PG 不落 H0，并释放 SQLite；持 fence 时不得再等 PG pool/advisory lock 或执行长 PG 工作。
- **切换与批处理边界**：只改 `DATABASE_URL` 不会复制、迁移或同步既有数据；停机 importer/activation 使用 `scripts/migrate_sqlite_to_postgres.py`，SQLite-active 连续 shadow 使用 `scripts/shadow_sqlite_to_postgres.py`，两者不得共用 target。停机 importer 只排除 SQLite-only 的 `shadow_capture_control` / `shadow_change_log` 运维表并把排除写入 receipt；退役用户数据表仍要求为空。正式切换必须停写、停服务、验证备份、只改一个 URL、启动后校验 readiness/认证/数量/代表性读取再放流量。`batch_ingest` 除 `vectors-to-blob` 外的 mutation phases（包括 `backfill-source-facts`）同时支持 SQLite/PostgreSQL；该回填只允许显式离线运行，绝不从交互请求触发。PostgreSQL 直连维护必须在构造 repository 前要求 `--confirm-service-stopped`，再由独立非池化 session 持有数据库级 advisory lock，结束时释放锁并关闭 repository。该 flag 只是运维确认，不会停服务；生产 wrapper 共用同一安全 opener。`vectors-to-blob` 因 PostgreSQL 已用 `bytea` 而仅适用于 SQLite。`kg` 的完整、`--limit` 与 `--retry-partial` 抽取都必须复用页面分析的 durable `kg_build_jobs` 单飞/探活/熔断/排空协议，批量末尾聚类与索引在 job 成功前完成；页面继续分析会自动修复 partial，CLI 则用显式 `--retry-partial`。修复时模型窗口未全成功或新结果为空保留旧图，仅“零失败窗口且非空”的结果事务替换。显式 `--workers` 必须在 repository 构造前写入本进程 `kg_job_concurrency`，不得只打印而不生效。`scripts/check.sh` 保持离线，PostgreSQL 16 只走独立本地/CI integration lane。
- **离线批处理大库边界**：完整/限量 KG、metadata、reextract、向量、关系和反向索引的 PostgreSQL inventory 都必须用 `C` collation 的有界 keyset 页；KG inventory 按原始 source 页推进，不能为凑满 eligible 页无界扫描。所有 `kg` 抽取都保留 durable `kg_build_jobs` 单飞语义，只把 inventory/提交窗口分页；`all`/`reparse` 等非 durable 池化阶段收到 Ctrl-C/SystemExit 时必须在 advisory lock/repository 释放前取消排队任务并排空已接受任务，SIGTERM/SIGHUP 保持默认进程级终止。
- **Knowhow 智能补全空列**：记录型表从行详情、带行标题分组的表从概念矩阵的物理分支显式发起；只有缺失格或精确空串可补，已存纯空白文本不算空。一轮请求把同表最多 8 条参考行（同 anchor/行标题分组优先，再比较已知列相似度与覆盖度）与一次有界 `ReasoningRetriever` 全库检索合并；全库只指当前 notebook + 当前有效显式挂载库。它复用 Ask `reasoning` 的规划/联邦检索/反思/扩展/查询期推导，但补全专用策略会在候选进入模型反思前排除私有 Memory 与当前表自身投影，并关闭来源归属不透明的 PPR/社区扩展与精确标识符通道（同一 `allow_*` 策略位机制——补全的查询是 JSON 信封而非自然语言，否则会让 seed pass 恒定探测信封自身的键名）；绝不能调用 `ask_reasoning`/`ask_answer`、创建对话/job 或保存 Ask 答案。响应必须带最终推理轨迹和服务端生成的库内证据 key；模型只能引用合法 evidence key 或同表行 id，无合法引用的建议强制 abstain。`reasoning_agent` 负责检索，`knowhow_complete` 负责结构化合成；两阶段都用 system 级不可信证据指令。推理响应畸形、任一 provider 未配置/执行失败，或合成响应不可解析/顶层结构不可用时显式失败；单条建议畸形则过滤、降级或转成 abstain，任何情况都不能静默退成同表补全或伪造离线结果。审阅弹窗分开显示同表参考与禁用链接/图片的库内 Markdown 证据，保持可拖动和逐项人工确认；确认仍走普通 cell PATCH，固定携带 `expected_before=""` 和 `origin="llm_complete"`，并保留正常历史与同步语义。
- **Ask 会话即时入历史**：历史按带亚秒精度的最近活动时间排序（SQLite 还须按绝对时刻处理 UTC offset 变化），不按创建时间。`/ask/stream` 的首个 `started` 事件必须同时带持久化的 `job_id` 与 `conversation_id`；前端当场将它置顶并设为当前会话。即使用户在 `started` 抵达前就切到同库旧会话，新会话仍须发布并可重新打开；历史发布、请求 generation 与终态摘要刷新都跟随当前 notebook，不依赖旧 run 或会话 epoch，同库旧调用要追随最新请求（包括跳过失败的中间请求），其他 notebook 的延迟请求也不能使当前库的有效响应失效。加载 notebook 最近会话或用户选择的会话详情期间，输入框和模式控制必须保持禁用，避免迟到详情接管新 run。若用户在 `started` 前点击中断，界面立即恢复草稿，但该 run 自己的 controller 仍继续读到 `started`、按返回的 job id 调取消端点后才 abort transport，不能先断流而遗失取消句柄，也不能与重连到其他 job 的取消控制串台。
- **Ask 空闲心跳**：`/ask/stream` 的交付队列空闲时每 5 秒发送一条不含业务内容的空白 NDJSON 心跳，并返回 `Cache-Control: no-cache, no-transform` 与 `X-Accel-Buffering: no`；前端继续忽略空行。它只防反向代理 idle timeout，不伪造推理进度、不改变断连/取消语义；代理的总请求时长硬上限仍由部署者调整。
- **模型 JSON 保守修复**：严格 JSON 永远先解析；只有 `reasoning_agent` / `ask_answer` 能走 `MODEL_JSON_REPAIR_MODE=off|shadow|on`（默认 `on`）控制的共享修复层。只修完整对象形态的语法错误（如缺引号/逗号），按 schema example 限制顶层字段与明确 boolean/enum 类型，每个非空字符串值都必须仍逐字出现在原始响应中；截断、数组/标量、未知字段、非有限数、改写字符串一律拒绝。`shadow` 只记 repairability 后仍拒绝；事件只含 workload/support id、稳定状态和稳定 reason。合法 JSON 字节不变，字符串 `"false"` 不能当布尔真，`ReasoningRetriever` 异常必须落为可持久化的终态降级回答，不能再解引用不存在的 result。
- **Ask 提问时间**：以网页端提交瞬间为准并随答案持久化，不能拿服务端答案完成时间代替。悬停问题气泡在下方显示，点击固定，点击其他位置恢复悬停；今天只显示时间，本周其他日期显示星期+时间，超出本周显示日期+时间（日期和星期二选一），今年省略年份、其他年份显示年份。固定显示跨过浏览器本地零点时必须刷新。
- **Ask 回答时间**：以 `answers.created_at` 的答案写入瞬间为权威，通过 `AskResponse.answered_at` 同时进入实时 final 与持久化 payload；旧 payload 重开时从答案行回填，不加 migration。回答卡沿用提问时间的浏览器本地格式、悬停显示、点击固定、外部点击取消与跨本地零点刷新规则。
- **问答引用定位图谱节点**：从知识对象引用打开全屏知识图谱时必须选中并居中目标；目标不在有界高连接度核心图时，按引用真实来源 notebook（包括挂载 base）定向拉取并叠加其一跳邻域，纯 graph-BFS anchor 也必须保留所属 base id。浏览器始终用当前 active notebook 过权限；后端只在它的有效 participant 集内校验/解析对象并内部代理读取，挂载公共 base 不等于获得该 base 的直接成员权限。引用携带的原始 Concept id 通过有界 `cluster_fold_rows` 解析为接口返回的 canonical `focus_id`，同时保留用于 context 的 raw object id，不能拿合成 `K-*` id 查询 `knowledge_objects`；大库 viz 产物不可用时显式返回暂不可定位，不得进入会物化全 notebook cluster map 的 DB fallback。前端保持图谱打开并给出可重试提示，不得挂一个永远消费不了的 raw-id focus。
- **参与库资源的代理读取**：挂载参考库的来源详情、元素与图片资产只能经 active notebook 维度的代理端点读取（`GET /notebooks/{active}/sources/{id}`、`.../sources/{id}/elements`、`GET /notebooks/{active}/assets/{asset_id}`），浏览器绝不直连另一个库。与「问答引用定位图谱节点」同一条合同：先按 active notebook 过读权限，再**只**在它的有效 participant 集内解析目标——资源自己声明所属 notebook，不在集内一律 404（不泄露存在性），参与集每次请求实时判定，被挂库降级/易主/深拷贝中或取消挂载即刻失权。挂载公共 base 仍不等于该 base 的直接成员权限：裸 `GET /sources/{id}` 保持 owner∪member 口径不放宽，写入（重新解析/删除）刻意**不**代理，来源详情弹窗对参考库来源按只读渲染（收起写入按钮 + 标注「来自参考库《名》」，防御性 handler 也要复查）。代理读取的披露面也必须比 owner∪member 端点更窄：详情模型**去掉**（不是运行时置空）`file_path` 与原始 `error_message`（两者都可能带服务端绝对路径），改回 `parse_failed` 布尔；跨库的隐藏合成源（memory/knowhow 投影行，集合地图刻意把它们算进作用域）直接拒绝，绝不让「查看来源」把整条合成源摊开（Memory 按创建者私有）；跨库资产响应必须 `no-store`，否则取消挂载会被浏览器缓存静默架空一整天，与「失效即 404」矛盾。同库资源先短路，不为「一张图一次请求」的主流路径多付一次挂载 join；参与集解析仍只有 `mount_sql.py` 一个定义点。
- **逐步推理档位与完整枚举是两套合同**：`retrieval_effort` 的稳定 id 为 `overview` / `standard`（默认）/ `deep` / `thorough` / `exhaustive`。五档的每查询相关性结果数依次为 `4/8/8/12/16`；最终 `floor/aspect/cap` 为 `8/2/12`、`20/3/36`、`24/4/48`、`32/5/64`、`40/6/96`；最大 `步骤/首轮子查询` 为 `4/2`、`8/5`、`16/6`、`32/8`、`50/10`；KG/原文 prompt 字符为 `4000/12000`、`6000/30000`、`8000/50000`、`12000/80000`、`16000/120000`；直接原文段（公式/表格/图片等）进入最终合成 prompt 的条数上限（`answer_element_items`）依次为 `4/6/8/12/16`，按检索相关度降序择优（tie-break element id）而非插入序，仍占用同一份原文 prompt 预算。同五档还携带集合枚举工具的预算：`enum_page_size` 各档恒为 `50`，`enum_pages_per_run` 依次为 `2/4/6/8/12`，`enum_rows_per_run` 依次为 `100/200/300/400/600`。最终相关性预算按 `min(cap, max(floor, aspect × 实际查询数))` 计算，模型只可提前停，不能突破上限。「首轮子查询」只约束**首轮并发**：超出它的已确认主题种子在步骤预算内顺延执行，预算不足时在轨迹中披露并回喂反思（见第四节）。候选召回不随档位变化，但由部署参数独立控制：`CHUNK_RECALL` 默认 200，分别约束带索引 Chunk/KG 的 ANN 与词法窗；`RELATION_RECALL` 默认 200，分别约束 Relation ANN 与词法关系 ID 总窗，后者内部仍为 source/target 方向预留份额。这些默认值不是请求级硬上限。意图合同用 `ranked` / `complete` / `aggregate` / `hybrid` 区分范围；Knowhow 的显式完整请求必须走稳定游标而非放大 Top-N。五档共用每页 25 行、最多 50 页/1,250 物理行、8 表、每表 8 列、模型单元格摘录 1,000 字符、结构化载荷 256,000 字符、正文内联 100 行、结果卡初始 20 行；只有游标耗尽且表目录、行数、列元数据和所选表范围均稳定才是 `complete=true`。触及任一安全线或并发改表必须返回 `complete=false` + `explicit_partial`，不得声称“全部”；100 行 Knowhow 表可返回 `100/100`，低档位也不得降低显式完整枚举上限。物理行完整枚举目前只覆盖 Knowhow；来源元素（公式/表格/图片/代码块）、知识对象（概念/论断/公式/过程）与库内文档目录另有一套模型显式调用的**集合枚举工具**（`enumerate_elements`/`enumerate_kg_objects` 两个 reflect 动作，加这两个动作上的参数值 `enumerate.collection="sources"` 取库内文档目录；这两个动作没有让模型面动作空间越过 10 个——大纲便签的 `update_outline` 才是随后新增的货真价实的第 11 个动作（见下面「大纲便签与按节合成」条），Agentic Memory P4 的 `consult_memory`（仅 deep 及以上档且经验注入闸开启时提供，见下面「Agent 库理解」条尾的「Agentic Memory P4 additions」段）是第 12 个），其余集合（Memory 等）仍是相关性结果并须披露边界。
- **集合枚举工具**：三要素合同——① 输出有界（游标翻页，命中 `enum_*` 预算即停）；② coverage 由执行器计算，前端徽章直读 `TypedCollectionCoverage`（`returned_total`/`total`/`complete`/`truncated_reason`/`overflow_semantics`），绝不采信模型自报「已全部」；③ 作用域走既有挂载谓词（active notebook + 有效挂载库），与 Ask 联邦同口径。**清单永不含私有 Memory**：确认 Memory 是 owner 私有的（其他通道都按 owner 隔离），而清单按 participant 作用域取数、没有 owner 过滤，共享库里会被别的成员整份读走。排除是**无条件**的（单人库同口径），且**计数与行同一谓词**——元素侧在 `source_change_signal_rows` 里排 `source_type='memory'`（那条查询同时是计划/信号/计数的唯一来源，三处自动一致；三个信号列不在任何索引上，谓词是纯残余过滤），KG 侧靠每参与库一次有界 `memory_source_ids` 在既有过扫描里过滤行、并用 `knowledge_type_count_rows_for_sources` 按同一批 id 从分母里减掉（只过滤行不减分母 = returned≠total = 把完整清单永久判成 `concurrent_change`）；显式 `source_id` 那条绕开计划的路径自己再挡一次。看板计数（`notebook_catalog`）刻意**不**跟着改——它答的是「库里有多少知识」。Knowhow 投影源仍在清单内。集合地图（`[Collections in scope] ...`，≤600 字符）每 run 建一次，注入 plan/reflect 上下文，让模型先看计数再决定值不值得全量列；同一份地图还必须随 `ReasoningResult` 带出、包成带固定表头的服务端小块（无 `[k]`，整块 ≤800 字符）装进**答案合成**上下文最前，并进入合成触发条件——reflect 教模型「太大就别枚举、直接报数」，那个数必须真的到得了写答案的模型手里，否则「大集合 + 零其它证据」时合成压根不跑。行/页/**载荷**三个池全部是 run 级：每次动作只发剩余额度，执行器按 `payload_chars` 回传真实消耗，一轮里的多次枚举累计不得超过 `structured_payload_chars`。载荷闸收**两次**、量两种东西：执行器按紧凑 dataclass 计费（拦「读得比该请求允许产出的多」），`typed_collection_results` 再按 `model_dump_json()` 的真实 wire 形状计费（拦「传/存得比声明的多」——联合体两臂的默认字段 + result 元数据让 wire 明显更宽），并**先预留**每份 result 的信封（那是「被裁过」的披露载体）；裁到的那份降级为 `complete=False`/`truncated_reason="payload"`/`returned_total=实际送达条数`，合成预览必须按 `delivered_outcomes` 的送达视图渲染，否则 prompt 会列出卡片没有的行、块头还写着 complete。合成预览的 `inline_rows` 是 run 级共享额度，必须**分配**而非先到先得（先按 `max(1, inline_rows // n)` 保底，余量按序贪心补），否则第一份清单吃光额度、多集合问题只按一张卡作答。KG 对象翻页是不带状态谓词的纯 keyset 读（索引不含 `status`，写进 SQL 就是无界残余过滤），执行器读回后用同一份 `USABLE_STATUSES` 过滤，每次动作最多过扫描 `max_rows × 4` 原始行，触顶发 `truncated_reason="budget"` 的诚实 partial 并让游标越过已扫的不可用区段（保证续跑推进）。`replace_elements` 必须在**同一写事务**里推进 `sources.updated_at`（双后端同修），让变更信号与元素换代原子同步——首解析 under-count 窗口由此根治，不再是「已登记的低报」。每次动作的页查询数有**强制**上界（元素 `max_rows + max_pages`；知识对象 `参与库数 + max_pages + 原始行过扫描上限`），越界抛 `EnumerationInvariantError` 并 fail-open 成一次 skip；刻意不给分片首页计费（会重新打破宽而薄语料的 complete 可达性）。收尾稳定性复检必须**重新解析参与库集合**（同一个 `resolve_participants` / `mount_sql.py` 谓词入口），集合不等即不稳定，指纹/seq 也用收尾解析出的集合算——只按开场那份 id 列表重算，挂载/卸载参考库这类变化根本看不见。限定单一来源按**标题**（`enumerate.source_title`）表达并由服务端有界精确解析，零命中/多命中一律 skip，计划超过解析上限时直接拒绝解析（不从前缀断言唯一），绝不扩成全库枚举。工具不依赖图谱：无图早退只在作用域**同时**没有任何可枚举集合（元素/知识对象/**来源**三类全为零）时才触发，放行后 `kg_required` 仍如实为 `True`。来源数**计入**这道闸——纯散文库（有文档、零元素、零知识对象）恰是来源清单的主力场景，挡住它等于把闸拆掉、让「库里有哪几篇」拿回一句非答案；零源库因此仍然早退。**来源清单**（`enumerate.collection="sources"`，**不是**第 11 个动作 id：给出它就忽略 enumerate 的其他参数，字段只识别 `"sources"`、其他值落回按动作 id 分派；代价是反思步「下一步意图」仍显示「列元素清单」，真正发生的那步由 enumerate 步 summary 说清）列的是**用户可见来源**那一批——计划 = `source_change_signal_rows` 里 `user_visible` 为真的那些（可见性由各适配器在 SQL 里对 `list_sources`/`visible_document_count` 共用的可见谓词求值、作为**投影列**随行返回：不新造第三份拼写，也不另开查询——`source_type` 无索引，独立的「哪些被隐藏」查询只能整表扫，且就紧跟在 signal 查询之后），地图行尾 `| sources: N` 与清单分母出自同一个 helper（纯算术、零新扫描）；遍历顺序 = 来源页签的 `(created_at, id)`（两侧 `list_sources`/`list_sources_page` 也必须带 `id` 次键——并列 `created_at` 下缺次键会与目录分叉、让「前 N 篇」在页签与模型手上是不同的 N 篇；PG 本来就带。排序键随 signal 行同一次访问回来，双后端各自归一化，PG 侧先转 UTC 再 isoformat（`timestamptz` 的 offset 跨 DST 不是常量）；**指纹只吃前两个字段**，创建时间不进摘要，否则上线即让 L1/L2 全量失效一次；元素侧顺序仍按 `source_id` 不动——它的游标是 `(source_id, element_id)` keyset）；每份文档计一行，整份清单是**一个**分片（首个 hydration 窗口免费）；条目 = 显示名（论文标题优先）+ 文档类型界面词（`extraction_profiles.PROFILES`，未识别就留空、绝不吐 `academic_paper`）+ 已存摘要摘录，wire 上复用元素臂字段（`location_label` 装类型、`text` 装摘要、`element_type` 留空）。schema 里 `collection` 显示为**空缺省**（写成唯一值会让照抄模板的模型在列公式时也带上它、被静默改道），取值只在动作说明里并写明「会覆盖动作本身」。收尾除作用域指纹外还对**整条链已发出**的文档做一次有界批量复读、比对 (显示名, doc_type) 摘要（账目挂游标、仅内存、不进用户面 coverage），不等即 `concurrent_change`——论文元数据回填不碰 `updated_at`，指纹看不见它；摘要字段刻意不进该键（它的写者同语句就推 `updated_at`）。**账目回喂对它有一条定向豁免**：只有来源清单的链在账目行后附有界标题清单（≤20 条/每条 ≤60 字符/合计 ≤800 字符，超出写 `(+N more)`，分母只数有显示名的条目），因为 prompt 教的「按标题逐篇深挖」离了标题就断链；元素/知识对象清单的账目仍一个字正文都不带，摘要也不回喂。**范围指示语只在 prompt 层接地**：`prompts.SCOPE_DEIXIS_GROUNDING` 一段共用文本进意图契约 + 两份规划拼写 + reflect，教模型把「当前notebook/这个库/知识图谱/KG」解析成范围后**剥掉**、不带进任何子查询/关键词/`exact_term`，但问题本身要原样留；刻意不做确定性词表剥离（那是被否决的词法路由，且会误伤真在讲知识图谱的文档）。总闸 `REASONING_ENUM_TOOLS_ENABLED`（默认 true）由 `reasoning_retrieval.enumeration_wiring_active()` 单点判定（地图注入 / prompt / schema 分支 / allowed_actions / 早退放行五处共用）：关闭即两个动作与来源清单参数一并不提供、地图也不注入、早退恢复，零额外查询，完全回到接入前。
- **清单出处与答案归因**：每个送达的元素/KG 清单条目至多携带一条仍存活的有界 `Citation`（KG 取首个有效 evidence element）；跨库证据必须由服务端在 active notebook 的 participant 集内解析，浏览器只经 active-notebook 代理端点打开原文，绝不直连参考库成员端点。真正进入合成预览的条目使用隔离的 `k5001+` 命名空间和反向映射；只有答案实际绑定的这些锚点才能为清单内容归因，且仅带存活 `source_id`/`element_id` 的绑定键可把确定性枚举行判为 grounded。枚举答案一个锚点都没绑时，不得拿无关 ranked citations 冒充清单来源。
- **问答集合清单默认折叠**：集合枚举结果卡始终显示覆盖率徽章、清单名称与可选的单一来源范围，但条目内容默认收起；用户主动打开后才渲染按来源分组的元素、知识对象或文档清单。打开后继续沿用前 20 条预览与独立的「展开全部已加载内容」控制。部分结果/合成预览披露放在折叠内容内，卡片关闭时不得提前挂载图片条目。
- **命令目录抽取（方案 C·v2 全文档窗口）**：面向工具手册的 opt-in 结构化摄取，纯函数层在
`backend/app/services/command_catalog.py`，job/持久化/API 在
`backend/app/services/catalog_job.py`。**规则分节已整体删除**（`command_sections` /
`detect_manual_shape` / 形状信号 / 截断概念一并退役），改为整篇按序打包成窗口。
不可省的条目：①**窗口几何零丢弃**——整篇文档按文档序打包成至多 `WINDOW_CHARS` 的窗口，
元素整放、单元素超预算按字符切进相邻窗口，**没有任何内容被丢弃**（v1「超预算整个丢掉」
是静默数据丢失：实测一张 120 参数表整张消失，只剩一个光秃秃的标题，否决率还报 0.0，
因为已经没有东西可以失败）。**窗内切点必须向前回退到最近的空白**（换行优先，回退窗口
`SPLIT_BOUNDARY_LOOKBACK_CHARS`，找不到才按原位切）：命令名与 flag 都不含空白，落在空白上
的切点就劈不开 token，而 `global_pl` + `acement` 会让这个名字在两窗里都不是候选、这条命令
彻底消失。**候选数超 `MAX_CANDIDATES` 的窗口必须拆窗而不是截名单**——窗口不重叠也不重看，
截断＝第 33 条命令这轮乃至任何一轮都不会被问到，且没有任何否决/比率/报告行会显示它（模型
只可能答错它被服务过的名字），与 v1 的丢弃分支同类。所以分窗是两遍：先按字符预算打包，
再把超限窗按元素边界近似对半拆（单元素窗按字符切，复用上面同一套安全边界），递归到
`WINDOW_SPLIT_FLOOR_CHARS` 为止；到底仍超限的是**命令名索引**（再拆每片还是索引、却每片
都要付一次调用），保留截断但必须**披露**——`ExtractionWindow.candidates_overflowed` →
`WindowOutcome` → run 账目，非零时随 `catalog_job_finished` 事件报出（正常文档恒 0，字段
出现本身就是信号）。窗口标签取窗内**第一个** heading，无 heading 时继承上一窗的
**最后一个**（继承第一个会把 `set_e` 的续表标成 `set_a`）；候选行是 `window N` 形状的
内部标签时，`_catalog_cells` 的「出处」列**只写来源名**——那个标签会被人长期保存在知识表里，
在那个语境里什么也没说。**分片视图恒为整窗全文**：v1 的 head+参数行裁剪在「一个分片＝
一条命令的小节」时对，在「一个分片＝一份文档切片」时是系统性遮蔽（几十个候选只有首个在
视图里），多分片窗口每片重复携带整窗是刻意接受的代价；②**跳过闸公式**——
`needs_model ⟺ own 非空 或 (flag 非空 且 carried 非空)`，四形态：own 非空→调用；
own 空+flag+carried→调用（接力续窗，大命令文档的主体）；own 空+flag+无 carried→跳过
（无名可认领，接地保证产出必为空）；own 空+无 flag→跳过，**不看 carry**（接力一旦开始就
再也不空，凭它开闸＝按页付整本书的钱）。跳过窗不发调用、不做 liveness 探针、不发
`catalog_section_done`（散文 PDF 有数千个），但必须 `record_section` 推进进度（分母算它、
分子不算它的进度条永远走不完），carry 也**必须**照常穿过它。⚠**`record_section` 的返回值
是跳过窗唯一的存活信号**，两个调用点都必须读（`_record_window`，零新增查询——那是写操作
本来就返回的值）：跳过路径刻意没有 liveness 探针，级联删掉 job 行后忽略返回值就会让 worker
写完剩下每一个窗口、再给一个已不存在的任务落终态（整篇散文的文档会报成一次普通成功，而
结果谁也打不开）。顺序是**先复核显式取消再抛 `CatalogJobGone`**（用户按的停止比删除更具体，
既有 `cancelled_by_owner` 用例钉的就是它）；因为取消与删除可以同时成立，所有终态路径的
`catalog_job_finished` 必须走 `_emit_finished`（行没了就不发事件）——在 `except` 子句里回读
那一行会把一个已settle 的结局变成未捕获 `KeyError`；⚠**候选扫描读整窗每一行，不设行数上限**（v1 的 `MAX_SCAN_LINES=200` 已删）：窗本身已按
`WINDOW_CHARS` 有界，而单个元素可以是三百行展平参数表——漏掉 200 行之后的行，那里记的命令
既进不了名单、也进不了 `_dense_overflow`（不拆窗），服务不到的名字永远不能被认领且不留任何
拒绝记录或比率变化，正是零丢弃承诺要根除的那类静默损失；③**候选接力**——
`carry(i) = candidates(i-1)`，`candidates(i-1)` 为空时继承 `carry(i-1)`，遇新候选即重置；
接力名可在续窗认领，**只**免除「逐字出现在本窗」（名单成员资格、`syntax`/参数/`default`
的本窗接地照旧——名单是构造性接地，接力带的是见证不是猜测）。⚠**已登记取舍**：窗口候选是
这窗**提到**的所有命令形状名字，接力可能把孤立参数表键到只被顺带提到的命令上，没有任何
接地规则拦得住，靠人工审阅兜底——别为它加「只接力用法行调用过的名字」这类收窄，那会连
接力本来要救的普通情形一起丢掉。own 为空时 prompt **不得**渲染 `- (none)` 候选段再把
carried 放补充段（那是在叫模型回空，等于对续窗关掉整个特性），carried 就是主认领名单；
④**成本闸**——`preview` 零模型调用：调用数只在有界前缀里真跑分窗+跳过闸+分片精确测量
（**接力必须一起推进**，否则前缀里的闸与真跑答得不一样），触顶必须回报 `sampled`，并用
`skipped_windows_in_prefix` 解释「为什么调用数远小于窗口数」。**前缀之外的窗一律不报价**
（`estimated_calls` 只覆盖前缀，`windows_in_prefix` 说明覆盖了多少窗，且**两者都只算已封窗**——分窗只在下一个元素装不下时封窗，被测前缀的末窗还开着，一个未读元素就能翻转它的跳过闸判定或多顶出一次分片，给它报价等于对没读完的文本下断言；末窗字符退回池子与余量同算，「哪窗还开着」与段数下界共用同一个判定、不写两份）：按每窗 1 次计会同时
往两个方向错——跳过闸让散文窗免费、参数密集窗要好几片——而且那是替没读过的文本报价，与旁边
「实际可能更多」自相矛盾。⚠**被测前缀必须停在第一个被裁的元素处**：有界读取裁掉某元素后仍
继续返回后面元素的头，一起分窗等于把「被裁尾巴之后」的内容提前拼到前面，量的是文档里不存在
的拼接却挂着「开头 X 段」的名义（11,990 字符散文 + 一条命令：真实两窗且首窗免费，拼接后成了
「第一段就要一次调用」）。判据是 strip 后全长 > 传回文本长度（**内容**被截断；只截尾随空白的
不算）；那一行起全部只进算术余量。首元素即超长时 `windows_in_prefix`/`estimated_calls` 都是 0，
前端必须退化成「只给段数下界 + 调用次数视内容而定」，**不得**渲染「按开头 0 段估算需 0 次调用」
（把「没测」说成「测出来是零」）。**窗口数不得只用字符算术**：`⌈全文字符 ÷ WINDOW_CHARS⌉` 会低报
两次（元素整放留下的预算空隙 + 密度拆窗，三个 7000 字符元素实际 3 窗、算术说 2 窗），两者
都只增不减，所以它是**下界**。前缀恰好覆盖全文（`sampled=false`）时取前缀真跑的窗口数
（**精确**且零额外成本——前缀本来就要读来估调用数）；触顶时取几个下界里的**最紧者**并按
**显式下界**报出（前端文案「至少约 N 段」，`sampled=false` 才写「约」）。⚠下界成立有两个
不显然的前提：①**字符必须按分窗自己的归一化数**（元素打包前会 strip，两条 SQL 都按
`TRIM` 后长度求和；按原始 `LENGTH` 会**高报**——2001 个「1 字符 + 20 尾随空格」的元素算术
报 4 窗而实际 1 窗，高于真值的「下界」比没有更糟；元素间连接符刻意不计，只会更小、方向安全）。
⚠**两侧剥的字符集必须逐字一致**：SQL 的 `TRIM`/`BTRIM` 只剥空格/制表/换行/回车四个 ASCII，
所以分窗侧钉死 `.strip(" \t\n\r")`（跨模块常量 `STRIP_CHARS`：分窗、预告的裁剪判据、store 聚合三处共用一份定义），**不得**用剥全部 Unicode 空白
的 `.strip()`——否则 U+3000（中文排版满地都是）或 NBSP 填充的文档真实字符数少于 SQL 报的，
算术下界又跑到真值之上；代价是这类空白按内容计、占窗预算，方向保守。切点回退用 Unicode 宽的
`isspace()` 不受此约束（那是选在哪儿切，不是数有多少）；
②**前缀窗口数不能直接加余量**（分窗只在下一个元素装不下时才封窗，前缀最后一窗**还开着**，
未读元素会继续往里填——4 个短元素、上限 3 条，实际 1 窗而相加报 2 窗）：只把已封窗算作确定，
最后一窗的字符退回池子与余量同算；余量用每行**自己的完整长度**（`full_chars`）从全文总量里
减，绝不能用传回来的裁剪文本减（否则每个被裁元素的尾巴留在余量里被重复计一次）。绝不为了
数准而先把要估的那次全文扫描做一遍。`sampled` 的条数那一半按**元素总数 > 读回行数**判，**不是**「读回行数 ≥ 上限」
——整篇恰好等于上限的文档是被完整读过的，判成采样等于在唯一一份完全准确的估算上把精确值
降级成下界。而这个比较能成立的前提是两个数**同代次**：两条读取是独立语句，中间落进一次重解析
就会把一个代次的字符总量配上另一个代次的前缀，结果不像错的、只是描述了一份不存在的文档。
所以两条读取前后各取一次来源代次（复用确认路径那份 `source_element_generation`，别造第二份
拼写），不一致就**整对重读**一次（只重读代次没有意义：那只是确认了漂移然后照样报混代次的数），
仍不一致就抛 `CatalogSourceBusy` → 409；⑤**单飞与终态**——`catalog_jobs` 的条件唯一索引覆盖
`queued` **与** `running`（行先写、线程后起，漏 queued 就会在那个窗口排出第二个写同一份
候选的 worker），每条退出路径（含 `BaseException`）都必须落终态，启动兜底同样要收
queued；⑥**接地校验不可绕，且每一批参数只按它自己那批判**——命令名必须在服务端候选清单里且
逐字出现在**本窗**（整条否决；接力名只免后半条），参数名必须带原始前导短横，`syntax` 必须是
原文连续拷贝，`default` 找不到就清空。⚠**参数/`syntax`/`default` 的接地基准是该命令的证据段
而不是整窗**（`window_segments`）：一窗常记好几条命令，「这个 flag 在窗里有没有」同时放过和
误杀——`foo_cmd density` 与 `bar_cmd -density` 同窗时，把 `-density` 挂到 `foo_cmd` 能通过，
而 `foo_cmd` 自己合法的位置参数 `density` 反被「掉短横」规则误拒。分段判据：一行在标题元素里、
或该候选是该行用法行的行首 token（**行内代码提及不开段**——那正是误归属的来源），开段直到下
一个别的候选的锚点；同名多次取并集；首个锚点前的**前奏段**归接力（零锚点整窗即全前奏，续窗
照常接地）。登记的后果：只被行内提及、又不在接力名单里的命令没有段，参数与 `syntax` 全拒，
保留名字丢正文（与 suspect 标记同向）。**指派与覆盖率账目仍是窗口级**——归属只管「在哪儿接地」，
切开它会把「模型没答这个参数」变成「模型挂到别处了」。分段每窗切一次（`_process_window` 与
缓存准入 validator 各自 hoist），别放进 per-entry 循环；被拦条目连同原因**一起入表**（`state='rejected'`），那是一次零产出抽取唯一
可解释的证据。同一窗的参数全写在同一段原文里，所以接地校验分不出「这是不是本批的」：
`validate_entry` 必须同时收下本批的 `param_names`，批外的一律拒（`arg_outside_slice`），
本批问了却没回来的记进账（`arg_not_returned`）。两半都不可省——没有归属，答别批的回复
会连内容寻址缓存一起通过（那个缓存唯一的准入票就是同一个 `validate_entry`），整个 TTL 都
被投毒；没有覆盖账目，`args_keep_ratio` 的分母是「模型愿意答多少」，二十个参数答一个是
100%。分母因此是 `args_seen + args_uncovered`。空指派（无 flag 的命令）表示不设约束而不是
「什么都不许返回」——位置参数正是这类窗口记的东西，提示词的无 flag 分支必须**主动问**
（`parameter_names` 是 flag 扫描器，`set_dont_use lib_cells` 因此拿到空指派；那一支命令模型
返回 `args: []` 就等于系统性丢掉整类命令的参数元数据）。这类命令没有可服务的清单，把关
全靠接地：名字仍须逐字在本窗原文，覆盖账目恒空（没指派就没有未覆盖），而模型一旦作答，
该窗就照常计入 args 轴；⑦**多条目回答合同**——顶层 `{"entries": [...]}`，一窗记录几条命令就
返回几条（一个窗口是文档切片不是单命令小节）；`entries: []` **合法**（这一窗真没有命令），
缺 `entries` 或它不是列表才按不可用回答走二分补救；清单里的非对象项转成**可见的被拦行**
（`_payload_entries` 给 `None`，`validate_entry` 照常产出 rejected），不留静默缺口。
内容寻址缓存的准入比这一层**更严**（空/零接地的 entries 一律不入缓存）：拒它只是一次
cache miss，收它是把「这窗什么都没有」冻结整个 TTL；⑧**跨窗合并一命令一行**——run 级
`flushed` 注册表把后续窗口的结果并回前面已写的那一行（args 按名去重 first-writer-wins、
`syntax`/说明只补空、anchors 在 `MAX_ANCHOR_ELEMENTS` 内取并集、excerpt 保留首窗）；
**「疑似只是被提及」按 AND 合并，但靠接力认领的条目没有这一票**（`ValidatedEntry.relayed`）
——续窗的「不疑似」是接力的豁免而不是判定结果（那窗既没标题也没用法行，本来就没在判这件
事），拿它 AND 会把上一窗挣来的警告洗掉，而且恰好在接力存在意义的那种形态上每次都洗掉；
判据是**本窗有没有直接证据**（标题或用法行里逐字出现）而不是「名字在不在接力名单里」，
所以接力名在本窗有直接证据时照样能清除标记，首次产出（新行）语义不变；
`update_candidate_payload` 只改 `candidate` 态的行，返回 False（已被 apply/dismiss 挪走）时
**降级为追加一行**而不是静默丢参数——重复行审阅者看得见，丢失看不见。⚠**写回必须拿
apply/dismiss 那把 `("catalog", notebook_id)` 目录锁**（`_persist_window` 一个临界区包住
「update + False 降级追加 + `flushed` 注册表更新」）：apply 是跨两个 store 的读后写（读
payload → 渲染进 knowhow → 才标 applied），写回落在它的读与写之间会让表里是合并前的参数、
而被标 applied 的候选行是合并后的——两份互相矛盾的「确认了什么」，单看哪一份都不像错的；
dismiss 反向吞掉合并（update 成功了所以不产生降级行）。锁序：run 线程只拿目录锁、**绝不**
拿来源写栅栏（那是唯一会成环的顺序），临界区里不含任何模型调用（模型调用在
`_process_window`，进临界区之前），进程内锁足够是因为部署钉死 `--workers 1`；⑨**三轴熔断**——满
`MIN_WINDOWS_BEFORE_ALERT` 个**实际发过调用**的窗口（跳过窗不进样本）后，命令名否决率超
`COMMAND_REJECT_ALERT_RATIO`、args 保留率低于 `ARGS_KEEP_ALERT_RATIO`（闸是「这一轮问过
东西」即 `args_seen + args_uncovered > 0`，不是 `args_seen > 0`，否则真正无 flag 的手册会被
误杀）、或不可用回答分片占比超 `SLICE_FAILURE_ALERT_RATIO`，任一成立即判 failed 并给用户
可读理由，绝不静默交付近空目录。第三轴不可省：一个什么都不返回的模型让命令名比率保持无害，
而它拖低的 args 比率报的是症状（「参数丢了」）不是病因（「这个端点在返垃圾」）；多轴同时
成立时报最具体的那个（不可用回答解释得了差的 args 比率，反过来不成立）。合法 `entries: []`
**不算**不可用回答（否则散文密集手册被误杀）。⑨b**全空产出不得假 succeeded**——发过 ≥1 次
调用、`entries_seen == 0`、候选行与被拦行皆零时落 `failed` 并给 `NOTHING_EXTRACTED_MESSAGE`
（走 `user_error()` 同款出处规则）；有被拦行仍 succeeded（用户能自己判断），全跳过的 run 也
仍 succeeded（压根没调用模型）。这是对**已跑完**的 run 的判决，刻意不做成第四根熔断轴；
⑩**apply 保守合并**——⚠先决条件：**任务未落终态时 `apply`/`dismiss` 一律 409**（闸在 API
边界 `catalog_routes.py`，白名单复用 `CATALOG_TERMINAL_STATUSES`，文案走 `user_error()`；
`queued` 也算未完成）。它堵的是一条会造出**永远确认不了的候选**的路：识别中途确认某条命令
→ 后续窗口的合并写回发现行已 applied → 按降级路径追加同名新候选（刻意的：参数看得见比丢掉
好）→ 确认这条替补行时目标表已有同名锚点，被下面这条「同名一律不改行、只回报 conflict」的
合并语义跳过，迟到发现的参数从此可见而不可落库。**服务层不加这道闸**——R2 的目录锁与降级
路径原样保留为纵深防御（用户先取消、恰好与最后一个窗口的写回交错这类合法竞态仍会走到）。
目标表不存在则建（「命令目录：<来源标题>」，命令/语法/参数/说明/
示例/出处，「命令」为行标题列），存在只**新增**表里没有的命令；同名一律不改行、只回报
conflict（分不出「陈旧行」与「人工订正过的行」，覆盖是本特性唯一不可逆的破坏），落库全部
走 knowhow 既有服务层因而自带 record_change。列按**名字**寻址而不是按位置——目标表有实时
增删列端点，位置映射在用户删一列之后仍会「正常工作」，只是把语法写进「说明」；缺「命令」列
直接拒绝写入。一次 `all_pending` 最多确认一页，必须用 `pending_remaining` 如实回报剩余，
不能让 300 条候选的库看到 `rows_added: 100` 就以为做完了。apply/dismiss/陈旧清扫的并发锁
**一律是 `("catalog", notebook_id)`**，判据只有一条:锁键必须**不可变**。按表 id 取锁的那一刻，
「表存不存在」正被这把锁自己保护的 `create_knowhow_table` 改写；按派生标题取锁则会被论文元数据
接地在两次 apply 之间把上传名换成论文标题。按来源取锁同样不行——两个派生标题相同的不同来源
解析到同一张表，而来源写栅栏是 per source、挡不住它们。`applied_table_id` 只用于锁内的目标解析，
绝不当锁键。锁序:来源写栅栏在外、目录锁在内，完整枚举在 `_target_lock_key` 的 docstring 里。
粗粒度的代价（同库不同来源的确认会串行）是刻意接受的:每个写者都有界、不调模型、由人点击触发。⑪**模型自撰字段在合并处
封顶**——说明/示例/每个参数的 desc 都是接地校验刻意不查的，`_merge_entry` 是它们到库之间
唯一的收口；参数 desc 需要两道闸（每条 `MODEL_ARG_DESC_CHARS`、整行 `MODEL_ARG_DESC_TOTAL_CHARS`,
一行的条数等于命令的参数个数），总量截断从尾部截并把条数记进 `reject_info.desc_overflow`
（与统计拦截记录条数的 `overflow` 分开两个键，合在一起两个数都读不出来）。
效率合同=每个**发过调用**的窗口的调用数=它的分片数（跳过窗为零）；唯一例外是共用同一套二分机制的两条有界补救（回答不可用、
或覆盖率过低且回答偏短——`SLICE_COVERAGE_RETRY_RATIO` / `MIN_ASSIGNED_FOR_COVERAGE_RETRY`，
只在 depth 0 触发，因而 `MAX_CALLS_PER_SLICE` 不变；回答条数与指派一样多只是答错了的不重问，
问得更少救不了它），单个分片最多 `MAX_CALLS_PER_SLICE` 次，失败分片一律记成 rejected 行。模型通道复用
`kg_extract` workload，不新增配置面。⚠两条 seam 事实决定这条补救怎么触发，都极易搞错：
①`chat_json` 不回传 `finish_reason`（签名被契约测试钉死），而 `_validate_json_object` 对
**空 content 与截断抛同一个** `malformed_response`——所以在这一层「模型没给出可用回答」是
**一个**信号而不是两个，别在文档里写成「空内容重试一次」；②必须按 provider 的**稳定错误码**
分类，绝不能按异常类：scheduled adapter 把一切重抛成 `ModelInvocationError`，它是
`MalformedModelResponse` 的**兄弟类**，`except MalformedModelResponse` 只会匹配测试 double、
在生产上永不命中（整轮会在第一次截断时直接 failed）。瞬态 provider 错误（限流/5xx/鉴权）
显式不走二分，直接冒泡判 failed，绝不能被吞成「这一窗本来就没有命令」。
⚠`catalog_candidates.job_id` **刻意不加外键**：加了 `catalog_jobs` 就不是叶表，而
`idx_catalog_jobs_one_active` 是 source_id 单列面（source_id 又是外键列、NOT NULL），
正向 shadow 会整个 PG catalog（当前 v28）判为不可停车。
⑫**解析前提与来源代次绑定**——`preview`/`start` 都要求 `parse_status ∈
{parsed, extracting, extracted}`（仓库既有白名单，后两档是解析之后的 KG 抽取阶段），
否则 409（未解析完/解析失败两套文案）；`catalog_jobs.source_generation` 记下创建时刻的
**来源代次**＝`MAX(source_elements.created_at)`（`replace_elements` 把整批新元素写成同一个
`created_at`，所以它当且仅当元素被换过时才变），`apply`/`dismiss` 在**同一把 per-target 锁
内**先比一次，对不上就 409 并用 `expire_pending_candidates` 把该 job 剩余候选**整批**标成
`dismissed`（`source_reparsed`）。三处不可省：代次**不能**用 `sources.updated_at`（那是有意
做粗的变更信号，重新抽取 KG／写摘要都会推进而元素没动，按它判会谎报「已重新解析」并逼用户
重跑一整轮付费识别）；作废**必须**整批（页界的 `mark_candidates_dismissed` 会让 >100 条候选
的 job 半作废且仍被重跑守卫挡着）；`start` 侧遇到**已过期**的旧候选要清掉并放行，否则「待审
候选拦重跑」与本条互相死锁——每次确认因过期被拒、每次重跑因未审被拒。
前端 `CommandCatalogReview` 的每次 apply/dismiss（含失败路径）都必须经 `onReviewed` 回调让
`CommandCatalogSection` 重读一次 `.../job`：弹窗在 page 根层、卡片在来源详情里，两者只剩
page.tsx 这一条接线，不重读则「重新识别」会一直被卡片手里那份旧 `pending_candidates` 挡着。
前端界面词一律说「段」（预告「将通读全文（约 N 段）…」、进度「已处理 N/M 段」）；后端的
`window N` 是**内部标签**，必须经 `command-catalog-model.ts` 的 `catalogSectionLabel()` 转成
「第 N 段」，不得原样上屏。数值上限（窗口字符、候选上限、每片参数数、每片调用上限、拦截/
出处条数、熔断样本与三轴阈值，以及已退役的 v1 分片视图/开头摘录上限）只登记在
`docs/product-and-api*.md`。
- **完整枚举免责的抑制规则**：`completeness_unavailable` 的前置警告只在**四条同时成立**时抑制——`result_scope != "aggregate"`、意图合同的 `constraints`/`excluded_topics`/`assumptions` 全为空、至少一张清单卡 `returned_total > 0`、且该卡 `complete=True`。方向是宁可多警告：清单卡的 coverage 只证明「某个物理集合被完整走了一遍」，证明不了它就是用户要的那个子集；这里刻意不做语义匹配（无确定性判据）。
- **完整语义与 prompt 硬预算补充**：确认时按最终编辑措辞与权威澄清答案重算 scope；结构化执行器只处理整表物理行/方法清单、直接物理行/记录计数及其 hybrid。“多少种”等去重/种类计数、条件筛选、group-by 没有确定性计划时回退并披露不支持完整。轻量 catalog 最多返回 8 个表描述，不读取格、代码附件或健康大字段，并在截窗前优先纳入显式点名表。响应分开 per-table、batch、synthesis coverage：枚举 200/200 而模型预览 100/200 时写“枚举完整、分析部分”，8 表截断不污染已耗尽单表。KG 对象/关系、confirmed Memory、查询期链共享 KG 字符硬预算；结构化预览、chunk、direct element 共享原文硬预算；最终证据块不超过两者之和。
- **大纲便签与按节合成**（借鉴 DualGraph，PR-3）：仅 `exhaustive`（穷尽）档且 `REASONING_OUTLINE_ENABLED`（默认 true）开启时，reflect 循环新增 `update_outline` 动作（第 11 个动作 id；章节结构全量替换，同一稳定节 id 的合法证据与旧绑定 union，遗漏不删，`remove_evidence` 才显式撤销；8 键满额时旧键优先、未接纳新键进入下一轮账目）。合法 key = 存活候选池 ∩（run 内候选摘要曾实际展示的 key ∪ 当前大纲已持有 key），既不会因窗口滑动丢旧绑定，也不接受从未展示的中段 id；pending 不冻结普通额度内的结构更新，只有 sufficient/stale 的预算内终态纠错与第 6 次后的单次资格才是同结构纯换键，`max_steps` 绝不因纠错增加，stale 事实先落 trace。终态仍未接纳的 key 写入可见 trace。非法键静默丢弃，空节记入回喂账目供模型定向补检索；大纲修订对 stale 熔断中性（纯整理不算进展，真正的检索动作仍照常清零）；一次畸形提交跳过并保留上一份大纲，绝不清空。循环结束时若按候选池解析后仍有 ≥2 节能装配出证据、且该 run 未产出集合清单/结构化整表枚举（清单 run 保持单次合成——清单预览与覆盖披露只进单次路径的合成上下文，节化会拿 ranked 样本写散文而让完整清单闲置），则按节合成——每节只喂该节绑定证据的上下文切片（集合地图/枚举预览/私有 Memory/查询期推导链不进切片，留在回退路径），号段偏移保证跨节不相交，锚点按各节自己的 id_map 解析后再合并（写出别节号段只可能是幻觉，直接丢弃）；每节用自己的切片与锚点通过 `classify_evidence`，逐节记录落 synthesis detail 的 `section_grounded` 列表（不是整篇 flag），全部有据照旧，否则只把全局结果封顶 `overview`，零节精确 grounded 也不强制改成 `inferred`。任一节合成失败即整体回退单次合成，且回退成功后不留假 model_error 横幅；按节被绕过（不足 2 节或清单 run）时大纲披露不消失——规划跑过就在收尾 synthesis detail 带出 `outline_skipped` 等键。模型写进节标题的引用形 `[k]` 标记在解析入口剥除。trace 新增 `outline` 步类型（前端标签「大纲」）与每节一条的合成进度步（收尾步在引用数后披露略过节/依据不足节）；关闭态/低档位与接入前逐字一致。v1 不进 `AskResponse`。数值上限（节数/层数/每节绑定数/每 run 调用次数）只作为契约写在 `docs/product-and-api.md`/`_zh.md`「大纲便签与按节合成」一节，这里不重复。真源见 `docs/reasoning-enumeration-tools-design.md` §3.1。服务端弱支撑边回喂（PR-4，`REASONING_OUTLINE_KG_GAP_ENABLED` 默认开，叠在上面这把总闸之上）：每次**被接受**的 `update_outline` 之后，按本次新绑定证据的 canonical 邻域有界探测 `canonical_relations`（判据 `source_count` 而非原始关系行数、经主键前缀+`idx_clusters_member` 解析显示名、run 级 `kg_gap_probed_seeds` 防重复探测、异常 fail-open 记 skip 步），把「支撑薄弱」的关系提示追加进大纲便签供模型用既有动作定向补检索；终态纠错轮不渲染也不消费该段。数值上限只在 `docs/product-and-api*.md` 契约段维护，真源见设计文档 §3.3。**大纲采用引导**（设计文档 §3.1.1，真机采用率 0/3 才加的）：闸开着、当前大纲**为空**、本 run 引导额度未用尽，且有一条服务端手上现成的结构性理由（本 run 已把来源清单枚举到 `state == "complete"` 且条数达下限，或已确认检索方向数达同一下限；方向数取 `run()` 现成方向清单长度减一，不重新解析契约）时，在几份账目之后、集合地图之前追加一行确定性引导，措辞含现状+具体条数+「判断不需要就忽略」的出口。两条理由同时成立用清单那条；没列完的清单一律不算（半份目录建出的大纲缺节而模型不自知）。零新增查询/模型调用/动作 id；与便签的互斥判据写在 `_outline_nudge_note` 自己的「sections 非空即返回空串」里，不是调用点的 `else`；只有真发出的那一轮给既有 reflect 步 detail 加 `outline_nudged: true`（无条件写 `False` 会破坏「detail 逐键不变」的冻结基线口径）。数值上限（每 run 引导轮数、触发下限）只在 `docs/product-and-api*.md` 契约段维护。同批：`_answer_with_retry` 的**重试成功不留假报警**——进入时记 `mark = len(sink)`（sink 取 `_ASK_MODEL_ERRORS.get()`，为 `None` 则不摘），重试成功时只丢 `sink[mark:]` 里 `workload_id == "ask_answer"` 的那几条（按身份而非位置过滤——今天 `synth()` 内部不记别的报警，但给证据精炼补一条就会让位置式截断在「首次即成功」时把它一起删掉）；两次都失败一条不摘（含终态 empty-content 的 `RuntimeError`，「检索到却答不出」必须可见），`mark` 之前其它 workload 的报警一律不动，取消路径不受影响，`events.jsonl` 始终记全。按节合成那处的区间删除保持闭上界 `outline_err_mark:fallback_err_mark`，不得改成开区间。深度报告穷尽档的逐节深挖同样启用大纲便签与弱支撑边回喂（PR-5）：报告自己的 depth 五档（1/2/4/8/16）映射到本条同一批检索档位名（阈值判定，中间值落更低档），每节 `max_steps` 仍用报告自己的 depth 值而非档位表的步数上限；深挖整理出的子大纲只折成有界「发现的结构」块，作为 `###` 组织建议附进该节撰写 prompt，绝不回写用户确认过的 `reports.outline_json`。档位同样管到检索之后的两段：按方向补检索的合并结果按该档 `ranked_final_cap`/`answer_element_items` 重新截断（相关度降序，大纲绑定对象豁免），节撰写上下文用 `kg_context_chars` 与**共享**的 `chunk_context_chars`（原文段吃 chunk 剩余额度、条数封顶 `answer_element_items`）而不是 `ANSWER_CONTEXT_BUDGET_CHARS`/`REPORT_SECTION_CHUNK_BUDGET` 定值外加一份 1/3 元素额度；大纲绑定对象的优先额度必须在**一次** `knowledge_context` 调用内完成（`priority_object_ids`/`priority_budget_chars`），拆成两次会丢掉所有跨两半的 `relations:` 边——只在 `run()` 里兑现的档位会被消费它输出的那两段原样送回去。真源见 `docs/reasoning-enumeration-tools-design.md` §3.4。**大纲阶段与深挖的调用/检索削减**：档位同样缩放大纲阶段两类 0-LLM 探针（覆盖/充分性）的每主题·每节查询宽度（数值只登记在 `docs/product-and-api*.md`；行上缺 depth 保持历史宽度），探针产出只喂 STORM/Judge 的覆盖计数、不进正文证据；同一次规划 run 内重复探针查询经 run 级 memo 只检索一次（memo 不跨 run、失败不缓存——瞬态错误保持逐探针重试）；「多视角规划大纲中」与「大纲就绪」之间必须有「检查各节证据充分性」进度写（STORM+探针段此前零进度写，大库上是分钟级表观卡死）；充分性 Judge 的 LLM 精修半只在 deep 及以上档运行，overview/standard 只跑确定性半（界面消费的 coverage 与结论都来自它，且与 Judge 既有 fail-open 产出逐字一致——LLM 半只降不升）。每节深挖把**节复合问题作为第一条种子**（镜像 Ask：完整权威问题恒为首条）、随后接该节已确认检索方向,经 `intent_queries` 进 run——已审阅方向是权威，不让第二个模型重规划（与 Ask 同一原则），每节因此省一次 plan LLM 调用；run 内**成功**执行的方向（attempted 记账且无稀疏 `failed` 标——检索炸了 ≠ 检索过）run 后合并**不再重复** federated 检索，元素补取照旧（run 不按方向逐条取元素），run 步数装不下或检索失败的方向仍由 run 后合并兜底——「确认后的检索方向必须实际执行」由 attempted 判据两边共同兑现；低档探针宽度带来的更保守充分性结论已在契约文档登记。
- **大纲未接纳键**：overflow key 是服务端持久 pending，不是单轮诊断；成功绑定、在 `remove_evidence` 点名放弃或整节删除之前持续回喂。只腾位却漏抄新 key 仍在终态 trace 披露；结构违规只拒绝真正的纠错提交，普通额度轮不会因 pending 被改成 repair-only。完整状态每节恰好以 56 key 为硬顶，prompt 每轮只展示前 8 个与剩余计数，处理后再滚动露出下一批。
- **检索结果带图与 Markdown 打包上传**：图注或图片描述是图片进检索的唯一入口——两者都没有就不进 chunk，也不做「就近带出」。`CitationImage` 必须同时挂在 `AnswerAnchor`（reasoning 权威 `[k]` 路径）与 `Citation`（回退列表），空列表整体缺席 JSON；Ask 按上面的正文内联规则展示，报告公开分享白名单绝不带 `asset_id`/`element_id`，深度报告正文内联留到第二期。报告侧仍是全篇一次、fail-open 的批量点查装配。`.zip` 已是后端 parser capability registry 的一等格式：浏览器原样上传，内建 `markdown_bundle` 按每份 Markdown 自身目录解析相对路径并持久化图片；拖入完整文件夹继续走浏览器 data-URI 兼容管线和五类持久配对回执。部署关闭图片存储时两条路径都必须如实提示/无图降级。精确数值只登记在 `docs/product-and-api*.md`。
- **Agent 库理解**：`agent_notebook_profile` 持久化三块共享底座（`corpus_shape`/`key_entities`/`corpus_gaps`，来源变更累计到阈值后由后台巡固刷新，输入 SQL 结构上够不到任何使用数据）与两块每用户覆盖层（`retrieval_notes`/`usage_gaps`，该用户完成足够多次提问或一次深度报告后刷新，只读该用户自己的轨迹，`created_by`/`user_id` 谓词写在取数 SQL 内而非 Python 侧过滤）。两条链路的隔离判据是 **SQL 谓词 + 语义扫描守卫**（`test_agent_profile_isolation_guard.py`），单点判定 `profile_wiring_active(settings, profile_store)` 同时管住 plan/reflect 注入、巡固触发与两个 API 读写面的可见性；关闭 `AGENT_PROFILE_ENABLED` 逐字回到接入前——不注入、不记 trace 步、不排巡固、前端不显示入口；API 读回 `enabled=false` + 空列表、写端点 409（刻意**不是** 404，前端要能区分「关了」与「还没有」）。块内容**只**作为 plan/reflect 的 prompt 脚手架注入，`ReasoningResult` 不新增字段承载它——绝不进答案合成上下文、不可被 `[k]` 引用、不进深度报告正文。渲染层（`render_profile_block`）是块值「先压单行再截断」的唯一收口，写入侧不必自己压。深度报告逐节深挖同样注入，但只落在 reflect（`_deep_dive` 恒传 `intent_queries` 不调 plan）。副本深拷贝不复制这两张表；同步 `POST /ask` 不计入覆盖层触发计数；成员被移出/降级后覆盖层按读侧参与集判定失效，物理清理挂在移除路径。共享底座的编辑权是 `agent_profile:write`，与 `knowhow:write` 同格（P2-T2 之后为 admin 档：owner ∪ `role='admin'` 的有效授权边），响应投影 `can_edit_base` 随 `_CAPABILITY_LEVELS` 自动跟随。精确数值（块字符上限、历史环形条数、触发阈值等）只登记在 `docs/product-and-api.md`/`_zh.md`「Notebook understanding blocks」一节。**P2 三条加固**：①巡固链路新增认领代际 `claim_token`——每次 `claim` 铸新值，`settle`/`write_block` 都把它当 CAS 条件的一部分，关掉成员移出后行被重建导致的结算错代/写错代（`settle` 从二值变三值 `settled`/`gone`/`superseded`，只有 `gone` 才抹块，`superseded` 绝不能抹——新一代可能已经写好了自己的块）；②`note_ask_completed`/`note_report_completed`/`start_overlay` 三处在 bump/认领**之前**复核读侧参与集（同一份 `user_can_read_notebook` 谓词），关掉「移出后才到达的完成通知」重建已移出成员那条链的残余，检查异常仍整体 fail-open；③报告自己的分节检索账目（`reports.sections_json[i].attempted`，只读 `created_by` 是本人、`status='done'` 的报告）并入覆盖层巡固样本，只喂 `retrieval_notes`（措辞经验）不喂 `usage_gaps`（零命中信号）——`attempted.new` 数的是 run 共享候选池的新增量而不是「这个方向查了没查到」，把它当零命中信号会把假阴性写进用户私有笔记。**检索策略经验库（P2-T5/T6）**：部署级全局表 `retrieval_experiences`，条目＝封闭情境指纹（意图契约的结构化字段分桶，不含问题原文）→ 封闭动作词表 + 好/坏极性 + 模型撰写的一句理由，只影响**怎么查**（动作/通道选择），**绝不影响检索范围**——动作词表结构上不含任何 `source_scope`/`base_scope` 相关取值，且渲染出的块里没有任何来源/参考库名，反向守卫钉住这条。蒸馏输入**零自由文本**：投影层 `RunObservation`（及其嵌套类型）每个字段都是 `int`/`bool`/封闭枚举，没有一个 `str` 自由文本字段，写理由的模型从未见过问题原文、文档标题或任何 id——隔离因此是可测的结构性质而非 prompt 侧软约束（`test_retrieval_experience_privacy_guard.py`）。表本身**全局无 `notebook_id`/owner 归属**（打法是通用知识，不是任何人的内容），深拷贝结构上够不着它，`merge_dbs.py` 归全局并集表，主键是情境+动作的内容寻址哈希（跨部署并集安全）。蒸馏闸（`RETRIEVAL_EXPERIENCE_ENABLED`，默认 true）与注入闸（`RETRIEVAL_EXPERIENCE_INJECT_ENABLED`，默认 **false**）分开——先攒观测数据，注入效果待验证再开。用户可见面只有轨迹里的一步（标签「打法」+ 条数），不做管理面板（集合地图式内部脚手架口径）。精确数值只登记在 `docs/product-and-api.md`/`_zh.md`「检索策略经验」一节。**Agentic Memory P4 additions.** 三个互不相同的读/写面，缺一不可地共同兑现「step→anchor 归因」这条 P2 登记残余：①**写侧**（T1/T2）——`retrieve`/`ppr`/`exact_lookup`/`expand` 四个真正产出可寻址 id 的动作分支无条件写 `result_ids`（含零命中的空列表，那本身就是信号），终态 `synthesis`/`answer` 步单独写 `anchor_evidence_ids`（答案真正绑定的 `[k]` 锚点），两者都截到 `docs/product-and-api*.md` 登记的上限；`expand_community`/`follow_chain`/`enumerate`/`outline` 刻意不碰，`search_elements` 落在词表外的 `fallback` 步，元素级锚点结构上不可归因（登记边界，不是遗漏）。前端 `getTraceStepDetail` 对两个键都没有任何渲染分支——它们是只供蒸馏投影读的裸句柄，不上屏。②**蒸馏侧**（T3/T4）——`RunObservation.ActionObservation` 新增 `attributable`/`anchored_hits` 两个字段（`bool`+`int`，不是一个可空 `int`：隐私守卫的类型注解扫描器只认 `int`/`bool`/封闭 `Literal`），求交只在投影自己的循环里就地算一次、用局部变量，原始 id 从不进入撰写理由那个模型的视野，隔离性质逐字不变。③**读侧的两条新消费路径**——`consult_memory`（T5）是 reflect 循环里模型可以主动选的**零参数动作**（不是被动块的第二份拷贝：选择上排除被动块已送达的行、优先给本 run 已经空转的动作），只在 `retrieval_effort` 为 deep/thorough/exhaustive **且** `RETRIEVAL_EXPERIENCE_INJECT_ENABLED` 也开着时才出现在 `next_action` 枚举里（总闸并入注入闸是对设计稿字面的刻意收窄，已登记：注入闸关着时经验库对全部署都读不到，只按独立 kill switch 放行会让模型看到一个调不动的动作）；它还额外合并调用者自己在 P2/P3 理解块里被整块字符硬顶截没的「检索心得」覆盖行。深度报告逐节深挖的 depth→档位映射（PR-5 同一张表）让它在 deep 及以上自动生效，不需要新接线。轨迹步标签「回想」（不是「记忆」——那个词已被 Memory 召回步占用）。`consult_memory` **不是** `RETRIEVAL_ACTIONS` 词表的第九个词——它是拉取机制本身，动作词表仍是那八个可被经验条目推荐的检索通道。**步级提示**（T6）是服务端确定性推送（零 LLM）：`ppr`/`exact_lookup`/`expand`/`follow_chain` 四个已埋点分支之一在本 run 内连续两次零新增后，把一句引用经验库 `bad` 条目原文理由的中文提示拼进 reflect 账目摘要，每 run 至多两次（四动作合计的总闸），只受注入闸门控、不要求档位（不占用额外反思轮）。三者的精确数值（`TRACE_RESULT_IDS_MAX`/`TRACE_ANCHOR_EVIDENCE_IDS_MAX`/`consult_memory` 每 run 调用与条目上限/块字符上限/提示阈值与上限）只登记在 `docs/product-and-api.md`/`_zh.md`「检索策略经验」一节。
- **缺口外扩检索（`ask.gap_consult`，X9 PR-A）**：新增生产扩展点，让部署插件在**逐步推理**问答（`ask_graph`/报告/Knowhow 补全都不经过 `_run_reasoning_stage`，够不到这个调用点）收尾**恰好一次**——response-draft 阶段**返回之后**、持久化之前发起（触发判据读草拟前就冻结的检索事实，含所选来源图激活与检索自身 fail-open 降级；排在草拟之后是隔离结构化的关键：注入的草拟实现从 envelope 里连「发生过一次外扩」都看不到，codex #584 R3）——提供笔记本之外的资料线索。触发是两条只读账目之一：终态披露步仍列着一个确认过却未执行的方向，或证据池薄于当前档位自己的 `ranked_final_floor`；两者皆否零调用。外发面只有一个有界 `GapConsultQuery`：用户**实际见过**的措辞截断到 300 字符（只有真走过澄清门——确认契约的 `needs_clarification` 为真——`resolved_question` 才算审阅过；自动确认的规范化问题是没人看过的模型改写，绝不外发，改用原始问题原文，codex #584 R5）+ 至多 2 条缺口短语各截 60 字符，`[k]`/【k】标记先剥除，意图契约的复合 `research_question` 与任何 notebook/actor/source 身份绝不外发。产出**不是证据**：不检索、不打分、不进 `anchors`/`citations`，`AskResponse.gap_suggestions` 由 core 在 response-draft 阶段返回**之后**才填充（绝不经 `ResponseDraftInput`），公开会话分享的白名单投影不带它。宿主 `GapConsultHost` 把探测与 `consult` 一起丢进一条私有 daemon 线程（不进线程池、不 `copy_context()`），50ms 分片 join 一个覆盖整次调用的硬 deadline（`ASK_GAP_CONSULT_TIMEOUT_SECONDS`，默认 4.0 秒，答案延迟预算而非完成后协作式 deadline）；除取消外，超时/异常/畸形结果一律 fail-open——答案不变、不出错误横幅/诊断——但**不是**把披露步一并抹掉：披露步记录的是「发生过一次有界外发、拿回了几条」这件事本身，失败时如实记 0 条，读者有权知道这次外发发生过，即使它什么都没带回来（codex #584 R4 已驳回，见 `ask_service.py::_consult_gap_sources`）。**真挂死插件每次泄漏一条 daemon 线程是已登记接受的代价**（熔断留待后续）。可用性走插件自己 manifest 的 `provides`，核心零专属开关，无插件部署零调用零事件。导入按钮复用核心 `POST /notebooks/{id}/sources/url`（`sources:write`，只读成员不出按钮），只收 PDF 直链，拒因逐字上屏。数值上限只登记在 `docs/product-and-api*.md`「Gap consultation」/「缺口外扩检索」一节。守卫：后端 `test_gap_consult_host.py`（宿主本身）、`test_gap_consult_ask_wiring.py`（触发/外发/持久化）、`test_conversation_public_view.py::test_gap_suggestions_never_reach_the_public_projection`（公开分享排除）、`test_gap_consult_docs_contract.py`（数值上限与 SOP 扩展点表反射对账）；前端 `frontend/tests/guards/gap-consult-import-route-guard.test.mjs`（导入必须走核心 `importUrlSources`，绝不悄悄改道插件路由）、`long-task-button-guard.test.mjs` 新增两条钉 `answer-gap-suggestions.tsx` 导入按钮的忙碌态、`answer-panel-callbacks-guard.test.mjs` 钉 `onImportGapSuggestion` 只读收起、`frontend/tests/component/answer-gap-suggestions.component.test.tsx`、`frontend/tests/component/answer-panel-readonly.component.test.tsx`（只读视图「区块在、按钮 0」钉在这里）。

### 工程约束

- **`RepositoryRuntime` 组合根**：`__init__` 只按序调用 11 个模块级 `_build_*` 纯领域构造函数并逐条挂载各 frozen bundle 的字段（外加它自己的两把 `threading.Lock()`），构造顺序即依赖拓扑；builder 只接收更早的 bundle 与三个迟绑定 callable（`_current_user_id`/`ask_service`/`_note_ask_completed`，构造期只存不调），不得接收 runtime、读 `RepositoryRuntime` 类名或经 `__self__`/`__closure__` 回指组合根；进程级副作用与唯一一次 `bundle.create()` 的相对顺序不变，86 项属性集合由 `backend/tests/test_repository_runtime_composition.py` 冻结，`__init__` 行数钉在零余量上限。真源见 `AGENTS.md` Architecture Baseline。
- **效率是一等约束**：新增 LLM / embedding / DB 调用前先问代价——能否合并、缓存、异步、按需 gate。强一致做成 opt-in，默认走低开销路径。
- **notebook 授权判定唯一定义点与能力守卫**：读/管理/写谓词只在 `backend/app/repositories/{sqlite,postgres}/access_sql.py`（双后端同修，镜像 mount_sql.py）。三条谓词构成严格包含链 `写权 ⊆ 管理权 ⊆ 读权`，这条不对称本身是安全边界：**写权（`NOTEBOOK_WRITE_SQL`）恒 owner-only**；**管理权（`NOTEBOOK_ADMIN_SQL`，P2 能力翻转）= owner ∨ `role='admin'` 的有效授权边**，其主体判定刻意**复用受限三臂（user/group/group_admins，`_restricted_principal_arms`）外加 `role='admin'`、排除 `everyone`**（构造性保证「管理权 ⊆ 读权」不靠逐格测试；`everyone` 被排除是深度防御——正向 shadow 停车会给 `(everyone,'',<哨兵 role>)` 冲突行的 `role` 暂写哨兵串，若被瞬时读成 `role='admin'` 就把管理权发给全站，从谓词侧根绝而不依赖「发放口径永不写 everyone+admin」这个外部前提）；**读权（`user_can_read_notebook`）= owner ∪ 只读成员 ∪ 有效授权边（`notebook_grants` 的 user/group/group_admins/everyone 四值精确白名单，停车行因而 fail-safe；四臂全收、role 不参与，一个字都没动）**。memory store 的三段式 FOR SHARE/三态站点按 allowlist 保留、扩读权时须手动同步。读权⇒可挂载（`mount_sql.MOUNT_VALID_EXPR`），但**受限**授权（everyone 以外）只在挂载方笔记本自身未被共享时生效——这道未共享门堵的是把借来的参考库转手再分享，`tier='base'` 与 everyone 不受此限）。API 写端点一律经 `deps.py` 的 `require_notebook_capability("<能力>")` 声明能力（**9 个能力名**，值域冻结为 `{owner, admin}`，未知名 import 时 KeyError），体内自查用 `notebook_capability_allowed(capability, ...)` 吃同一张表；不得挂裸 owner 守卫，不得手写「owner∨成员」SQL 复刻。**能力两档（P2-T2 翻格，73 个端点声明一字未动，只改这张映射表的值与工厂判定分支）**：内容管理**六格**（`sources:write`/`kg:write`/`knowhow:write`/`knowledge:write`/`catalog:write` + `notebook:manage`＝`PATCH /notebooks/{id}` + 三个授权边端点）解析到 **admin 档**——⚠ 那个 PATCH 编辑的是**整份描述性画像**（`NotebookUpdate` 八个字段：`name`/`purpose`/`primary_domain`/`target_users`/`expected_questions`/`source_types`/`taxonomy`/`access_scope`）而不只是改名，「改名」是端点的简写、不是字段清单（codex #519 R10）；安全性靠「八个字段没有一个参与授权判定」（授权只在 `access_sql.py`/`mount_sql.py`，引用的是 `created_by`/`tier`/`notebook_members`/`notebook_grants`；`access_scope` 是描述文字不是访问控制列）与 `extra="forbid"` 挡住生命周期列两条，反向护栏 `backend/tests/test_notebook_update_authorization_free.py` 冻结字段集合——组管理员因此能经浏览器添加/删除/重解析来源、触发构建、写 knowhow/知识治理/命令目录；**恒 owner 的三格**是 `notebook:delete`（删库爆炸半径整本库且 owner 不可撤销）、`notebook:configure`（**挂载配置 + share_token 链接分享，P2-T2 评审 P0 拆出的新格**——它们是 owner 对本库检索范围与对外处置的配置，不随内容管理权转移：组管理员若能改挂载，`mount_sql` 的「同 owner 候选」安全论证即失效，他能经 `GET /notebooks/{id}/mountable` 列出库主全部**从未共享**的私有库名、`PUT .../bases` 挂进来经代理端点读全文，链接分享则能替库主铸对外链接让组外人整本 copy；故单列一格、不并进 `notebook:manage`）、`reports:write`（P1 起无端点消费——报告改 `require_notebook_read` + 体内行级 `reports.created_by` 判定，名字留给 P2/P3 组管理员批量管理动作，翻它既不放开也不收回任何东西）。**Agent/MCP 面刻意不翻**：`sources:write`/`sources:delete`/`maintenance:execute` 仍走 `mcp_server._writable_notebook` 的 owner-only 判定——浏览器 HTTP 面已放宽 admin、长期 Agent token 面没有，这是拍板取舍不是疏漏（token 的白名单可能包含 owner 事后才被授管理权的库；MCP 写工具删文档的爆炸半径正是这道 owner 门当初要防的，见「MCP 工具面与来源管理」）。守卫：`backend/tests/test_access_sql_contract.py`（双后端自省 parity/占位符方向/内联形状扫描/两段式 allowlist）与 `test_notebook_capability_guard.py`（AST 标识符扫描+空转保护+值域断言 `{owner,admin}`）。群组知识共享（设计稿 `docs/superpowers/specs/2026-08-17-group-knowledge-sharing-design_zh.md`）只改谓词与能力映射表，端点声明不动；翻转能力级别时须核对 `_CAPABILITY_LEVELS` 旁登记的**五个表外消费点**清单（Agent 面刻意不翻、`knowledge_routes.can_edit` 投影随表跟随、knowhow transfer 写半随表跟随、source parse/delete 体内自查随表跟随、前端 `NotebookSummary.can_manage_content` UI 信号须另行改）。**成员贡献审批流**（P2）独立成 `notebook_share_requests` 表、不进 grants 判定（判定谓词零 status 过滤）：普通成员对**自己 manage 的库**、向自己**只是普通成员**的目标组提交申请（组管理员分享进自己管理的组永远走既有 grants 端点、不经这张表），组管理员审批——批准在**同一写事务**里插 `(group, viewer)` 边并更新状态。状态机 `pending → approved/rejected` 单向，撤回是申请者走整行 `DELETE`（仅 `pending` 时）、不写第三个状态；`decided_at` 只写 SQL `NULL`/ISO 时间戳绝不写空串；`uq_share_requests_one_pending` 防同一 (库,组) 重复在飞申请，创建端点撞它**不是普通 409、而是幂等返回既有 pending**；`status` 消费方一律精确匹配 `pending`/`approved`/`rejected` 三值，绝不用 `!=` 当「已决定」判据（措辞平移 `principal_type` 同款红线）。**群组维度另有系统管理员运维旁路**（可读任意组详情、可管理任意组成员与授权边，不要求他是成员）——与「可转移 `notebooks.created_by`」同性质，理由是「最后一名组管理员」保护在并发窗口下仍可能留下零管理员的组（无旁路则该组在 API 里永远不可恢复）且 `?scope=all` 是管理面；旁路**不覆盖自助退出**、不伪造 `my_role`、不放宽笔记本维度任何守卫。群组可见性口径是 **404 不是 403**（组名本身可探测），唯一例外是 `POST /notebooks/{id}/grants`——那里「组不存在」404 与「你不是该组管理员」403 可区分（能走到这一步已证明对库有管理权、且组 id 是 128 位随机值，不构成枚举通道）。发边是**双重条件**（对库有管理权 ∧ 是目标组组管理员），且群组那一半的权威判定必须在 **store 的写事务里**——边一落库就立刻给整组人读权，先查后插的窗口足够让组被删或发起者被降级而边照发。撤销**不对称**：库维度删任意边、组维度删指向本组的全部边，各只需自己那一半权限。删组同事务清指向该组的授权行；`principal_id` 无外键，合库复活的孤儿边由 `scripts/merge_dbs.py::sweep_orphan_group_grants` 在 GLOBAL_UNION 之后、`foreign_key_check` 之前清扫（判据只认 `group`/`group_admins` 两个主体——`user`/`everyone` 的 `principal_id` 根本不指向 `groups`，一起扫等于删掉两类完全正常的授权），运行时兜底是 `list_grants` 给解析不出组的边打 `principal_kind="missing"`。前端全栈对等面（P1-T4，逐条不得回退）：①`GET /notebooks/{id}/mountable` 换 `MountableNotebook`，多一个 `origin ∈ {base, mine, shared}`（由 `MOUNT_ORIGIN_COLUMN` 从 `MOUNT_VALID_EXPR` 已有列信息投影，零新增查询），挂载选择器据它分三档——「读权⇒可挂载」放开后别人 owner 的库也进候选，只按 `tier` 分会把它们标成「我的笔记本」；优先级 base→mine→shared 保证既有分组逐字不变；该字段**只能**挂在新模型上，不得加进 `NotebookRef`（它同时是 `MountedBase` 的基类与另一条不计算此标志的查询的响应模型）。②`NotebookSummary.granted_via` 表达「经哪几个群组共享给我」，`access` **不新增枚举值**（群组成员就是 reader 那一档）；**列表与详情两条路径都回填**，去重口径**成员行优先**——既经链接加入又在被授权组里的人 `granted_via` 为空、「退出共享」仍可用。反过来 `granted_via` 非空的卡片**必须隐藏「退出共享」**：那个按钮只删 `notebook_members` 行，对授权边是空操作，点了而库还在列表里是必然发生的假失败，改为「由组管理员管理」的静态说明。③owner 侧「已分享」徽标与 `shared-by-me` 总览口径改为**并集**（`notebooks.is_shared` ∨ 有指向群组的授权边），`SharedByMeItem.group_count` 带组数；没有链接的行不算规模、不查成员（`mode`/`size`/`members` 说的都是链接）。④新增只读 `GET /notebooks/{id}/share`：打开分享弹窗**零副作用**、绝不铸 token、无 token 时**不算**规模统计，链接只在用户显式点「开启链接分享」时才 POST（幂等）。⑤未共享门关上的借入边有**自己的**文案与名字可见性（判别式 `MOUNT_GATE_CLOSED_EXPR` 由挂载谓词同源派生，消费点不许手拼）——此形态下挂载方 owner 对被挂库仍有合法读权，名字照常显示、文案给「取消本笔记本的共享即可恢复」的出口，而不是那句对借入边根本不成立的「该库已不是公共知识库，且不属于你」；分享弹窗还必须在**发出共享之前**提示借入参考库将暂停参与检索。⑥深度报告面对只读成员开放（`canManageReports` 恒真）并按创建者行级隔离，owner 也只见自己的；待确认中心铃铛的报告条目现在带**非自有库**的库名，且它的报告那一半在「有没有自有库」这道闸**之外**（该半的谓词只有 `created_by`、一个 notebook id 都不消费，闸内会让无自有库的成员铃铛恒为 0 而报告正卡在 `intent_ready` 等他确认）。⑦公开报告页 `GET /public/reports/{token}` 每次请求**实时复核创建者读权**（两个 id 显式传参，匿名 router 绝不碰 `current_user` 的 seeded-admin 回退），不通过与无效 token 同为 404（可区分响应会把组成员身份透露给匿名调用者），恢复授权即复活。⑧管理员用量总览的报告数按 `reports.created_by` 归集（不按笔记本 owner），与 `questions`／展开清单／活动流同口径。精确数值上限（组名/组说明字符数、三个清单端点无分页的口径、群组分区的 N+1 放大）只登记在 `docs/product-and-api*.md`。
- **大库检索 hydration 必须按候选有界**：ANN 后的孤立节点判断只能对当前候选做带索引的逐候选 `EXISTS`，返回候选 id，不得物化高出度节点的完整邻边；canonical fold 只能经 `cluster_fold_rows` 查询本轮 scored id，不能加载全 notebook cluster map；同一 scale-index 实例同一工件类型的惰性 ANN open 必须单飞。这三项只优化执行成本，不得顺手改 score、阈值、PPR 或召回。
- **引号即用户检索语法，真源是 `backend/app/core/query_syntax.py`**：英文半角双引号内的内容整体匹配、绝不分词。放在 `core` 是因为两端都要用而它们互相 import 不了（`app.repositories` 与 `app.services.retrieval` 经 ports 成环）。三条边界：只认 ASCII `"`（中文排版引号在散文里是普通引用，认它等于把大量既有提问悄悄变成带约束的）、至少 `MIN_LEXICAL_TERM_CHARS` 个字（SQLite trigram 索引更短的根本索引不到，识别了就是一句兑现不了的承诺）、**不同的引号内容超过 `MAX_QUOTED_PHRASES` 段则整段语法不生效**（数不同内容而非出现次数——内部检索问题会把同一段短语在目标/规范化问题/每条必答主题里各留一份，按出现次数数会在推理与报告里把语法整个关掉）——最后这条是形状判据不是预算：knowhow 智能补全的「查询」是 JSON 信封，每个键和格值都是引号跨度，取前 N 个既会凭空造出用户没提的约束，又会把 CJK 格值的三字片段召回路径剥掉。**被拒的跨度必须留在余量里**，遮蔽了又不用等于把那几个词从查询里删掉，精确请求变成召回窟窿。消费方：`lexical_recall_terms` 把短语放在词项表最前（在去掉引号字符的整句项之前），只对余量做分解；`probe_keyword_basis` 让通道按**它自己探测过的名称**打分且每个名称都是原子的（原子性是探测的性质，绝不按「有没有空格」推断——那会把 `config.yaml` 与所有 CJK 短语降级回 token）；`retrieval.KeywordBasis` 把每个短语当**一项**、整段子串命中才算覆盖（比较两段**已存文本**的调用方——如治理合并相似度——传 `honor_quotes=False`，文档引用别人的话不等于声明检索约束）；Memory 的候选是「整串当一个短语」探测,每段被接受的短语还要经 `phrase_queries` 作为额外 OR 词项进同一条有界查询(否则「只含该短语、不含整句」的记忆永远进不了候选池);`exact_probe_terms` 无条件放行短语（那把闸拦的是**顺带出现**的名称，用户的引号从不是顺带的），但模型给的 `exact_term` 走 `honor_quotes=False` 留在窄闸上；四处规划/反思 prompt 追加一句「原样保留引号内容」，且与 `SCOPE_DEIXIS_GROUNDING` 不同是**按需注入**——无引号的问题一个字都不该多付，深度报告是每节每步付一次。调用方已选定的名称经 `exact_probe_query` 逐个加引号交给精确通道：通道会对收到的串**重新**抽名称，裸空格拼接会把多词短语丢掉。无引号查询在以上每一处都必须逐位不变。`frontend/app/query-syntax.ts` 镜像这份解析，让提问框在提交前回执识别结果（没有它，不被识别就是一次静默失败），两侧由同一份用例表钉住（`test_query_syntax.py` 的 PARITY_CASES / `query-syntax.test.mjs`）。
- **词法候选属于索引检索契约**：SQLite FTS5 对每个用户派生 clause 单独按数据转义，可保留整句精确匹配加分，但必须 OR 拉丁字母/数字词项与重叠中文三字片段，禁止把多词查询只包装成一个强制连续短语；`_`/`-`/`.` 连接的完整标识符（`set_db` 这类）额外作为整体词项召回（trigram phrase＝精确子串），受「须含 ASCII 字母、长度 ≥4、至多 16 个、不超整句上限」约束，溢出共享词项预算时给 CJK 词项保有界尾部配额，不含标识符的查询词项集合逐位不变；PostgreSQL 在原生 trigram 候选生成前拆分同一组有界词项，并对 `ILIKE` 分支转义 LIKE 元字符（`%`/`_`/`\`），使 `set_db` 这类词项保持字面量、不退化成通配把 `setXdb`/`set db` 拉进候选；trigram 运算符与 `similarity()` 仍用未转义原词，转义只属于 LIKE 分支。**最终被探测的词项集合还要过一道语料语言闸**（`LEXICAL_LANGUAGE_GATE_ENABLED` 默认 true）：采样探针 `_notebook_langs` 判定该库没有任何 CJK 字符时，丢掉「每个字符都是 CJK」的词项——这类词项加空格补边后每个 trigram 都含 CJK 字符，对该库保证零命中，却在 PG 上各买一次真实 LATERAL 探针（7,026 块英文库实测：64 词项冷 29.7s / 3 词项暖 0.26s，返回同样 26 行；报告 4 节并发即撞 `POSTGRES_STATEMENT_TIMEOUT_SECONDS`，整条词法臂 fail-open 阵亡，代价不是慢而是**本该命中的拉丁词项一起没了**）。闸放在**消费侧**：由知道 `notebook_id` 的调用方把 `corpus_langs` 传进 `fts_search`/`chunk_fts_search`，且**永远传适配器即将查询的那个 notebook 的语言**（挂载参考库不能拿 active 库的语言过滤），绝不改写 `lexical_recall_terms` 自身的输出。三条不对称是硬性的：①只做 CJK 方向（拉丁标识符是中文文档里的常规内容，且空库探针本来就答 `["en"]`，镜像规则等于按猜测过滤）；②只过滤优先头**之后**的词项，用户引号短语与整句词项永不参与（引号契约红线，闸不能变成第二次更安静的截断）；③只丢「全 CJK」词项，不丢「含 CJK」词项（`abc时序` 与拉丁语料共享 `abc` trigram，无零命中证明）。两个后端必须过滤出同一组词项，否则同一个库的召回口径会随适配器分叉。语言判据是**采样**探针：唯一 Chinese 来源落在未采样的中段时会丢该库的 CJK **词法**召回（ANN/语义不受影响，且同一探针本来就决定要不要生成中文关键词），设 false 完全回退。**选定来源（`allowed_source_ids`/`source_filter` 非空）的运行一律不过滤**——那条路刻意跳过全库 ANN，词法是它**唯一**的候选来源，采样误判就不是「少一份召回」而是**整次检索零证据**；而且来源谓词在 LATERAL 内、LIMIT 之前生效，scoped 探针本来只扫选定来源那几行，这道闸要防的全库扫描在那条路上根本不会发生（风险最高、收益最低）。凡是能带来源范围的调用点必须**显式**写 `source_scoped=`，由守卫按形态钉住。带索引的 Chunk/KG 检索合并有界 ANN 与词法窗口；带索引的 Relation 检索通过 source/target 索引补入与词法命中 KG 端点相邻的有界有效关系，并保留 FTS 端点顺序、为两个关系方向预留预算，禁止高出度 source 饿死 target-only 命中。候选 id 与语义分 map 必须分开，纯词法候选按 keyword-only 计分，不得写入伪造的零语义分。
- **KG 边契约、补全与来源追踪**：内置 12 种边及核心四类端点只认 `app/domain/kg/edge_schema.py`（`app/services/kg/edge_schema.py` 只是 re-export shim，不得在那里声明新 EdgeSpec）；prompt、抽取验证、trust、graph/PPR/canonical/relation hydration、`follow_chain` 和工件版本都从它派生，非法 core→core 历史边只留审计、不进查询。已知边触及管理员扩展类型时保持兼容。`scripts/audit_kg_edge_contract.py` 只读且不得输出证据正文。跨元素关系补全默认 `off`；`shadow/write` 还必须命中 notebook allowlist 或稳定灰度，按模式和来源代次的持久 keyset 水位逐页处理。每个任务只 hydrate 有界对象/证据 ID 窗口，仅做索引化 relation `EXISTS` 与有界同源 FTS/ANN overfetch，受 section/pair/batch/字符上限约束；未完水位重新入队且启动恢复当前 pending 代次，模式切换用同一个 generation-CAS 事务先发布新模式可恢复游标再把旧 pending 游标标成 `stale`，无索引时保留水位并 fail closed。模型只能选服务端 id，独立 verifier 后在短事务复核 source/run/object/element 并保存 verifier 看到的同一 excerpt，稳定 relation id 保证重试幂等；非法零值护栏不推进，且绝不全扫整本书或整库。检索候选来源必须按 producer 累积，不能从最终 score 反推；`CHUNK_GRAPH_RESERVE` 只为已过原门槛的 graph-only chunk 预留既有预算席位，不扩 token/item 上限；新增保底席位必须走同一个 `select_with_reserves` rule list，不得另写并列 selector——各 reserve 共享一份预算、reserve=0 必须完全惰性、任何 reserve 不得逐出其他 reserve 刚保住的 chunk。
- **精确标识符通道（exact_lookup）**：问题里出现**可精确查找的名称**（`exact_probe_terms` 非空）才启动（`EXACT_LOOKUP_ENABLED` 默认开）：精确子串定位命中章节 → 取齐该节+子节 → `EXACT_SECTION_RESERVE` 在 mix 选择时给这些 chunk 预算内保底。**闸比词法召回窄**：`exact_probe_terms` 是 `identifier_terms` 的过滤视图，带 `_` 或 `.` 的一律放行，只用连字符连接的必须含数字（`GPT-4`/`v1-2` 留，`state-of-the-art`/`real-time`/`end-to-end`/`high-level` 拦）；**词法召回侧的 `identifier_terms` 保持宽定义不动**（多一个 OR 词项无害，多一次探测不是）。窄闸的理由是实测：这批词几乎出现在每个分析型问题里，报告引擎每节的 sec_question 恒抽出一个＝每节白发一次探测（2 万块库 16ms/50 命中），`real-time` 命中章节标题时整章 12 块以 1.0 分进证据。这把闸就是成本合同——无此类名称的问题零新增查询；每步探测受 `EXACT_LOOKUP_*` 硬界约束，SQLite/PG 两侧 LIKE/ILIKE 必须转义 `%`/`_`/`\`（命令名天然含 `_`）。**命中先按「最近的 identifier-named 面包屑前缀」折叠成组再分 slot**：按面包屑各自排名会让一条命令的子节吃光名额（实测 `set_db > Arguments` 4 命中 / `> Examples` 3 / 主节 2 / `report_timing` 2，三个 slot 下 `report_timing` 整条丢失），而「子节块数多于主节」是参考手册的常态。组键 identifier-named → 对组键做子树取齐；**否则按命中 chunk id 直接 hydrate，绝不按 `(source, section_path)`+LIMIT 重查**——面包屑只有 markdown 解析路径才有，MinerU 解析的 PDF/DOCX 下全库几十个裸「Arguments」节共享同一 section 键，重查只拿回前 12 个、把探测已经找到的命中块丢掉。章节取齐的兄弟 chunk 刻意不套 `RELEVANCE_FLOOR`（准入是结构性的且有 sections×chunks 硬界，分数保持诚实 [0,1]，套地板会把特性要救的低分参数表原样丢掉）；**通道分是「对本次实际探测名称的覆盖率」，不是问题相关度**（按调用方 query 打分会让 mix 报 0.2857、reasoning 报 1.0，同一块证据两个口径）。通道只作用于当前 active notebook，挂载 base 联邦刻意未做。**逐步推理侧接两处，覆盖 `reasoning` 问答与深度报告逐节检索（报告引擎逐字复用 `ReasoningRetriever`），graph 模式不接**：①初检索之后无条件跑一次确定性 seed pass（镜像 PPR seed pass，不赌 agent 选动作），按抽出的名称本身（而非整句问题）打分——与 action 同构、也是 producer-native 打分的既有惯例，整句打分会把命中节的相关度拖到锚点阈值以下——记 `exact_lookup` 轨迹步（前端标签「精查」），排在 PPR seed **之后**以保 PPR 计数逐位不变；②reflect 白名单加 `exact_lookup`（参数 `exact_term`）。动作与 seed 共用 `exact_probe_terms` 这把闸（低选择度短串、纯连字符英文词组都直接 skip——那是按实测定标的成本闸，不能交给模型）、共用按名称的防重账目（seed 查过的名称 agent 不必再查）、只探测本轮新名称，agent 主动调用每 run ≤ `_MAX_EXACT_LOOKUPS = 3`（seed 不占额度）。**四类 skip（未启用/缺名称/非标识符/超上限）都写入同一份账目并回喂教学措辞**（措辞写成「该给什么」而非「你给错了」：「「2.1」不是可精确查找的名称(要像 set_db、config.yaml 这样带下划线或点;只用连字符连接的词还需带数字,如 GPT-4)」——只说非法，模型下一轮往往换一个同样非法的普通词再试），按理由/名称去重，不是只留 TraceStep 沉默；`exact_term` 解析期只去包裹标点不截长，截长挪到使用点，好让 fail_closed 的 2000 字符硬闸对它真正生效。两条路径都必须过 `_filter_candidates`，且受 `allow_exact_lookup` 策略位（镜像 `allow_ppr`）约束——knowhow 智能补全关闭它，否则策略边界会被开后门。动作名与 prompt **不随 flag 改写**（沿用 `ppr_retrieve` 先例）：flag 或策略位关时在执行处 skip、零 I/O，不从 prompt/白名单剔除。
- **大库冷加载前置 readiness**：默认 `STARTUP_PRELOAD_SCALE_INDEXES=true`，服务必须在 `/api/ready` 前加载全部已发布 scale 索引、启用的 ANN handle 与可安全复用的单索引 PPR core；加载失败或索引数超过 `SCALE_IDX_CACHE_MAX` 必须 not-ready，不能把工件冷成本留给首位用户。单一 self-index 组合图复用原 node list/map/idf/PPR core，且 core 随专用 ScaleIndex LRU 常驻；不得在启动时全量物化跨库 mounted 组合图（现有拼接会成倍复制千万节点图并 OOM）。严格保证只覆盖启动时已经发布的工件集合；运行中 build/fold 或新增第 `SCALE_IDX_CACHE_MAX+1` 个索引仍走既有在线路径，稳定部署应提前扩容并重启以重新建立 readiness 保证。关闭 preload 只用于修复坏索引的临时恢复。
- **Scale/viz 热路径辅助索引与后台队列**：新建的 scale/standalone viz 工件必须持久化稳定度序以及按 source 的边 permutation/indptr；旧 compact/JSON 工件继续可读，只允许首次使用时派生一次。有界核心图只能遍历 kept source 的邻接段并恢复原边序，不能再分配全图 membership mask 或扫描所有边。多 participant PPR 组合必须保持历史有序左折叠语义：最后一个 participant 之前的重复坐标先折成结构存在，最后一个 participant 与既有结构重叠时保留归一化前的双权重；最终 CSR 只构造一次，并用 PPR 分数/排序差分测试钉住。重活与轻活使用两个固定 worker 队列；submit 在发起线程捕获 ContextVar 并返回可 join 的兼容 handle，但排队中的每一项不得各自创建一条阻塞线程。冲突治理分别按对象数与关系数准入，worker 内再读 relation 哨兵封住预检竞态；超限整轮跳过，绝不能把前缀冒充完整检测。精确 rail 只写 `docs/product-and-api.md` / `_zh.md`。
- **生产启动生命周期**：`npm run start` 默认先把 `backend/requirements.txt` 安装到 `PYTHON_BIN` 环境，并以 `npm ci` 按 lockfile 重建前端依赖；只有明确预装依赖的部署才用 `SKIP_INSTALL=1`。安装前以监听行判定端口占用，不得因 `ss` 看不到 PID 就当端口空闲。随后在前台 build，再用脱离 terminal 的后台进程拉起前后端后立即退出，不探测 readiness 或前端 HTTP；运维方自行校验 `/api/ready` 和前端。脚本完成两个子进程交接前若中断，必须同时 SIGTERM 两个直接子进程，有界等待后 SIGKILL 残留并 `wait` 回收；交接后正常停服只走 `npm run stop`。
- **检索索引立即构建覆盖排队**：手动 `when=now` 只有在原子认领到新的立即构建时才移除同 notebook 的旧 idle 项；已有构建保留后续排队，认领后的新 idle 项也保留，daemon 启动失败则恢复被覆盖项。idle scheduler 必须逐项认领，禁止先清空整批：忙碌项继续排队，单项启动失败不得丢失或阻断其余项。`AskResponse.index_required` 只是回答时快照，前端必须结合实时 `ScaleIndexStatus.exists`；有界前台轮询结束后仍由 `index_done` 刷新当前 notebook，索引发布后立即隐藏历史降级提示。排队态披露队列项数、入队时刻、下一个空闲窗口开始时刻（UTC 下发、前端本地显示）与上次构建总耗时；位次是入队序而非等待顺序（窗口开启后并行启动）；铃铛对 queued 透传、不再显示成构建中。
- **LLM 响应缓存是 opt-in**：`chat_json` 的内容寻址缓存默认开，但**只有传 `response_validator` 的调用方才读写它**——不传就既不读也不写（对调用方透明、正确性保留、只失去性能）。占成本大头的 KG 抽取三处传 validator 保持缓存；Ask、paper_meta、summary 等不传的调用方刻意不缓存，一次关掉「偶发坏值被固化整个 TTL」的投毒类。**插件索引管线的 KG 策略调用（`kg_ingest._plugin_extract_window`）同样刻意不传**——插件自定的响应形状 core 无法核验，缓存准入没有可用的 validator，代价是插件管线的每次重建按全价重跑模型（已登记取舍）。每个 chat `WorkloadSpec` 必须显式选择思考默认值：`ask_answer`/`reasoning_agent`/`graph_chain_verify`/`report_outline`/`report_sufficiency`/`schema_induction`/`agent_profile_consolidate`/`retrieval_experience_distill` 这组有界、高杠杆调用开启；其余当前 chat workload 全部关闭，包括所有 KG 阶段、chunk 问题生成、元数据/摘要抽取、改写/筛选、报告分节与最终审计、Memory 预览、Knowhow 转换/补全。部署覆盖只能写在 model-services TOML 的 `[thinking]`，按 workload 接受 `enabled`/`disabled`/`provider_default`，随 registry 热加载；未知项、非 chat 项或非法值直接拒绝配置。解析后的模式对所有已绑定 chat 服务都是权威：`enabled`/`disabled` 经 `extra_body` 发送为 `thinking.type`，`provider_default` 不发送覆盖，任何 workload 都不发送 `reasoning_effort`，provider 与 transport 两层都不得检查模型名来决定模式是否生效；提交调用时必须把物理路由与解析后的模式一起冻结。显式模式必须进缓存身份，禁止跨模式复用；`provider_default` 保持历史缓存键。健康探针不进入 workload 策略，固定同时传 `bypass_cache` 与 `thinking_mode="disabled"`，不接受 TOML 覆盖或模型名例外；admin 清缓存 `tag` 与 `clear_all` 二选一（同时传即 400，绝不静默全清）。UI 上传按内容哈希做**同 notebook 内**去重（对齐 `batch_ingest`）；同内容不同后缀重传复用既有源、保留原解析（要换解析器请删除该源再重传）。本特性追加迁移 v30（`sources(notebook_id, file_hash)` 去重索引）。
- **构建任务必须落终态**：KG 构建的每条退出路径都要把 `kg_build_jobs` 那行写成终态，**包括 Ctrl-C/SIGTERM**——它们继承 `BaseException`，`except Exception` 接不到。留在 `running` 会被 notebook 级条件唯一索引把后续每次构建挡成 `KgBuildAlreadyRunning`，界面还一直显示「分析中」；启动期崩溃兜底只属于服务端 lifespan，离线 CLI 刻意无权自清，只能等后端重启。中断要 abort run control 停掉在飞窗口并**先排空再落终态**（守卫是跨进程的，随那行一起释放：不排空，活着的后端可以起新分析，而垂死进程的 worker 还在写图，会把新任务状态冲掉；顺带线程池 atexit join 也会把进程按住不退）。所有 `kg` 抽取形态统一接管 SIGINT/SIGTERM/SIGHUP：第一次信号启动收尾，重复信号在原 handler 还原前被吸收，不能再次打断 extraction drain 或 finalizer executor shutdown。失败事件只在 `finish()` 真的把 `running` 落成 `failed` 时才发（信号可能落在成功提交之后）。但**不得**记 `kg_build_circuit_opened`——那是模型熔断，人工中断不是。真源见 `AGENTS.md`「Architecture Baseline」。
- **离线跳过模式**（`batch_ingest.py kg --skip-model-failures`，默认关、API 路径够不着）：模型不可用只中止**该来源自己的子控制器**（`KgExtractionRunControl.child()`），来源退回 `parsed` 故重跑即重试，任务继续。三条不可省：①任务级熔断（中断、别的 worker 升级）必须原样上抛，绝不能降级成「跳过这一个」——判据是 `control.aborted` 复核，少了它跳过模式下的任务再也停不下来；②兜底阈值 `--max-consecutive-model-failures` 必须**高于** `--workers`，且这条关系要**强制**（省略即按 `max(32, 2×workers)` 派生，显式给出不高于并发度的值直接报错、不静默抬高）——一次瞬时抖动会同时打中所有在飞来源，阈值不高于并发度就是单次波动即误判「服务已挂」；起始可用性探测刻意**不**在跳过范围内（服务开跑就是死的，快速失败比逐源白烧重试预算再凑满阈值更省，反向护栏 `test_skip_mode_still_fails_fast_when_the_startup_probe_fails`）；③该计数是**任务级**、跨目标分页存活——目标按 500 行原始来源翻页，稀疏库每页可能只有一个目标，按页重置的计数永远到不了阈值，兜底就形同虚设。显式降级须打告警，跳过数与有界 id 在成功/升级熔断/中断三条路都要报。
- 不引入 Docker 作为一期默认工作流；装新包前先问。
- **打开笔记本的单一 transition**：打开编排由 `frontend/app/notebook-transition.ts`（纯逻辑，无 React/DOM/网络）的**一条**确定性流程承载：全部 `begin` 先按声明序跑完再判拒绝 → `enter` → `load` → `isCurrent` → `apply` → 各步可选 `commit`（按声明序）→ `conclude` → 对**已 begin 成功**的步骤按**逆 begin 序** settle 恰好一次（成功传 outcome，拒绝/取数失败/被顶替/异常一律传 `null` 回滚，异常再原样抛出）。被顶替的旧 transition 只 settle 自己铸出的那批 ticket，绝不碰更晚 transition 刚建立的 owner。`page.tsx::notebookTransitionSteps` 是各 owner `begin`/可选 `commit`/`settle` 的**唯一登记点**（接入前同一个 owner 要在 `openNotebook` 的四处各补一笔：begin、拒绝分支的 finish、成功路径的 commit、`finally` 里带 `opened` 的 finish，漏一处是静默失败），新增 owner 只加一项；root-modal 那一步必须排第一——它的 close sink 是暂存文件/bundle 勾选 resolver/迟到解包世代的唯一清理路径。`openNotebook` 只剩自己的 prologue 与 plan 声明，四个相位落在具名 helper 里。请求数（notebook + 首个来源页并行一次）、`workspaceEpoch` 语义与两次守门的逻辑位置、迟到响应丢弃、tombstone、`#notebook=<id>` 历史处理与失败落点逐字不变。回归门 `frontend/tests/guards/notebook-transition-guard.test.mjs`（编排函数体内出现任何 owner 生命周期调用即红）与 `frontend/tests/unit/notebook-transition.test.mjs`。
- **来源状态 owner**：`frontend/app/use-source-library.ts` 独占来源行/本库检索选择、分页搜索、详情元素、重解析/删除 busy、删除 tombstone 与解析轮询。`page.tsx` 必须继续并行读取 notebook + 首个来源页并显式提交稳定快照（现分别由上条 transition 的 `load` 相位与来源库那一步的 `commit` 相位承载），不能改成 effect 再拉一次；已发出的写请求可安全完成，但只有 exact user/notebook/workspace owner 仍有效时才能写 UI。切库/换用户同步失效旧列表/详情/轮询/timer。hook 只暴露 readonly view、具名 command 和窄刷新 effect，绝不暴露 raw setter、万能 context 或 Ask/Report/KG/collection/modal owner 的 setter；请求数、删除 A→B→A 收敛、tombstone、代理详情读取与轮询节奏不得变化，不引入全局状态库。详细真源见 `AGENTS.md` 的 Frontend Workspace State Ownership。
- **Ask 状态 owner**：`frontend/app/use-ask-session.ts` 独占 Ask 草稿/对话、意图预检与确认、持久 stream/reconnect、会话历史/tombstone、重命名/删除/批量清理和回答反馈。切库/切会话只 detach 已发出的 durable job，绝不自动 cancel；只有显式停止才取消，若尚未收到 `started`，必须继续读取到 job id、恰好一次 cancel 成功后再本地 abort。同步取消端点没有可强制的整请求数据库期限，因此浏览器只保留一条权威取消请求，直到服务端明确响应；不得用客户端计时器提前中止请求或释放重试权。预检与确认执行复用同一份冻结 source/base scope。hook 以 exact actor/notebook/workspace owner 拒绝迟到可见提交，同时允许返回同一 actor/notebook 时校准 durable history；只暴露 readonly view 和具名 command，不接跨域 setter/万能 context，不改变既有请求数、轮询节奏或 Memory answer-link 批次。详细真源见 `AGENTS.md` 的 Frontend Workspace State Ownership。
- **报告状态 owner**：`frontend/app/use-report-workspace.ts` 独占报告列表/详情、按需首读、列表/详情轮询、意图/大纲提交、生成/取消/重试、分享、导出选择、删除 busy 与 tombstone。打开 notebook 或非报告页签保持零 report I/O；进入报告页才恰好拉一次列表，待确认中心焦点在列表 settle 后至多拉一次详情。导航只 detach 后台报告任务，不自动 cancel。exact actor/notebook/view owner 拒绝迟到可见提交，成功删除无论当前 generation 都写 actor+notebook tombstone，A→B→A 不复活；创建时冻结 source/base scope，每个写命令重验 live manage policy，六秒列表/详情轮询互斥且请求数不变。hook 只依赖 report/pure contracts，只暴露 readonly view 与具名 command，不接跨域 setter/万能 context，不引入全局状态库。详细真源见 `AGENTS.md` 的 Frontend Workspace State Ownership。
- **KG 状态 owner**：`frontend/app/use-kg-workspace.ts` 是三个各自可独立测试的领域 owner 之上的**薄组合层**——`use-kg-knowledge.ts`（Knowledge 行/类型/筛选/分页/重复项/上下文）、`use-kg-schema.ts`（Schema view 与 mutation）、`use-kg-graph.ts`（统一图搜索/范围/节点、待合并审阅/tombstone，以及持久 KG build/relink/rebuild 追踪），共用 `use-kg-owner.ts` 里唯一那份 actor/notebook/generation 门；四者合起来仍独占上述全部状态。三个领域彼此不 import，门不持有任何领域状态、也不 import 任何 API 模块：owner 变更时的清空、invalidate 与新 owner 认领，一律由组合层把门的扇出路由进各领域自己的**具名 command**，绝不是把某个领域的 setter 交给另一个领域。打开 notebook 不读取 Knowledge/Schema/图内容，只保留既有维护状态恢复探针（图谱领域的 `adoptOwner`）；内容读仍由显式入口惰性触发。exact actor/notebook/generation 拒绝迟到可见提交，actor+notebook identity 保留 A→B→A 的维护认领与合并决定 tombstone。每条写命令重验 live Knowledge/Schema/KG 权限，只读成员没有审阅控件或 review-job 恢复请求；轮询维持既有节奏并单飞。四个 hook 都只暴露 readonly view 与具名 command，不接跨域 setter/万能 context，不引入全局状态库；`page.tsx` 看到的仍是一份命名空间视图（`.knowledge` / `.schema` / `.graph`）加扁平命令面，领域内部的扇出 command 不出组合层。详细真源见 `AGENTS.md` 的 Frontend Workspace State Ownership。
- **集合状态 owner**：`frontend/app/use-notebook-collection.ts` 独占 actor-scoped notebook rows、有界集合搜索、筛选/排序/视图/菜单、issued/published 清单水位、focus/visibility 访问权对账、collection editor/delete、默认创建 single-flight 与删除 tombstone。壳层仍原子编排 health + list + system-config composite bundle，并只用 opaque list ticket 提交清单；打开 notebook 的 paired notebook/source 读取不变、没有新增 collection effect I/O。搜索保持 250ms 与全局有界扇出，写动作在阶段间重验 live row authority，成功删除先写 actor tombstone，A→B→A 与旧 list 都不能复活卡片。hook 只暴露 readonly view、具名 command 与窄 shell effect，不接 modal/sharing/source/Ask/Report/KG owner 或全局 store。`use-root-modal-coordinator.ts` 另行独占 typed presentation lease、actor/workspace/source scope、层级/冲突/topmost 与焦点归还；它不接领域 payload/API/busy/timer，异步 opener 必须先冻结 ticket 再按 exact owner publish，切库/换用户同步隐藏，info overlay 与 primary 合法共存。详细真源见 `AGENTS.md` 的 Frontend Workspace State Ownership。
- **owner hook 的隐藏态视图与扇出入口**：六个 owner hook 在 owner 不可见时返回的每个回退值都必须是**稳定引用**（模块级 `Object.freeze` 常量或 per-instance ref），绝不能是裸 `[]`/`{}`/零参 `new Set()`——消费方 effect 以它们为依赖，每帧新引用会让 effect 每帧重跑，page.tsx 的 Memory 链接 effect 还会在里面 setState，直接变成无限循环（PR #557 的真实回归，Next 报 `Maximum update depth exceeded`）；该 effect 的重置也必须带 bail-out，不靠依赖稳定单边兜底。actor 激活与离开对所有 owner hook 的扇出只有 `page.tsx` 里 `activateWorkspaceOwners` / `leaveWorkspaceOwners` / `leaveActorOwners` 三个入口（前者两个认证点共用，后两者分别服务回集合页与登出、成员刻意不同），新增 owner hook 只改那里。owner 视图不得在 `Home` 顶层再摊平成局部别名（逐字段摊平、或对命名空间别名二次摊平皆算违规；唯一放行的新写法是命名空间级解构本身，回归门 `frontend/tests/guards/owner-view-no-reflatten-guard.test.mjs`）。回归门是 `frontend/tests/guards/hook-view-stable-empty-guard.test.mjs`（语义 AST、含 `as`/括号/`??` 解包与局部变量形态）与 `frontend/tests/guards/workspace-owner-transition-guard.test.mjs`（这些调用只能从三个入口发出），各 hook 的组件测试另钉跨重渲染 `Object.is` 稳定。
- **Workspace UI registry**：`frontend/features/extension-sdk` 是唯一 build-time registry/host；canonical slot 只有 `workspace.side_panel` 与 `source.detail_section`。首个真实 contribution 是既有 Agent Profile 入口：插件本身点击前 0 领域 I/O，只经 exact-owner `openUnderstanding` 窄 action 打开既有 modal/data owner。`workspace.side_panel` 的落点是**来源栏固定区**（滚动的来源列表之上）里的一行入口，复用既有按钮类与 `:root` token，不写颜色字面量；它**绝不给工作区加独立一列**——那一列只放一个入口、其余整列空白，还把问答面板挤窄（来源栏收起时入口随栏隐藏——视觉隐藏但仍可 Tab 聚焦，与整个来源栏既有机制一致，已登记接受）。成功提交 workspace 后每个 actor generation 共享 1 次 `/system/extensions`；请求失败不缓存，下一次 workspace 提交（切库等）会重新发起，不引入 timer/轮询。集合页/未登录/空 registry 仍 0 I/O，同用户切库复用投影但以 actor/notebook/workspace generation 同步隐藏旧入口并拒绝旧 action。本地精确 tuple、实时 availability、核心权限、normalized UI mode、当前 workspace owner 五门全真才渲染。禁止远程 JS、运行时扫描/注册、全局 store、raw fetch、page/其他 hook setter 或 service locator；后端投影不得下发 capability 名、内部 reason、依赖拓扑、endpoint/path/credential/异常。跨栈 parity 必须保持 non-vacuous。新增 contribution 后跑 `scripts/generate_ui_extension_contract.py` 刷新 `backend/tests/fixtures/ui_extension_contract.json`，contracts lane 以 `--check` 对账。**构建期私有 UI 插件包**的注入链是 `registry.ts`（内建，闭包全 `.ts`）→ `workspace-registry.ts`（合并）→ 生成且被 `.gitignore` 忽略的 `registry.local.ts`，由 `SILICON_NOTEBOOK_UI_PLUGINS` 与 `npm run sync:ui-plugins`（`postinstall` + 五个 `pre*` 钩子）产出。插件只许 import `contracts.ts`/`ui.tsx`/`react`/`lucide-react` 与同包兄弟，绝不得 import `api.ts`——端口按 `pluginId` 由 host 经 `actions.api` 注入，插件自己 import 不到就造不出别的插件的端口。插件不写 CSS、不内联颜色字面量（视觉复用既有类与 `:root` token），也不读 `error.message`/`.error`（`errors-guard` 精确计数普查登记不进仓库外的包），用户可见文案走 `api.userMessage`。`refreshSources` 与 `openUnderstanding` 同为 exact-owner 窄 action，只在自己动作完成后调一次并 `catch` 住 rejection；仍禁远程 JS、运行时扫描/注册、全局 store、raw fetch。**插件弹窗经一格通用的 `"extension"` slot 接入核心 root-dialog 裁决**——核心的 `RootModalSlot` 联合类型里不出现任何插件名（否则每装一个插件就要给公网仓库打补丁）。认领由壳层按 **contribution id** 记录（不是 plugin id：一个插件可以注册多条 contribution，按 plugin 判会让它们一起挂出弹窗），host 只把 `context.dialog`（`{ open, topmost, zIndex }`）投影给持有它的那条，其余恒拿 closed view。插件因此不留弹窗的 `open` 状态：`actions.openDialog()` 申请、`context.dialog.open` 是答复、`actions.closeDialog()` 释放，冲突的 primary、切库与换用户都由协调器替它关。`openDialog` 必须先由 `rootModals.open` 发出 lease 再记持有者，持有者只经协调器的 close sink 清空，`closeDialog` 对非**当时**持有者（判据读 ref、不读渲染闭包）一律拒绝——一次迟到的回调关不掉别人的弹窗。`ExtensionModal` 在 `dialog.open` 之前什么都不渲染，`dialog.topmost` 为假时加 `inert`/`aria-hidden`，并把协调器的 `zIndex` 作为它唯一被放行的内联样式落到 DOM；焦点归还仍归协调器。`source.detail_section` 的 contribution 现由宿主结构性拒绝开弹窗（codex #578 R4 P2，不再只是约定）——那个宿主自己就是 `FloatingModalCard`（`position: fixed` 相对它解析），且握着与之互斥的 `source-detail` primary lease：该槽位下 `actions.openDialog()`/`closeDialog()` 是不触达壳层的拒绝函数（开发模式打一句 `console.warn`），`context.dialog` 恒为冻结的 CLOSED 视图；`ExtensionModal` 自己在该槽位下直接 `throw TypeError`。新增只读管理员页 `/admin/extensions` 是 `GET /admin/extensions` 的前端对等面，列出实际加载的扩展拓扑供运维查看。
- **Root dialog 顶层裁决**：来源详情也必须加入 coordinator 的同一 frozen primary issue 水位；来源目录审阅可作为其兼容上层，任何被覆盖 dialog（含来源详情与图谱分析）都必须 inert/aria-hidden，不能继续接收后台键盘交互。归还焦点必须等 React 提交并移除 inert 后，在 layout effect 中复核预期底层仍为 topmost；禁止用 timer。
- **浮动弹窗**：新增的居中浮动弹窗要可拖标题栏移动——复用 `frontend/app/use-floating-window.ts`（`page.tsx` 内联弹窗走 `FloatingModalCard` 包装：只接管卡片、把 `dragHandleProps` 交给标题栏），不要另造一套拖动实现；侧边贴边抽屉、锚定 popover、全屏视图除外，窄屏（<720px）自动停用。真源见 `AGENTS.md` 前端章节。
- **长任务按钮的忙碌态**：点一下就触发长任务的按钮（排后台 job、同步等完的重活、大体积上传），点完必须**立刻**不可点并换成按该动作语义写的进行态文案（「补齐向量」→「补齐中…」，不是笼统的「处理中」）；纯图标按钮改用 `.busy-spin` 转圈并同步 `title`/`aria-label`。理由是硬的：`backfill-vectors`/`reparse` 后端**没有单飞守卫**，POST 立即返回而活在后台跑，按钮不变化用户就反复点、每点一次就再排一份同样的活。两种形态二选一：`disabled={busy}` + 文案切换，或忙碌时整排 CTA 换「取消」/不渲染；不能什么都不做。解除忙碌位要**按证据**，且证据按**修复形状**选、不能一刀切（真源=`frontend/app/checkup-view.ts` 的 `repairRelease`/`isRepairing`，有单测）：逐轮修一批的（`reparse`）存触发时的 count，count 一变就恢复可点；一次修全库的（`backfill_vectors`）⚠**不能**用 count——H4/H5 口径排除活跃租约、job 跑的过程中 count 就递减，按它解锁会排出并发全库补齐（正是后端 `checkup.py` H4/H5 注释点名要防的重复模型调用），只在该组从体检结果消失时解除；⚠ 另注意 H4/H5 走带 30 秒 TTL 的进程内 memo，**补齐完成到计数归零之间还可能多等至多 30 秒**，忙碌位晚一个 TTL 解除是刻意接受的（提前放行 = 重复付费模型调用）。**按笔记本单飞的后台任务（「补上关联」「重新合并」）的忙碌位存的是「哪个库在忙」而不是裸布尔**——裸布尔会在「点完 A 切到 B」时让 B 的按钮变灰、让 A 的轮询刷 B 的图谱并清掉 B 的展开节点、回 A 又永远等不到刷新；判据/解除是纯函数 `busyForNotebook` / `claimNotebookSlot` / `releaseNotebookClaim`（`frontend/features/kg-maintenance/notebook-busy-set.ts`，两个动作共用同一份实现、有单测；`kg-relink-status.ts` 只是按历史命名再导出一遍），解除必须是「只清自己那一格」的函数式更新。⚠ 服务端把这两件事放进**同一个** per-notebook 单飞槽（重新合并整表重写 `concept_clusters` 与板块划分，补上关联只是把关系边追加进这张聚类正在读的同一张图——两者共槽是因为并发不只是多花一份钱，而是一方会在另一方还在读的输入上发布结果，不是因为两者做的是同一件事）：409 必须点名真正占着槽的那个动作，而各自的 status 端点对**另一种**任务一律如实回 `idle`，否则一条轮询会替它没发起过的任务空等到上限。轮询另需**尝试上限**（服务端只在进程内记状态，任务卡死时会一直如实回报 running），超限用中性文案。忙碌位还要随体检结果收敛（摘掉已消失的分组键），否则旧条目会被重新出现的同名问题继承、锁死新卡片。有界轮询窗口关闭与切库是共用兜底。⚠ `.sort-button`/`.icon-button`/`.ghost-button` 写死了 background/color，浏览器默认 `:disabled` 变灰对它们无效——`globals.css` 那条共用 `:disabled { opacity: .55 }` 是让禁用看得见的必要条件。真源见 `AGENTS.md` 前端章节，回归门是 `frontend/tests/guards/long-task-button-guard.test.mjs`（按 `onClick` 源码文本认入口、不含行号；`disabled={false}` 这类恒假值同样报红）。
- **添加来源弹窗**：待上传文件名超过 48 个 Unicode 字符时中间压缩，保留末尾/扩展名并用 `title` 暴露全名；CSS 仍需在窄窗口省略保护。拖放与文件选择共用同一套入列校验（`classifyStagedFiles`）：拖放必须由前端 `onDrop` 显式接管——不能交给铺满拖放区的原生 `<input type=file>`，浏览器会按 `accept` **静默**过滤不支持的文件；入列时被跳过的文件（类型不支持——旧版 Office 另给「另存为」引导、超单文件大小上限、超单次批量上限）必须在弹窗内逐条持久列出文件名与原因，只发 2.2s 即逝的 toast 不算给了提示。确认上传前必须把整个暂存批次计入当前笔记本的有效文档上限，超过剩余名额时按钮直接置灰，并在按钮附近写明还能上传几个、需移除几个；防御性 handler 也要复查。服务端的**权威**容量强制在建源写事务内（上传与 URL 导入两端点：SQLite BEGIN IMMEDIATE / PG notebook 行锁后 COUNT+INSERT；`/sources/import` 无前端调用方、刻意只留预检），端点预检 409 只是便宜的先手，并发抢占最后名额的输家在事务内被拒并拿到同一句文案；**事务内**去重重查在容量闸之前（闸绝不拒绝复用、复用不占名额），Agent 面先探内容哈希后预检、满库重传仍复用，而浏览器上传端点保持既有行为——在消费任何 multipart 字节（因而也在能算哈希）之前就拒绝已满笔记本。单文件大小只认部署变量 `SOURCE_UPLOAD_MAX_MB`（整数 1–1024，默认 50，1 MB = 1024 × 1024 字节）：Settings 派生字节上限，经认证的轻量系统配置接口下发给选择器即时拒绝，上传端仍以携带当前上限的流式 413 作权威兜底。Next.js 15 的 external rewrite 默认只克隆 10 MiB，因此 `next.config.mjs` 必须从同一个单文件设置、固定 20 文件批量护栏和有界 multipart 余量推导整次请求的 `experimental.middlewareClientMaxBodySize`，绝不新增第二个用户可见限制；源码/dev 构建读取 launcher 导出的根 env，离线 standalone 包因目标 `.env` 属于运行时配置而按协议最大值构建传输层。前后端还必须固定限制每次 multipart 最多 20 个文件，后端在解析流时执行，避免单文件额度累积成无界 spool；类型元数据只接受预期的两个字段，最多 40 个标量 part、每项最多 4 KiB，未知或超额字段直接拒绝。最后一排操作按钮与卡片底边保持明确留白。
- **五档强度控件**：深度报告的「研究深度」与逐步推理的「检索档位」档名相同，必须复用 `frontend/app/effort-picker.tsx` 的 `EffortPicker`（chip + 滑块 popover + 该档一句说明），不得再造平铺 chip；档位的**数值阈值不上屏**，精确上限只在 `docs/product-and-api*.md` 的契约表。popover 必须打开时量一次再夹回最近的裁剪祖先（静态锚点做不到：`right:0` 与 `left:0` 各只在一段宽度成立），夹取算术在 `effort-picker-logic.ts`。真源见 `AGENTS.md` 前端章节，回归门是 `frontend/tests/unit/effort-picker.test.mjs`（含「滑块只许存在于共享控件」的移动变异守卫）。报告的「研究深度」现与逐步推理的「检索档位」共享同一张预算表（PR-5，见上面「大纲便签与按节合成」条），控件复用不变，只是背后的数值贯通了。
- **群组界面版式**：现行入口是独立页 `frontend/app/groups-page.tsx`；旧 `groups-panel.tsx` 不再路由，只保留为兼容遗留。独立页复用 `globals.css` 的系统变量与现有按钮/徽标语言，桌面为左侧群组导航 + 右侧工作区，窄屏自然折叠；Notebook、成员、申请、设置四区不使用临时内联样式拼装。独立页另有五条版式不变量：⓪**页面标题在顶栏**——「群组」就是顶栏 `.brand-title` 那一格（SN 徽标旁，16px 不变，替换掉该视图下的 `silicon-notebook`，副标题改 `成员、共享与审批`），页面里因此不再有 `<h1>`；那一格在群组视图下必须**真的**是 `<h1>`（否则整页没有标题层级），`.brand-title` 必须写 `margin: 0`（h1 默认 0.67em 外边距会把 56px 顶栏顶变形），页面顶部留白同时从 44px 收到 24px。⓪′**「返回主页」与笔记本工作区共用 `.back-home-button`**（原 `.notebook-home`，声明逐字不变）——两处都调 `showCollection()`、都回笔记本列表，同一个动作不许长成两种样子；改类名要同时改 `page.tsx` 与 `groups-page.tsx`。①**测量线与「笔记本列表」一致，且量在不带内边距的块上**——1200px 挂在 `.group-page > *`（同 `.collection-title`/`.notebook-grid` 的做法：它们是 `.page` 的子块、自身无内边距）；**不得**挂在 `.group-page` 上——它继承 `.page` 的 `padding: 44px 24px`，全局 `box-sizing: border-box` 会把那 48px 吃进 max-width，内容只剩 1152，≥1248px 视口上比兄弟页每边窄 24px（codex #589 R1 P2）。子项还**必须**写 `width: 100%`（auto 外边距会关掉网格子项的 stretch，只给 max-width 会退回 fit-content、宽屏上缩成窄柱）；`.group-page` 自己**必须**写 `align-content: start`（它被 `1fr` 行拉满视口高度，auto 行默认 stretch 会把短页签的页头凭空撑开几十像素）。三条都不报错、只是长错了。②**不挂样式表里不存在的 class**——改版前 7 处 `<span className="eyebrow">GROUP WORKSPACE</span>` 之类的英文装饰小标题在 `globals.css` 里根本没有规则，以继承来的 16px 正文裸奔在标题上方（`tsc` 不看 className 字符串，testing-library 只看文本，既有门禁全绿），已整体删除。③**视觉词汇取自兄弟页、计数不新增请求**——群组知识库卡片复用笔记本卡的米色卡面 `#f3f1e8`（指向的就是同一批笔记本），抬头右侧三个计数全部取自本页已取回的数据，「待审批」只对有审批权的人显示（普通成员不加载队列，恒 0 的格子会被读成「审批过了」）。回归门是 `frontend/tests/guards/group-page-style-guard.test.mjs`（走整棵 AST 收 `className` 的每个静态 token 逐个对账类选择器是否存在，并钉住 `.group-page > *` 的三条声明、`.group-page` 的 `align-content: start`、`.group-page` 自己不得出现 `max-width`、两个调用点都用共享的 `.back-home-button` 且 `.group-page-back` 不得复活，以及 groups-page.tsx 无 `<h1>` 而顶栏那一格是 `<h1>`；不检查间距/配色数值）。Notebook 内的「共享给群组」仍由 `frontend/app/notebook-group-share.tsx` 提供，两处共享 `.group-*` 的基础行、标签和危险动作语义。
- **只读/群组共享库的顶栏身份行**：`ReaderNotebookBadge` 那一行由 `.reader-badge-row` 排版，在固定 72px 顶栏下**恒不换行**（≤760px 时顶栏本就是 `height: auto`，前提没了就放开换行、标题独占一行——那里不换行反而会把标题挤到 43px，26px 字号连一个半字都放不下）。**≤1200px 不在顶栏渲染「退出共享」**：它固定 ~98px 且不缩，是紧凑桌面宽度上把标题挤没的那一个（1000px 实测 37px→105px）；能力没丢，同一动作在笔记本卡片菜单里一直都在。刻意不动 `.workspace-title` 的 flex-basis——那会改变所有工作区（含 owner）的顶栏分配并让 800px 的工具栏开始横向滚动。它此前用 `.tag-row`（`flex-wrap: wrap`），而 `.workspace-header` 是**固定 72px 单行**——标题 + 徽章 + 一句长说明换成三行、在 72px 里垂直居中，标题那行就被推到可视区**之上**（实测内容高 141px vs 容器 72px），群组共享库点进去整份库名一个像素都看不见，说明文字还漏出去盖住下方内容。标题用 `.reader-badge-title`（`width:auto` 解开 `.notebook-title-input` 的 `width:100%`，`min-width:0` + 省略号——被压缩的必须是标题而不是徽章）；身份标注是**状态**不是主操作，用轻量 `.reader-badge-chip`，不拿 42px 实心黑 `.new-pill` 冒充（它摆在 26px 库名旁会把主角盖过去）。身份解释**只进 tooltip**，顶栏不占行；「要停止访问请联系组管理员」这类指引尤其不留——群组共享本就没有自助退出，把一条他按不动的操作指引常驻在标题旁没有意义（用户裁决 2026-08-20）。回归门 `frontend/tests/guards/reader-badge-layout-guard.test.mjs`（CSS 侧钉不换行/省略——jsdom 没有排版，量不到那 141px）与 `frontend/tests/component/notebook-reader-actions.component.test.tsx`（结构侧钉库名在、用的是 `.reader-badge-row`、行内无 `.tool-hint`）。
- **访问权变动必须连当前工作区一起对账**：独立群组页从工作区顶栏也打得开，退出群组／被移出／删组／撤销共享都可能把用户正站在里面的那本库从脚下抽走。只刷 `notebooks` 清单不够——工作区开在 `currentNotebook` 这份**另一个** state 上、一动不动，人会继续待在一本已经读不到的库里，整屏毫无反应直到手动刷新。`page.tsx::reconcileOpenNotebook(remaining)` 是唯一实现，「退出只读共享」与独立群组页的 `onChanged`（`refreshAfterAccessChange`）共用它；判据的主语是 `activeNotebookIdRef`（此刻真正装载着哪一本），**不是**渲染期的 `currentNotebookIdRef`（切库落地前它还指着上一本，会让对账把用户自己发起的导航作废掉；世代闸挡不住——那次导航发起在快照之前）；`null` 同时覆盖「导航在飞」与「人在集合页」，直接返回。**「还在」≠「没变」**——撤掉一条边后访问权可能只是降档（组管理员只剩只读边），库还在时必须 `refreshActiveNotebook()` 刷详情，否则整屏写入口继续亮着而写入在 API 上被拒；不拿列表行合并（`document_limit` 是 0 哨兵）。谓语是**「重取回来的清单里还有没有它」**而不是「刚才那个动作针对的是不是它」——同一本库可经多条路进来（另一个群组、直接成员、`everyone` 边），退掉其中一条不必然失去访问。兜底导航**自己**失败时必须退回集合页并说明一句（`openNotebook` 已经把 `activeNotebookIdRef` 置 null——那一步现在落在打开 transition 的 `enter` 相位 `enterNotebookTransition` 里，时机与接入前相同：全部 owner `begin` 成功之后、并行取数之前；就地抛出去会让之后每次复核都在「无库可对账」上返回，人被永久钉在陈旧工作区）。落点沿用既有那套：优先跳到自己的第一本库，一本都没有才回集合页，并 `replaceState` 顶掉旧的 `#notebook=<id>`（否则按返回键会撞上一本已 403 的库）。删组文案同批订正：`delete_group` 只清 `notebook_grants`，知识库一个字都不动，原文「知识库会一并收回」是假话。两次复核会叠在一起（切回标签页一次、弹窗动作又一次）且**先发的可以后回**，所以重取还带一道**发布闸**：比 `notebookListPublishedRef`（已发布那次的发起序号）而**不是**「发起序号还等不等于最新」——后者「发起即占位」，一次失败的复核会把并发的成功加载一起作废、初次加载可能永远停在空清单上；失败不推进水位所以谁也吞不掉。发起序号 `notebookListSeqRef` 仍在，只回答「期间有没有更晚发起的请求」（有就让它对账，并因它可能只刷列表而重来一次）（`loadNotebookCollection` 同样要过——它把 `listNotebooks()` 与更慢的 health/config 放在同一个 `Promise.all` 里，清单可能撤销前取回、撤销后才落地；输掉竞态的那次访问权复核会重来一次，因为赢家只刷列表不做对账）：旧响应整条丢弃，否则它会用 `setNotebooks` 把撤销前的清单盖回去——工作区已正确跳走，列表里那本读不到的库却又活过来（`navEpoch` 挡的是导航，挡不住这个，两回事）。`handleLeaveShared` 因此必须调同一个收口而不是自己抄一份重取。**远端撤销另算**:别人把你移出群组／删组／撤销共享时本浏览器收不到任何事件，没有推送通道，只能在标签页**重新可见**时复用同一条对账路径复核（节流 `ACCESS_REVALIDATE_MIN_INTERVAL_MS`，`listNotebooks()` 失败时整条不执行——一次网络抖动绝不能把人赶出他还有权限的库）；这是尽力而为**不是保证**，一直停在前台不动的标签页仍要等下一次交互撞 403，文档按这个口径写（codex #529 R3）。回归门 `frontend/tests/guards/group-sharing-guard.test.mjs`。
- **异常提示分级**：来源异常小字（解析失败/抽取缺口/待补全等）分三档（integrity=红/retrieval=黄/info 中性），唯一渲染路径是 `AnomalyBadge` + `sourceAnomalies()` + `--danger`/`--warning`/`--warning-solid` token；新增异常小字必须走它，不得手搓内联样式或裸 `⚠`。真源见 `AGENTS.md` 前端章节，回归门是 `frontend/tests/guards/anomaly-guard.test.mjs`。
- **加了守卫 ≠ 有效**：必须做**变异验证**——把代码改回违规形态，确认它真的报红。只做「删除」变异不够，还要做「移动」变异。变异本身极易打空（替了字面量但代码用的是常量、按行号插入而行号已漂），先 `grep -c` 确认改到了再跑。
- 收尾提 PR，不直接合 `master`。分支先 rebase 到基分支保持线性，再 push、`gh pr create --base <基分支>`（通常是 `master`，stacked PR 不是，见第三节）。提 PR 与合入都必须经过 codex 评审，见第三节。

### 刻意不遵从 `AGENTS.md` 的几处

本文件其余部分都服从「冲突时以 `AGENTS.md` 为准」。以下是**穷举**的例外，各有理由：

1. **合入方式**：`AGENTS.md`「Feature Completion」第 1 步写的是把最新 `master` **3-way merge 进特性分支**，与本仓库实际使用的 Rebase and merge 冲突——把 `master` 合进来会破坏分支线性，GitHub 会报 `cannot be rebased`。以 **rebase** 为准，并提醒用户订正 `AGENTS.md`。
2. **worktree 里的 `npm install`**：`AGENTS.md`「Frontend/UI」写「缺 `frontend/node_modules` 就先跑 `npm install`」，在软链共享安装树的 worktree 里照做会写穿主 checkout。以上面「先 `ls -l` 再决定」为准。
3. **编辑方式**：`AGENTS.md`「Git And File Safety」写 `apply_patch`，那是 codex 语境；Claude Code 的等价物是 Edit/Write。这条是载体差异不是规则分歧。

---

## 二、子代理规范

### 1. 起子代理必须显式选模型

**不得默认继承主 agent 的模型。** 每次 `Agent` 调用要么显式传 `model`，要么用 `.claude/agents/` 里已钉好模型的角色。

`.claude/hooks/require-subagent-model.py` 是这条的 PreToolUse 硬门：没显式选模型且角色未钉模型的调用会被拦下。唯一的豁免是 `subagent_type: "fork"`——fork 语义上必须继承父模型，传 `model` 本就无效。省略 `subagent_type` **不算** fork，它等于默认 general-purpose，照样要选模型。

**判据是任务需要多少判断力，不是任务有多大。**

| 模型 | 适用 | 典型任务 |
| --- | --- | --- |
| `opus` | 需要判断力，要能推翻既有结论 | 写实现计划、规格/代码评审、架构取舍、疑难 bug 归因、安全审查、跨文件因果推理 |
| `sonnet` | 规格已定死的转录型工作 | 计划已写明改哪些文件怎么改、机械重构、补测试、文档同步、照既定模式扩展 |
| `haiku` | 纯检索定位清点 | 找文件、列符号、grep 汇总、清点调用点——只需汇报不需推理 |

内置角色（`Explore`、`general-purpose`、`Plan` 等）同样要传 `model`：`Explore` 做的是检索，一般 `haiku` 或 `sonnet` 就够。

**拿不准就上 `opus`**：返工一次的成本远高于模型差价。

**返工时先查计划，别默认怪模型。** 实测归因（PR#288）：`sonnet` 实现者反而抓到了计划里的 bug，问题出在计划不在执行。所以升级路径是——子代理报告「计划有歧义」或返工 ≥1 次时，**回去用 `opus` 重写计划**，而不是把同一份烂计划换 `opus` 再跑一遍。

### 2. 仓库角色（模型已钉在定义里）

| `subagent_type` | 模型 | 用途 |
| --- | --- | --- |
| `impl-task` | sonnet | 执行单个规格已定死的实现任务 |
| `spec-review` | opus | 任务级规格符合性评审 |
| `code-quality-review` | opus | 任务级代码质量与回归风险评审 |

实现任务本身需要设计取舍时，不要用 `impl-task`，改用通用子代理并显式传 `model: opus`。

⚠️ 可用的 `subagent_type` 列表在**会话启动时枚举**：刚新增或改名的角色定义，要重启会话才会出现，否则调用会报 `Agent type not found`。这是**响亮失败**不是静默降级——未知的 `subagent_type` 一律直接报错，不会悄悄落回默认角色。撞上时的兜底是用内置角色并显式传 `model`，判据不变。

同理，`subagent_type` 是**精确匹配**，不做大小写与分隔符的宽松归一：`Impl Task` 不会解析成 `impl-task`，只会报错。

### 3. 何时**不**起子代理

子代理会重建上下文、重新探索、写报告，然后你还要再读一遍报告。收益不明显就自己做：

- 几个文件的读改、简单搜索、小范围验证 —— 自己做更快更准。
- 一个不大的任务不要拆给多个并行子代理。
- 能一个子代理做完的，就不要用多个。

### 4. 本仓库刻意覆盖的通用默认：评审外包

Claude Code 的通用默认是「评审和验证留在主 agent 循环里，不要外包给子代理」。**本仓库刻意相反**：

> 已批准的多步实现计划按「子代理逐任务」执行——每个任务用一个全新的实现子代理，完成后跑任务级的规格评审与代码质量评审，再推进下一个任务。

理由是实测的（PR#288）：opus 的价值主要兑现在评审环节——变异验证、CSS 推演、推翻错误诊断都出自评审子代理。这是用户的明确决定，别按通用默认改回去。

例外：纯研究、设计、状态查询、只读评审类工作**不需要** worktree，也不需要子代理。

### 5. 子代理简报必备字段

一次把上下文喂足，避免「起 → 等 → 补简报」的往返：

1. **目标与验收标准** —— 怎样算完成。
2. **相关文件的绝对路径** —— 别让它自己猜。
3. **适用的红线** —— 从上面第一节挑相关的贴过去；子代理看不到你的对话历史。
4. **输出格式** —— 改了什么 / 没改什么 / 跑了什么验证命令与真实结果 / 阻塞与存疑。

多个互相独立的子代理放在**同一条消息**里并发发起。默认并发 ≤ 4；超过 20 个必须用户明确要求。

### 6. 委托后不返工

派出去了就认结果：不要自己再重做一遍它的工作，也不要在它汇报后重新推导它的结论。有疑问就针对性复核具体某一条，而不是整体重来。

---

## 三、提 PR 与合入：codex 评审闭环

**提 PR 之后、合入之前必须有 codex 评审，每一轮的原始输出逐字贴回 PR。**

执行由本机全局的 PostToolUse hook `~/.claude/hooks/codex-pr-review.sh` 承担。**它不是仓库产物**，`git grep` 在本仓库里找不到它；换机器或新 clone 上没有它时规则依然成立，那就手动跑。

### 触发判据不看命令文本

**这条是踩出来的（2026-08-16，PR #511）**：原实现按 `gh pr create` / `git push` 的命令**文本**匹配。我用 `$GH pr create`（把 gh 放进 shell 变量）开了 PR，文本匹配不上 → 评审**一轮都没跑、PR 上零评论**，而我还在汇报「hook 正在评审」。这类失败不报错、只是什么都不发生，是整条链最危险的形态；命令文本是其中最脆的一环，所以判据整个换掉了。

PR 号的来源，按顺序：

1. **命令输出里出现 PR URL** —— `gh pr create` 必然打印，且不受调用形式影响（`gh` / `$GH` / 绝对路径一视同仁）。
2. **本分支已知的 PR** —— 第一次见到就把 branch→PR 缓存在本机，之后任何一条 Bash 命令都能零 API 认出它。

两者都只认**当前分支**的 PR（比对 `headRefName`），免得别处贴出的 PR URL 把评审引到无关 PR 上。

跑不跑，两个闸：

- **从没评审过（rounds=0）** → 跑第 1 轮。
- **上一轮判了 P0/P1（`awaiting_fix`）且 HEAD SHA 与上轮评审的不同** → 重审。「改完了」由提交本身证明，不由命令长什么样证明。

**推论，很容易踩：上一轮是 🟡 或 🟢 之后再提交修复，不会自动重审。** 这时必须自己补跑并补贴——hook 只代贴它自己跑的那几轮。

钩子必须 `cd` 到发起该命令的目录（PostToolUse 载荷里的 `cwd`），否则空 diff 硬失败会在每次多 worktree 的会话里误报。载荷不是合法 JSON 时**出声**而不是静默退出：它与「这条命令与评审无关」在退出码上原本长得一模一样。

手动命令与 hook 内部一致：

```bash
codex exec review --base <base> --ephemeral \
  -c 'model_reasoning_effort=medium' -c 'notify=[]' -o <out>
```

`review --base` 与自定义 PROMPT 位置参数互斥（报 `cannot be used with '[PROMPT]'`），所以不传自定义指令，正文是 codex 原生英文输出。对 codex 评审行为的约定只能经 `AGENTS.md` 传达（codex 每轮自动加载它）：已写明评审场景**勿重跑 `check.sh`/前端构建**（评审沙箱写不了 `frontend/node_modules`，必然 EPERM；完整门由提交方本地跑并在 PR 附结果）——该规则仅限评审场景，不放松实现场景的硬门。

### 判成败要双判据

**退出码 0 且输出非空。** codex 被 SIGTERM 杀掉时退出码也是 0，只看退出码会贴出一条空评论、假装评审通过。

### 每轮都贴原文

包括零意见的轮次，也包括手动补跑的轮次。评论里带上：触发方式（自动 / 手动）、完整命令、head SHA、退出码与输出字节数。**贴原始输出，不贴我的转述**——用户要能自己核对我确实跑了、且结论没被我复述失真。

### 分级与闸门

| 判定 | 含义 | 动作 |
| --- | --- | --- |
| 🔴 P0/P1 | 阻塞 | 状态自动置 `awaiting_fix`；**核实后修掉再重审**（2026-08-16 用户原话：「review不通过就返回修复，修复完之后再等CI变绿然后合入」）。只有意见站不住（走驳回三件套）、或修复方向需要人拍板时才停下来问人。改完提交后会自动触发下一轮 |
| 🟡 P2/P3 | 非阻塞 | 可以如实说明后不改 |
| ⚪️ 解析不出标签但正文很长 | 格式可能变了 | 保守拦人，绝不因解析失败默认放行 |
| 🟢 无输出标签且正文很短 | 通过 | 等 CI 绿后按下面「合入」处理 |

自动评审上限 5 轮（`CODEX_REVIEW_MAX_ROUNDS`）。人决定放弃修复时，由我显式落状态；🟡/🟢 之后修完再审也由我手动补跑（那种情况 push 不会自动触发，见上）：

```bash
~/.claude/hooks/codex-pr-review.sh set-state <PR号> <waived|awaiting_fix|passed>
~/.claude/hooks/codex-pr-review.sh show-state <PR号>
~/.claude/hooks/codex-pr-review.sh run <PR号> manual
~/.claude/hooks/codex-pr-review.sh verify <PR号>
```

`verify` 是**给人核对用的**：它不读本地状态文件（那是我写的），只查 GitHub 上的实际评论里有没有针对 **PR 远端 head**（`headRefOid`）的评审，没有就非零退出。基准必须是远端而不是本地 `git rev-parse HEAD`：本地落后时（别人推了、或我从另一个 checkout 推了），按本地 SHA 会命中一条旧评审而放行，而 `gh pr merge` 合的是远端那个没被评审过的 head。合入前必须先跑它——见下面「合入」。

### 意见不是照单全收

codex 的评审对象是 diff，它未必了解本 harness 的运行时事实。核实后可以驳回，但必须三件事齐全：在 PR 上写明驳回理由与证据、在代码里留下这条取舍的注释、加反向护栏用例钉住它。（PR#322 驳回过「豁免省略 `subagent_type` 的隐式 fork」，照做会把守卫整个掏空。）

### 空 diff 是硬失败

`base..HEAD` 算成空 diff 时 hook 会硬失败，而不是跑出一句「未发现问题」——假绿比不跑更糟。多半是当前目录不是 PR 改动所在的 worktree，`cd` 到正确目录再跑。

### 合入

- **默认闭环到合入**：codex 判 🟡/🟢（非阻塞）**且** CI 全绿时直接合，不再逐次问人。**唯一例外**：用户明确说过「等我合入」——说过就绝不自动合，只把 PR 链接和状态交回去。这是 2026-08-15 用户的明确决定，取代了原先「必须先拿到用户明确同意」。
- **合入前先跑 `verify <PR号>` 自证**：它只查 GitHub 上的实际评论、不信本地状态，判据是 **PR 远端 head**，非零退出就是「远端 head 没有已贴出的评审」，那就不许合。这条不是形式主义——评审静默没跑过一次（#511），而当时我自己汇报的是「正在评审」；本地状态和我的说法都不是证据，PR 上的评论才是。
- **闸有三个，缺一不可**（评审非阻塞 + CI 全绿 + `verify` 通过）：🔴 P0/P1 与 ⚪️（解析不出标签但正文很长）**一律不自动合**——先修掉并重审到非阻塞，修不动或意见站不住时才停下来问人；CI 未全绿也不合。判 CI 用 `gh pr checks <PR号>`，只有全部 `pass` 才算绿——`mergeStateStatus: CLEAN` 只说没有冲突/没有必需检查在拦，不等于检查跑绿了。
- 本地门禁有既有失败时，不拿它当合入依据也不假装没有：在 PR 里写清楚它**改动前就存在**（证据：相关目录零改动，或在 base commit 上跑出逐字相同的失败），CI 才是权威。
- 本仓库用 **Rebase and merge**：`gh pr merge <PR号> --rebase`（也有 squash 合入的历史，标题带 `(#NNN)` 后缀的即是）。
- base 不一定是 `master`——stacked PR 的 base 是它的基分支，别硬写 `master`。
- 判断分支是否已进 `master`，**只认 `gh pr view --json state` 为 `MERGED`**。别用 SHA 祖先判断（rebase 会改 SHA），也别只信 `git cherry`（squash 合并会把提交全报成未合）。

---

## 四、深度报告与逐步推理准确性契约

深度报告创建后先做完全不读取语料的问题理解，并停在 `intent_ready` 等确认；有阻断性歧义的问题无论是否 `auto_generate` 都必须回答后才能推进，清晰问题在人工路径下也要确认可编辑的最终研究问题。`auto_generate=true` 且问题清晰（无阻断性歧义）时，服务端复用人工确认端点 `backend/app/services/reports/intent_confirmation.py` 的同一份确定性冻结自动确认意图（不二次调用理解模型）并直接推进到规划，随后仍在无阻断性歧义时自动接受默认大纲直接生成；带范围（任一维）的报告在自动确认前还必须过与人工端点同一道范围重验（由 api 层经回调注入引擎，重验不过或无回调即留在人工门，reason=`scope_invalid`——意图理解期间来源被删/参考库被卸载不得带着失效范围继续规划；重验产出的刷新冻结必须被**采用**而非只当布尔闸：期间新增来源/挂库会翻出 narrowed=true，认领的 understanding 与后续规划/生成的范围上下文都用刷新后的冻结，与人工确认路径持久化的内容一致）；任一步自动推进失败或竞态一律 fail-open 留在原状态、人工确认门保持可用，只发脱敏事件 `report_intent_auto_confirm_skipped`（kind/notebook_id/report_id/稳定 reason，无正文）。确认端点必须以数据库原子转换认领 `intent_ready → planning`，确定性保留用户已经看过的全部合同字段，不得在确认后用第二次 LLM 输出静默替换；确认后的问题/答案才是后续检索、规划和生成的权威输入，补充答案可进入内部检索串但不得污染报告可见标题。此后才侦察语料；语料只能影响检索措辞、证据覆盖和章节排序，不能替换或收窄用户明确要求的主题。大纲中每个必答主题都要有可见绑定，最后一个绑定在 UI 与 API 两层都不可删除，确认后的检索方向必须实际执行。`SourceElement` 在 reasoning Ask 与报告中都是可 `[k]` 引用的一等证据，报告点击引用要能看到绑定的来源、位置和原文片段；小库可直接评分 element，大库禁止全表加载元素文本/向量，必须从有界 chunk ANN/FTS 候选的 `element_ids` 做有界 PK hydration。若来源已被接地判定为论文且解析出非空 `paper_title`，Ask 与报告引用优先显示论文标题，否则保留普通来源名/文件名；引用响应另以 `source_file_name` 保留 `sources.file_name`，当它与显示标题不同时，Ask/报告引用卡显示「原始文件」，公共参考库证据同样适用。该值只能来自持久化上传行，绝不能取 MinerU 临时/输出 Markdown 名。标题规则只有一份实现 `app/domain/source_display.py::source_display_title`（`app/services/source_display.py` 保留 re-export 供既有 import 不变），所有为用户命名来源的路径（引用标题、检索证据卡、清单卡）一律 import 它——它们命名的是同一批来源，写第二份的后果是同一篇论文在同一屏里有两个名字；守卫是 `backend/tests/test_source_display_title.py`。章节的 grounded 状态由真实锚点与证据相关度重算，不能相信模型自报，也不能把同源的不同 chunk/element 折叠成一个引用。最终编辑器只可摘要并报告遗漏/冲突，不可改写正文或新增事实。报告复用共享 intent 解析并持久化 `result_scope`/`completeness_required`；完整枚举未接入前，`complete`/`aggregate`/`hybrid` 必须如实披露仍是相关性检索，假设只限定范围、不得充当证据或进入检索词。每份报告以有界「资料基础」和分组参考文献披露保守可区分资料、身份不确定性、集中度和重复膨胀，精确锚点不折叠；「引证覆盖率」仅是高风险断言的引证覆盖率，不是语义蕴含评分。可选、用户确认的正交比较框架配合「并行检索 → 一次全局证据蓝图 → 并行分节撰写」：该路径在所有档位对多节报告生效，正文按主张/条件综合而非逐篇拼贴，终审拿到公平摘录、账本与冲突只做审计。任何新增结构畸形都 fail-open；主张账本是对已写成正文的**事后审计覆盖层**（不是撰写输入），接地检查按**行**处置：主张文本不是正文原文、引用键非法或不在同一句话里，一票否决**该行**而不再连坐整节；超上限截前 24 行；状态四值 missing/available/partial/invalid（有保留也有丢弃即 partial，空提交没有可审计内容记 invalid），可用计数含 partial 并单列披露。蓝图 claim id 成员检查已整体移除——正文合法包含蓝图外新主张，全仓没有任何下游按账本 claim id join 蓝图，该检查曾让有蓝图的报告账本 0/N 全灭。`frame_assignments` 仍逐条降级——一条未知 facet 键或越界取值只丢该条标签。行级化对下游安全的论证是**单调性**而非独立性：互斥 facet 冲突检测跨行聚合，但保留行集恒为旧全有全无行为的超集，检测面只增不减。全局综合是唯一新增模型调用（每报告 ≤1，任意 depth，章节数 ≥2）。数值上限只在 `docs/product-and-api*.md` 登记。完整约束见 `AGENTS.md` 的 Product Flow。

逐步推理的正式界面也必须先调用 `/ask/intent` 做不读取 notebook / 参考库语料的问题理解；该预检只可使用当前会话最近的用户问题，不可把语料派生的助手回答回灌为意图依据，也不得创建 conversation 或 Ask job。意图清晰时自动确认，但因没有人工审阅，原始用户问题必须保留为第一条权威检索种子，模型规范化问题只能补充；关键歧义暂停并由用户补充后，审阅过的最终问题才成为权威。必答主题/检索方向、实体、比较轴、约束、排除项、前提、期望输出和答案确定性冻结，并统一用于 Memory、PPR、首轮子查询、证据检索与答案合成；原始输入仍作为可见会话问题。首轮先执行完整权威问题，再按主题轮询确认方向，确保每个必答主题先得到一个种子：档位的「首轮子查询」上限只约束首轮并发，装不下的已确认方向进入待覆盖账目，在确定性 seed pass 之后、reflect 循环之前按合同顺序补种，与 reflect 共用同一份 `max_steps`（补种最多用掉一半，reflect 始终留有可用份额，披露才到得了模型）；per-run 注册表把每个已确认方向映射到唯一简称(默认截断撞出同一简称就加宽、仍撞则加确定性序号后缀)并可反查回方向原文;方向仍未覆盖时每轮回喂 reflect,模型用简称提交 `add_subquery` 会经注册表解析——命中未执行方向按方向自己的完整原文执行且账目记在方向身份上，命中已执行方向（补种或更早的 add_subquery）则判重复、不重跑。reflect 循环结束时，才按这份终态把仍未覆盖的方向记成一条 `skip` 轨迹步（不是预算刚耗尽那一刻的旧账，全部被补上则不落这一步），绝不静默丢弃。第二个规划模型不得替换，后续反思只可按证据补充查询。无效确认必须在创建持久 job 前返回 422，预检取消要把取消事件传到意图模型调用。上述**首轮**（run 级账目初始化 → 理解块/打法块/集合地图注入 → 规划 → 初检索 → PPR seed → 精确查找 seed → 空证据兜底 → 已确认方向补种）由 `reasoning_retrieval.ReasoningRetriever._run_first_round` 承载，各阶段是 `_first_round_*` 方法，run 级状态经 `_ReasoningRunState` 显式交接（可变容器同对象，标量在 `run` 一次性解包后即陈旧，reflect 循环与收尾只认解包出来的局部名）；阶段顺序本身是合同，尤其精确查找 seed 必须排在 PPR seed 之后。长期合同由意图、PPR、精确检索、空证据兜底、方向补种账目、枚举与合成的聚焦行为测试承担；仅为证明拆分零变化而录制的轨迹措辞、内部请求计数或阶段形状快照，在这些行为覆盖到位后退役，不得重建成第二份实现。

逐步推理的来源范围**只由用户勾选决定，模型不参与来源判断**。可见来源复选框（`SourceScope`：`include`/`exclude` + `source_ids`）是当前 notebook 的硬上限，经请求级 `source_scope_context` 下推到有界候选生成，并在证据裁剪、合成、anchors 与 citations 再校验。KG 候选路由必须看服务端算出的语义状态，不能看冻结 include tuple 是否存在：未漂移的默认全选（含单篇 notebook）保留 notebook ANN＋词法召回、只在 hydrate 应用快照上限；显式 allow-list、真正收窄或来源宇宙漂移才在词法 Top-K 前过滤。`source_index_backfilled=false` 只表示历史完整性未验证，绝不表示图/反查表为空；这类受限读取在 `LIMIT` 前检查权威 evidence，新 notebook 则从认证为空的反查索引开始、由在线 KG 写事务持续维护。无法证明可安全按来源隔离的 PPR/图扩展/精查等通道在受限 run 中跳过并写可见原因，不得先跑全库再只过滤输出。意图模型**不得**从问题措辞推断用户点名了哪个来源：曾经的 `source_refs` / `QueryIntentSourceScope` / `search_evidence(source_refs)` 已整体移除——精确等值匹配无法兑现「pdagent」这类简称，匹配不上就 422 让常见问法确定性失败，而用户已能自行勾选，模型再猜一遍只增加失败模式。

KG 抽取必须在全局对象同一事务内发布不可变的来源事实和规范化证据元素绑定：按来源抽取代次归属，不依赖融合后全局对象的生命周期；代次过期或证据跨来源时 fail closed；删除、重解析与 notebook 复制同步处理。深复制须用同一来源代次映射重写事实、绑定与终态回填账本，并为副本生成本地 completed KG run，副本审计/修复不得依赖原 notebook 的运维抽取历史。内部 `SourceSubgraphSnapshot` 按来源代次缓存且双后端同义：来源谓词、关系两端授权与状态过滤必须发生在 LIMIT 前，SQLite 的对象/chunk/cluster 必须从来源索引起步而非先扫 notebook；缓存身份只用 O(1) KG/cluster 单调序列与有界的来源/run/回填状态，缓存命中路径禁止新增 COUNT/MAX 扫描，live 完整性在受限事实窗口内校验。对象/chunk/事实/证据/cluster 任一越界只关闭依赖它的能力，来源索引未回填、代次漂移或未知事实投影版本则在依赖图能力使用前 fail closed；缓存 payload/evidence 树必须递归不可变。仅统一激活层使用的 `SourceGraphPrimitives` 为邻居/扩展、两跳 chain、关系词法/端点补召回、精确小节和游标枚举逐项复验来源归属、端点、evidence、review 状态和 capability；任何原语都不得回退 notebook 全图/map，未选来源不得成为中间节点，exact/enumeration 只作用于已经受限的 snapshot。live 或回填投影不完整时只能用来源事实中的安全名称标注节点，必须关闭把事实或 membership 当完整集合的消费者。scoped PPR 由默认开启的 `SOURCE_SUBGRAPH_PPR_ENABLED` 独立回滚；必须直接从 snapshot 构建稀疏 transition，禁止先切 notebook 全图 CSR 再过滤排名。节点/chunk 归属、关系来源/两端/evidence/review、对象到 chunk membership 及 cluster member 必须在度数归一化和任何 LIMIT 前过滤。reset 只接受过滤后图内的对象/chunk id，min-max、Top-K 和 hydration 也只看所选来源 chunk。按冻结 scope 与 KG/cluster/source 代次做有界 LRU，冷 transition single-flight，避免并发请求成倍分配 CSR；节点/边越界及 build/run 异常返回明确局部 capability reason 和空 PPR 命中。PPR 只给 G 排序，绝不改 B 的 relevance 或顺序。大 notebook 的按来源分区 scale 伴生产物让所选来源图成本不再跟整库大小增长：full build/fold 对每个可见来源使用 source-first LIMIT+1 护栏，发布绑定主 scale manifest identity 的哈希直寻 CSR partition，并失效专用 single-flight LRU；每个 partition 文件都校验摘要。运行时先只读全部所选小 manifest，在打开任何 payload 前按 node/nnz/cross-edge 累计值拒绝越界；只选一个来源时直接复用其 CSR，选择多个来源时只用稀疏数组组合并一次性分配受限的 cross-edge 数组。结果选择必须做有界局部 Top-K，禁止排序该来源全部 chunk。跨 partition 关系必须同时满足所属来源被选、evidence 同源、两端都在所选对象并集及中央 edge registry 合法。旧版、缺失、损坏、越界或 identity 失配伴生产物返回 capability unavailable，绝不回退整图遍历。发布和运行时消费有独立的默认开启回滚开关；Ask/Report 只通过统一激活层消费 G。来源图增强必须走独立受保护双通道：历史路径先冻结 `B_final`，不得把 B 与 G 放回同一 reserve/token selector；G 只可消耗独立 token 预算及 reasoning action/step 账本。重复 chunk 只能在隔离副本合并 provenance，baseline 的正文、来源/引用句柄、分数、相关度和位置不变；`baseline_evicted_count != 0`、图失败或超时均丢弃整段 G 并保留完全相同的 baseline manifest。增强关闭或预算为零时不得调用 provider，也不得改变历史调用、prompt 或结果序列。Ask/Report 只通过统一质量门控激活服务消费图增强；off 与 shadow 的公开检索分数、通道、证据、prompt 与答案保持历史 baseline。

用户可见的所选来源图激活必须先有经过验证的配对质量 attestation。生产契约只在 `app/services/source_graph_quality.py`，production service 禁止反向 import `app.eval`。golden suite 把来源别名绑定到实际 source id，覆盖单篇、少量来源、超大单篇、mounted base 与跨来源同名对象；baseline/shadow observation 必须使用完全一致的模型/采样、corpus signature、冻结本地/基座范围与别名映射。越界证据/引用、baseline manifest/candidate/citation key 改变、任何 baseline eviction、缺少必要图动作或场景规模不足均为硬失败；质量与成本既按案例也按汇总比较，禁止用一个案例的提升掩盖另一个案例回退。attestation 不含问题、答案正文、evidence id、citation 或 source id。摘要只检测意外修改，不是签名；部署只能从受信工件路径读取，并在 allowlist、稳定 hash 或 on 前钉住期望 corpus/model。shadow 不改变用户输出，因此不要求 attestation。Ask/Report 现在只通过统一激活层消费 G；off 与 shadow 始终返回 B。

**轨迹必须覆盖整轮，不只是检索段。** 问题理解跑在持久 job 之前，前端自行合成理解阶段的前几步（理解中 → 已理解/待澄清 → 已确认）并让后端步骤接在其后，不得再为这一阶段另起轨迹之外的独立提示条；该阶段的客户端墙钟以 `intent.understanding_ms`（可选、有上限、绝不参与检索）回传，成为持久 `intent` 步的 `duration_ms`，重开会话回放时不会凭空少掉这一段。后端必须在 Memory 检索**之前**推送 `intent` 步；命中私有记忆时记 `memory` 步——它记录的是**召回**而非归因（记忆进了 prompt 却没被引用是常态，按锚点过滤会漏报；推迟到合成之后又会离开事情真正发生的位置），归因由答案里的 `[k]` 引用承担，措辞不得声称被采用；零命中改记一条**带耗时的 `skip`**，候选查询与 embedding 调用照样发生，丢掉这段就与「覆盖整轮」矛盾。答案生成之后记 `synthesis` 步——那次生成通常是整轮最长的一段，既不能不可见，也不能被排除在轨迹总耗时之外；它的引用数取绑定锚点，不取检索到的证据卡数。实时轨迹面板按引擎是否真的流式推轨迹（后端 `AskMode.streaming`，前端镜像为 `streamsTrace`，由 `scripts/check_ask_modes_contract.py` 锁同步）判断，**不得按显示分组判断**：深入分析组里的非流式引擎会因此全程只显示「等待后端事件…」。

统一激活层现已接入 Ask/Report 路径。`SelectedSourceGraphActivationService` 是 Ask `chunk`/`reasoning`/实验 `graph` 与深度报告唯一消费入口，只处理服务端冻结、真正收窄的本地 `include` scope；省略/全选（含单来源 notebook 全选唯一来源）在 snapshot I/O 前留在历史路径。历史 `B` 先完成，`G` 只用独立预算并追加在 `B` 后；默认不可见 shadow 恒返回 `B`，active 必须通过受信无正文 attestation 与精确 corpus/model pin，任何漂移、失败、越界或 baseline eviction 都返回 `B`。mounted base 保持独立 participant。Shadow/off/degraded/active 控制态只能进入无正文内部事件，禁止进入公开回答/报告字段、推理轨迹、stream 或 UI；浏览器还必须过滤历史已持久化的 `source_subgraph` 步骤。

已有部署的 shadow 准备只由 `scripts/prepare_selected_source_graph.py` 编排。它是离线、全 notebook 状态机：要求显式停服确认，以指定且已存在的 env 文件为权威配置、不得被进程环境变量重定向，并持有中央维护锁；续跑持久来源反查索引与 source-fact 账本，按运行时精确格式低成本复验主/partition manifest、仅在失配时重建，最后独立审计全部目标。无正文 receipt 只作参考，每次运行都重新验证数据库/工件权威状态。全部检查成功且 repository 已关闭后，才原子把三个来源图 producer/artifact 开关设为 true、rollout mode 设为 shadow；更新必须保留原 env 的 owner/mode/group 及系统支持的元数据，0600 只用于新 receipt。任何失败都保持 env 文件不变。这个流程不得进入任何用户界面或公开响应。

## 五、`AGENTS.md` 章节索引

无正文 `selected_source_graph` 事件仅供运维内部 telemetry；用户可读 debug 日志 API 必须在列表、统计、搜索、翻页和详情（包括按 seq 直接读取）统一过滤。

按需查阅，用章节标题定位（不要依赖行号）：

| 要查什么 | `AGENTS.md` 章节 |
| --- | --- |
| 文档同步规则（四份） | Documentation Sync |
| 完成 spec 特性怎么记账 | Tracking Completed Spec Features |
| 后端能力必须配前端 | Full-Stack Parity (Backend ⇄ Frontend) |
| 产品形态、四个页签、上传到问答的完整流程 | Product Flow / MVP Scope |
| 分层架构、repository facade、端口与适配器 | Architecture Baseline |
| Python 环境、`PYTHON_BIN`、后端启动命令 | Python Environment / Backend Commands |
| 前端约定、`object_type` 标签契约、错误层三段规则 | Frontend/UI |
| 界面词 ↔ 内部词对照表（词汇守卫的真源） | 界面词汇表 (User-Facing Vocabulary) |
| 依赖政策、Python 版本下限、不加 Docker | Dependency Policy / No Docker In First Version |
| LLM 环境变量、模型服务状态与诊断 | LLM Configuration |
| 事件日志、按用户隔离的日志目录 | Logging / Observability |
| `scripts/check.sh` 三泳道、暖门时间目标、CI | Verification / GitHub Actions CI |
| 测试怎么分层、测试根在哪、并发 worker | Test Architecture |
| worktree、子代理逐任务、文件安全 | Git And File Safety |
| 收尾提 PR 的标准流程 | Feature Completion (Finish With a PR) |

## KG 探活响应合同

KG 可用性探测必须绕过且不写入 LLM 响应缓存，并复用已配置的短输出预算，不能另塞更小的字面量上限；推理型 provider 可能在输出可见 JSON 前就耗完小预算。所有受控 KG 模型调用（含探活和抽取窗口）都必须把 run-control 的中断信号交给共享流式 JSON 传输，不能恢复非流 KG 路径或另造传输层；这样长回复持续有 chunk 时连接保持活跃，分类后的熔断也能停掉兄弟流。流式调用必须请求 OpenAI-compatible 的最终 usage trailer，并在跳过空 choices chunk 前采集它，让按用户 prompt/completion/total token 统计保持精确；provider 明确拒绝该可选参数时可以回退普通流式传输，但必须在物理 client 上记住能力结论，后续调用不得重复付出一次失败请求，也不得在 provider 未返回 usage 时本地猜数。空白、截断或畸形探测结果必须落成可操作的 `model_response_invalid`，前端显示“模型响应不可用”而不是误称人工中断，并保留继续分析入口。

## KG 检索查询材料

服务生成的已确认意图 envelope 完整保留给 KG 候选生成、语义 query embedding、编排与轨迹，只有 RRF/关键词打分使用自有精确契约分隔符前的检索方向；确认约束仍影响召回，但重复模板绝不能分进 keyword basis。召回与打分共用窄范围的拉丁字母模型名分隔符别名（`Cosmos3` ↔ `Cosmos 3`，以及装饰性的 `-`/`.` 变体），且不得改变 `chapter 4` 这类编号散文标签或 `ZXCV9000` 这类普通标识符。词法语言闸里的“来源受限”统一指 `source_candidates_restricted`（显式 allow-list、真收窄或冻结宇宙漂移），不能再用 `allowed_source_ids` / `source_filter` 非空来推断；默认全选的冻结 tuple 仍走正常 ANN＋词法路径。
