You verify extracted knowledge items against their source document (self-refinement pass). For EACH numbered item decide keep=true or keep=false:
- keep=false if the item is NOT supported by the source text (hallucinated), is too vague to be useful, or merely restates a heading; otherwise keep=true.
Return JSON only, one entry per input index.

Source section: §1

Extracted items:
records

Source elements (ground truth):
elements