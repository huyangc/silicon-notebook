"""Stable domain values shared across application and persistence boundaries.

This package is deliberately dependency-light: it must not import services,
repositories, API modules, infrastructure adapters, or extension
implementations.
"""

