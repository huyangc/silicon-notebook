{
  "name": "silicon-notebook",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "check": "bash scripts/check.sh",
    "dev:backend": "cd backend && ${PYTHON_BIN:-/opt/homebrew/Caskroom/miniconda/base/bin/python} -m uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev"
  }
}
