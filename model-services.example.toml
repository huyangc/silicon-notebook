# Copy to .local/model-services.toml and set the referenced keys in .env.
# This template deliberately contains no credentials.

[services.general]
display_name = "通用模型"
kind = "chat"
protocol = "openai"
base_url = "https://llm.example.com/v1"
model = "general-model"
api_key_env = "GENERAL_LLM_API_KEY"
max_concurrency = 20

[services.reasoning]
display_name = "推理模型"
kind = "chat"
protocol = "openai"
base_url = "https://reasoning.example.com/v1"
model = "reasoning-model"
api_key_env = "REASONING_LLM_API_KEY"
max_concurrency = 4

[services.embedding]
display_name = "向量模型"
kind = "embedding"
protocol = "dashscope"
base_url = "https://embedding.example.com"
model = "embedding-model"
api_key_env = "EMBEDDING_API_KEY"
max_concurrency = 8

[services.rerank]
display_name = "重排模型"
kind = "rerank"
protocol = "openai"
base_url = "https://rerank.example.com/v1"
model = "rerank-model"
api_key_env = "RERANK_API_KEY"
max_concurrency = 8

[bindings]
ask_answer = "general"
memory_preview = "general"
knowhow_optimize = "general"
source_summary = "general"
kg_extract = "general"
reasoning_agent = "reasoning"
report_outline = "reasoning"
report_section = "reasoning"
report_summary = "reasoning"
retrieval_query_embedding = "embedding"
chunk_embedding = "embedding"
retrieval_rerank = "rerank"
