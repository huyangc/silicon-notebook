# PR #306 Takeover Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 PR #306 无历史改写地合并到最新 `master`，把冲突中的源码/行号测试迁移为语义契约，补齐 anchor guard 的存储边界，并在更新原 PR 后完成 exact-SHA 独立审查。

**Architecture:** 在 `codex/pr306-takeover` 隔离 worktree 中从 PR head `0e4b6e85` 合并 `origin/master`，保留 #307 的新测试架构并移植 #306 的行为契约。产品实现只做与 anchor guard 同域的 fail-closed 补强；最终 fast-forward 推送到原 PR 分支，不 rebase、不 force-push。

**Tech Stack:** Python 3.13、FastAPI、SQLite、pytest；Next.js 15、React、TypeScript、node:test、TypeScript Compiler API；GitHub PR #306。

## Global Constraints

- Worktree：`/Users/hzf/workspace/silicon_notebook/.worktrees/pr306-takeover`。
- 本地分支：`codex/pr306-takeover`；远端 PR 分支：`claude/knowhow-md-normalize-followup`。
- Python：`/opt/homebrew/Caskroom/miniconda/base/bin/python`。
- 不 force-push、不改写 #306 的八个原始提交、不修改原 Claude worktree。
- 不恢复源码行号、源码切片、函数顺序或格式型测试。
- 保留 #307 的语义 AST / 组件测试策略；纯逻辑优先直接行为测试。
- 完整 warm gate 目标：60 秒内；GitHub Actions 当前不是 required check。
- `README.md`、`README_zh.md`、`AGENTS.md` 对产品/约束描述同步更新。
- 更新 PR 后必须由 `gpt-5.6-terra`、`reasoning_effort=high` 的独立 subagent 审查 exact pushed green SHA。
- Critical / Important 审查意见必须修复、重新验证并对新 SHA 复审。

---

### Task 1: 合并最新 master 并建立冲突基线

**Files:**
- Delete: `backend/tests/test_repository_surface_manifest.py`
- Modify: `frontend/app/knowhow-optimize.test.mjs`
- Preserve: `backend/tests/fixtures/repository_contract/api_contract.json`

**Interfaces:**
- Consumes: PR #306 head `0e4b6e85`、`origin/master`。
- Produces: 一个无未解决 Git 冲突、同时包含 #306 产品代码和 #307/#308 基线的工作树。

- [ ] **Step 1: 再次确认工作树干净且起点正确**

Run:

```bash
git status --short --branch
git rev-parse HEAD
git rev-parse origin/master
```

Expected:

```text
## codex/pr306-takeover
0e4b6e85edb22edd040c98595742c9d2d40d3a17
77e0547c0d83b3145999be761b8132f7ca38b035
```

- [ ] **Step 2: 合并最新 master，确认预期冲突**

Run:

```bash
git merge --no-ff origin/master
```

Expected: merge 暂停；冲突仅涉及
`backend/tests/test_repository_surface_manifest.py` 的 modify/delete 和
`frontend/app/knowhow-optimize.test.mjs` 的 content conflict。

- [ ] **Step 3: 保留 #307 对旧行号测试的删除**

Run:

```bash
git rm backend/tests/test_repository_surface_manifest.py
```

Expected: 文件保持删除，不把 #306 的行号平移 allowlist 搬入新架构。

- [ ] **Step 4: 先把前端测试冲突整理成可解析但会被新策略检查的版本**

`frontend/app/knowhow-optimize.test.mjs` 的 imports 必须同时保留：

```js
import {
  groupRowsByAnchor,
  isSharedColumn,
  groupCellWriteTargets,
} from "./knowhow-grouping-logic.ts";
import {
  assignmentsIn,
  callbackFlowsIn,
  callSitesIn,
  controlFlowIn,
  findFunction,
  findFunctionIn,
  ifBranchesIn,
  jsxElements,
  parseModule,
  variableInitializersIn,
} from "./test/semantic-source.mjs";

const panelModule = await parseModule("knowhow-panel.tsx");
```

删除 merge markers，但暂时不要恢复 `readFile`、`panelSrc`、`runSaveSrc` 或
`handleCellSaveSrc`。

- [ ] **Step 5: 确认没有残留冲突标记**

Run:

```bash
rg -n '^(<<<<<<<|=======|>>>>>>>)' .
git status --short
```

Expected: `rg` 无输出；Git 仅显示已合并/修改的正常文件。

---

### Task 2: 将 #306 前端接线守卫迁移为语义契约

**Files:**
- Modify: `frontend/app/knowhow-optimize.test.mjs`
- Test: `frontend/app/knowhow-optimize.test.mjs`

**Interfaces:**
- Consumes:
  - `findFunctionIn(sourceFile, parentName, name)`
  - `variableInitializersIn(node)`
  - `callSitesIn(node)`
  - `controlFlowIn(node)`
  - `callbackFlowsIn(node, target)`
  - `jsxElements(sourceFile, elementName)`
- Produces: 不读取源码文本、不依赖位置和格式的 #306 wiring coverage。

- [ ] **Step 1: 保留所有纯逻辑并发回归测试**

确认这些行为测试继续存在：

```text
writeTarget.anchorMd 来自冻结快照
coversAnchorGroup：单例组 true
coversAnchorGroup：多行非共享列 false
coversAnchorGroup：多行共享列 true
记录型表 coversAnchorGroup false
实时组分裂时 targetRowIds 仍来自冻结 save unit
409 进入 stale_skipped 终态
```

Run:

```bash
rg -n 'anchorMd|coversAnchorGroup|实时组分裂|stale_skipped' frontend/app/knowhow-optimize.test.mjs
```

Expected: 每类契约至少有一个直接行为测试。

- [ ] **Step 2: 用语义 AST 测试延迟重载**

以以下测试替换 `panelSrc` 正则：

```js
test("接线（F1）：stale 只登记待刷新，真正刷新发生在卸载 cleanup", () => {
  const modal = findFunction(panelModule, "KnowhowReformatBatchModal");
  const request = findFunctionIn(
    panelModule,
    "KnowhowReformatBatchModal",
    "requestStaleReload",
  );
  assert.deepEqual(assignmentsIn(request), [
    {
      target: "pendingReloadRef.current",
      operator: "=",
      value: "true",
    },
  ]);
  assert.equal(
    callSitesIn(request).some(({ target }) => target === "onStaleReload"),
    false,
  );

  const mountEffect = callbackFlowsIn(modal, "useEffect").find(({ flow }) =>
    flow.some(({ kind }) => kind === "return-callback"),
  );
  const cleanup = mountEffect?.flow.find(
    ({ kind }) => kind === "return-callback",
  );
  assert.ok(cleanup?.flow.some(
    ({ kind, condition }) =>
      kind === "if" && condition === "pendingReloadRef.current",
  ));
  assert.equal(
    callSitesIn(modal).filter(
      ({ target }) => target === "onStaleReload",
    ).length,
    1,
  );
});
```

- [ ] **Step 3: 用语义 AST 测试冻结快照和 save fan-out**

```js
test("接线（F1/P1）：runSave 的规划、写目标和锚点守卫同源于冻结快照", () => {
  const modal = findFunction(panelModule, "KnowhowReformatBatchModal");
  const runSave = findFunctionIn(
    panelModule,
    "KnowhowReformatBatchModal",
    "runSave",
  );
  assert.deepEqual(
    variableInitializersIn(modal).find(({ name }) => name === "snapshotRef"),
    {
      name: "snapshotRef",
      initializer: "useRef({ allRows, anchorColumnId })",
    },
  );
  assert.deepEqual(
    callSitesIn(runSave).find(({ target }) => target === "planReformatSaves"),
    {
      target: "planReformatSaves",
      arguments: [
        "changed",
        "snapshotRef.current.allRows",
        "snapshotRef.current.anchorColumnId",
      ],
    },
  );
  assert.deepEqual(
    variableInitializersIn(runSave).find(
      ({ name }) => name === "targetRowIds",
    ),
    {
      name: "targetRowIds",
      initializer: "unit.writeTargets.map((t) => t.rowId)",
    },
  );
  const saveCall = callSitesIn(runSave).find(
    ({ target }) => target === "onSaveCell",
  );
  assert.deepEqual(saveCall?.arguments.slice(-2), [
    "targetRowIds",
    "anchorGuard",
  ]);
});
```

- [ ] **Step 4: 用语义 AST 测试 endpoint 选择**

```js
test("接线（P1）：显式冻结目标优先，anchor guard 强制使用批量端点", () => {
  const save = findFunctionIn(panelModule, "KnowhowPanel", "handleCellSave");
  const initializers = variableInitializersIn(save);
  assert.deepEqual(
    initializers.find(({ name }) => name === "targets"),
    {
      name: "targets",
      initializer:
        "targetRowIds ?? (group && isSharedColumn(group, columnId) ? groupCellWriteTargets(group, columnId) : [rowId])",
    },
  );
  assert.deepEqual(
    initializers.find(({ name }) => name === "useBatchEndpoint"),
    {
      name: "useBatchEndpoint",
      initializer: "targets.length > 1 || Boolean(anchorGuard)",
    },
  );
  const batchCall = callSitesIn(save).find(
    ({ target }) => target === "batchPatchKnowhowCells",
  );
  assert.ok(batchCall);
  assert.match(batchCall.arguments[2], /anchorColumnId: anchorGuard\.anchorColumnId/);
  assert.match(batchCall.arguments[2], /expectedAnchor: targets\.map/);
  assert.ok(
    callSitesIn(save).some(({ target }) => target === "patchKnowhowCell"),
  );
});
```

- [ ] **Step 5: 删除所有源码位置/切片变量**

Run:

```bash
rg -n 'readFile|const panelSrc|const runSaveSrc|const runBatchSrc|const handleCellSaveSrc|panelSrc\.indexOf|panelSrc\.slice|runSaveSrc\.indexOf|runSaveSrc\.slice' frontend/app/knowhow-optimize.test.mjs
```

Expected: 无输出。

- [ ] **Step 6: 运行聚焦前端测试**

Run:

```bash
cd frontend
node --test app/knowhow-optimize.test.mjs app/knowhow-normalize.test.mjs app/knowhow-model.test.mjs
```

Expected: 全部 PASS，0 fail。

- [ ] **Step 7: 提交测试架构冲突迁移**

```bash
git add frontend/app/knowhow-optimize.test.mjs
git commit -m "merge: update PR 306 to latest master"
```

---

### Task 3: TDD 补齐 anchor guard 的存储边界

**Files:**
- Modify: `backend/tests/test_knowhow_store.py`
- Modify: `backend/app/repositories/sqlite/knowhow_store.py`

**Interfaces:**
- Consumes:
  `KnowhowStore.update_knowhow_cells_guarded_atomic(notebook_id, updates, require_assets=(), anchor_column_id=None, expected_anchor=None)`.
- Produces:
  相同方法签名；非法 guard 返回
  `{"written": [], "conflict": True}`，合法请求行为不变。

- [ ] **Step 1: 写 expected_anchor 长度不匹配的失败测试**

```python
def test_atomic_anchor_guard_rejects_parallel_length_mismatch(
    store, notebook_id
):
    table_id, anchor_col, shared_col, r0, r1, anchor, before = (
        _anchor_group(store, notebook_id)
    )
    result = store.update_knowhow_cells_guarded_atomic(
        notebook_id,
        [
            (table_id, r0, shared_col, before, "new"),
            (table_id, r1, shared_col, before, "new"),
        ],
        anchor_column_id=anchor_col,
        expected_anchor=[anchor],
    )
    assert result == {"written": [], "conflict": True}
    rows = {
        row["id"]: row["cells"]
        for row in store.get_knowhow_table(table_id)["rows"]
    }
    assert rows[r0][shared_col] == before
    assert rows[r1][shared_col] == before
```

- [ ] **Step 2: 写 foreign anchor table 的失败测试**

```python
def test_atomic_anchor_guard_rejects_anchor_column_from_another_table(
    store, notebook_id
):
    target = store.create_knowhow_table(
        notebook_id, "target", "", BASE_COLUMNS
    )
    target_cols = _columns_by_name(store, target)
    row_id = store.add_knowhow_row(
        target,
        {target_cols["修复方法"]: "old"},
    )

    foreign = store.create_knowhow_table(
        notebook_id, "foreign", "", BASE_COLUMNS
    )
    foreign_anchor = _columns_by_name(store, foreign)["违例类型"]

    result = store.update_knowhow_cells_guarded_atomic(
        notebook_id,
        [
            (
                target,
                row_id,
                target_cols["修复方法"],
                "old",
                "new",
            )
        ],
        anchor_column_id=foreign_anchor,
        expected_anchor=[""],
    )
    assert result == {"written": [], "conflict": True}
    row = store.get_knowhow_table(target)["rows"][0]
    assert row["cells"][target_cols["修复方法"]] == "old"
```

- [ ] **Step 3: 运行测试并确认 RED**

Run:

```bash
/opt/homebrew/Caskroom/miniconda/base/bin/python -m pytest \
  backend/tests/test_knowhow_store.py \
  -k 'parallel_length_mismatch or anchor_column_from_another_table' -q
```

Expected: 至少一个 FAIL；旧实现可能 `IndexError` 或错误写入目标格。

- [ ] **Step 4: 实现最小 fail-closed 校验**

在 `anchor_guard` 分支进入成员扫描前添加：

```python
if len(expected_anchor) != len(updates):
    return {"written": [], "conflict": True}

anchor_col_row = db.execute(
    "SELECT table_id, role FROM knowhow_columns WHERE id = ?",
    (anchor_column_id,),
).fetchone()
if (
    anchor_col_row is None
    or anchor_col_row["role"] != "anchor"
    or any(
        update_table_id != anchor_col_row["table_id"]
        for update_table_id, _row, _column, _before, _content in updates
    )
):
    return {"written": [], "conflict": True}
```

继续复用 `anchor_col_row["table_id"]` 作为 `anchor_table_id`，不要新增第二次查询。

- [ ] **Step 5: 运行 store / API 聚焦测试**

Run:

```bash
/opt/homebrew/Caskroom/miniconda/base/bin/python -m pytest \
  backend/tests/test_knowhow_store.py \
  backend/tests/test_knowhow_api.py -q
```

Expected: 全部 PASS，0 fail。

- [ ] **Step 6: 提交存储边界补强**

```bash
git add backend/app/repositories/sqlite/knowhow_store.py backend/tests/test_knowhow_store.py
git commit -m "fix(knowhow): fail closed on invalid anchor guards"
```

---

### Task 4: 同步产品并发语义文档

**Files:**
- Modify: `README.md`
- Modify: `README_zh.md`
- Modify: `AGENTS.md`

**Interfaces:**
- Consumes: #306 的最终并发行为。
- Produces: 三份一致、面向维护者的事实描述。

- [ ] **Step 1: 在英文 README 的 knowhow Markdown 段落补充并发语义**

在现有 “Backfilling knowhow-cell Markdown formatting” 附近加入：

```markdown
Interactive row/table reformat batches freeze the complete table snapshot when
the batch opens. Saving a merged shared cell revalidates the target contents,
the active anchor designation, and the exact anchor-group membership in one
SQLite write transaction; concurrent drift rejects the whole save and refreshes
the table after the batch dialog closes.
```

- [ ] **Step 2: 在中文 README 加入等价描述**

```markdown
编辑器的整行/整表规整会在批次打开时冻结完整表快照。保存合并共享格时，系统在
同一个 SQLite 写事务中重新校验目标内容、当前行标题列指定和精确分组成员；发现并发
漂移会整组拒绝，并在关闭批量弹窗后刷新整表。
```

- [ ] **Step 3: 在 AGENTS.md 的 knowhow Markdown 契约中加入同一约束**

```markdown
Interactive row/table reformat batches freeze one complete-table snapshot for
their lifetime. Merged shared-cell saves atomically revalidate target content,
the current anchor designation, and exact anchor-group membership; any drift
rejects the whole unit and defers the parent-table reload until the batch modal
closes.
```

- [ ] **Step 4: 验证三份文档都包含契约**

Run:

```bash
rg -n 'freeze|冻结|anchor-group membership|分组成员' README.md README_zh.md AGENTS.md
```

Expected: 三个文件均命中。

- [ ] **Step 5: 提交文档同步**

```bash
git add README.md README_zh.md AGENTS.md
git commit -m "docs: document atomic knowhow reformat saves"
```

---

### Task 5: 完整验证、更新原 PR

**Files:**
- Modify if generated contract drift is reported:
  `backend/tests/fixtures/repository_contract/api_contract.json`
- Modify: PR #306 body（GitHub，不是仓库文件）

**Interfaces:**
- Consumes: Tasks 1–4 的提交。
- Produces: 本地 exact green SHA、fast-forward 更新后的原 PR。

- [ ] **Step 1: 运行 Markdown / API / frontend 聚焦验证**

Run:

```bash
/opt/homebrew/Caskroom/miniconda/base/bin/python -m pytest \
  backend/tests/test_md_normalize_rule.py \
  backend/tests/test_md_normalize_invariant.py \
  backend/tests/test_knowhow_store.py \
  backend/tests/test_knowhow_api.py -q
cd frontend
node --test app/knowhow-normalize.test.mjs app/knowhow-optimize.test.mjs app/knowhow-model.test.mjs
```

Expected: 全部 PASS，0 fail。

- [ ] **Step 2: 运行完整门禁并记录 wall time**

Run:

```bash
cd /Users/hzf/workspace/silicon_notebook/.worktrees/pr306-takeover
time PYTHON_BIN=/opt/homebrew/Caskroom/miniconda/base/bin/python bash scripts/check.sh
```

Expected:

- backend complete pytest PASS；
- contract/harness lane PASS；
- frontend node/component tests PASS；
- `tsc --noEmit` PASS；
- Next build PASS；
- warm wall time < 60s。

- [ ] **Step 3: 检查工作树和提交范围**

Run:

```bash
git status --short
git log --oneline --decorate origin/master..HEAD
git diff --check origin/master...HEAD
```

Expected: 工作树干净；diff check 无输出。

- [ ] **Step 4: fast-forward 推送到原 PR 分支**

Run:

```bash
git push origin HEAD:claude/knowhow-md-normalize-followup
```

Expected: 普通 fast-forward push 成功；无 `--force`。

- [ ] **Step 5: 更新 PR body**

PR body 必须包含：

```markdown
## 接管收口

- merged latest `master` without rewriting the original eight commits
- migrated PR #306 wiring guards to semantic AST / behavioral tests
- kept the deleted line-number repository manifest test deleted
- added fail-closed anchor guard length/table binding checks

## Verification

- focused backend: `<count> passed`
- focused frontend: `<count> passed`
- full `scripts/check.sh`: `<counts and wall time>`
- exact head: `<sha>`

GitHub Actions remains informational and is not a required merge check.
```

---

### Task 6: 独立 subagent 审查 exact pushed SHA

**Files:**
- Modify only if review finds Critical / Important issues.

**Interfaces:**
- Consumes: PR #306 exact pushed green SHA。
- Produces: 独立审查结论；Critical / Important 为零的最终 SHA。

- [ ] **Step 1: 完整读取并使用 `superpowers:requesting-code-review`**

审查请求必须明确：

```text
Repository: huyangc/silicon-notebook
PR: #306
Base: origin/master
Review exact head: <pushed SHA>
Scope:
- preservation of the four original PR #298 follow-ups
- correctness of frozen batch fan-out and anchor structural guard
- semantic-test migration after PR #307
- no source-line/source-slice contracts
- local gate under 60 seconds
Return Critical / Important / Minor with file and line evidence.
```

- [ ] **Step 2: 派独立审查 subagent**

使用：

```text
model = gpt-5.6-terra
reasoning_effort = high
fork_turns = none
```

审查提示必须自包含仓库、PR、base、exact SHA、需求与验证证据；审查者不得复用
实现者结论，必须读取 exact SHA diff 和相关调用链。

- [ ] **Step 3: 处理审查结果**

- Critical / Important：使用 `superpowers:receiving-code-review` 验证后 TDD 修复。
- Minor：记录但不无条件扩 scope；只处理确定、低风险且与本 PR 同域的项。

- [ ] **Step 4: 若发生修改，重新完整验证并复审新 SHA**

Run:

```bash
PYTHON_BIN=/opt/homebrew/Caskroom/miniconda/base/bin/python bash scripts/check.sh
git push origin HEAD:claude/knowhow-md-normalize-followup
```

Expected: 新 head 全绿；再派独立 subagent 审查新的 exact SHA，不能沿用旧结论。

- [ ] **Step 5: 最终交接**

报告：

```text
PR URL
final exact head SHA
merge base
focused/full verification counts and wall time
subagent review result
GitHub Actions informational status
```
